{
  "name": "@pact-network/zerog-compute-client",
  "version": "0.0.1",
  "private": true,
  "description": "Typed wrapper over @0gfoundation/0g-compute-ts-sdk. Compiled CommonJS for build stability against the SDK's require-conditional export; ESM consumers import via the default form.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@0gfoundation/0g-compute-ts-sdk": "0.8.3",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.0"
  }
}
