# Vercel CLI local artifacts: `npx vercel build` output and the linked-project
# metadata (`vercel link` / `vercel pull`). Never committed — the deploy is
# driven by `vercel.json` + the Vercel project settings, not by these files.
.vercel/
