{
  "name": "@pact-network/db-zerog",
  "version": "0.0.1",
  "private": true,
  "description": "Prisma schema for the Pact-0G indexer. Forked from @pact-network/db with EVM-shaped fields (addresses, tx hashes, root hashes).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "clean": "rm -rf dist",
    "typecheck": "prisma generate && tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma"
  }
}
