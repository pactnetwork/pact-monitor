// SPDX-License-Identifier: MIT
pragma solidity ^0.8.30;

/// @title Deploy
/// @notice Deploy-script placeholder for the Arc Testnet rollout.
/// @dev WP-EVM-01 SCAFFOLD ONLY. The real `forge script` deploy (forge-std
///      `Script`, env-driven RPC/key, ownership rotation to a Safe, arcscan
///      verification) is WP-EVM-07. This stub exists so the `script/` layout
///      matches design §7.3 and the project compiles.
contract Deploy {
    function run() external pure {
        revert("NOT_IMPLEMENTED");
    }
}
