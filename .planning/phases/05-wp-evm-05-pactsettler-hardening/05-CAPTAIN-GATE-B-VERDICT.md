# WP-EVM-05 — Captain GATE B Verdict: APPROVED

Verified the 4 seams against settle_batch.rs and spot-checked deviation 2157b75.

## Deviation 2157b75 (inline intendedRefundAfterCap to ev.refund): RATIFIED

Provably parity-neutral. WP-04 declared the local = ev.refund and never mutated it (ev.refund is calldata read-only; grep-confirmed zero reassignment). Solana seeds intended_refund_after_cap = refund_lamports at settle_batch.rs:380 and ev.refund IS refund_lamports, so the ratified P1 predicate payableRefund < intendedRefundAfterCap becomes the byte-equivalent payableRefund < ev.refund; equivalence to Solana intended > cap_remaining holds at every boundary (clamp / no-clamp / == / intended==0). Compiler-forced (stack-too-deep from the new status local), one WP-04 alias line, the seam-pinned recordCallAndCapAccrual signature and passed value unchanged, P1 mechanism / no-rollback / set-before-pool-check all intact. Same posture as the ratified WP-04 GATE-B deviations. APPROVED.

## 4 seams: all verified parity-faithful

1. ProtocolPaused then BatchTooLarge as first two body statements, pre-loop, in that order (settle_batch.rs:99-115 then :132-135); strict > (50 OK, 51 reverts). P3 OPTIMIZED-DIVERGENCE honored (only authorized+paused and resume tested; unauthorized+paused in the WP-06 matrix, not tested). CORRECT.
2. EndpointPaused at the D-LOCK-PREC slot: dedup READ < EndpointPaused < RecipientCoverageMismatch < dedup SET (settle_batch.rs:209, after dedup :194, before RecipientCoverageMismatch :213). LOCKED precedence test stays green + new ordering test. CORRECT.
3. ExposureCapClamped + P1: cap clamp exactly between period-reset and currentPeriodRefunds accrual in recordCallAndCapAccrual; ternary capRemaining parity-exact to saturating_sub (currentPeriodRefunds >= exposureCap => 0, no underflow); signature unchanged; inference status set after the call and before the pool-balance check; currentPeriodRefunds accrues the clamped amount, no rollback. Implemented exactly as ratified at GATE A. CORRECT.
4. PoolDepleted: empty if filled with exactly status=PoolDepleted; actualRefund=0; WP-04 else byte-unchanged; recordRefundPaid skips on depleted (actualRefund==0); D-LOCK-CLAMP-ORDER (cap-clamp then pool-check => PoolDepleted overwrites ExposureCapClamped, mirrors Solana :407-then-:468) proven by the combined test. CORRECT.

102/102 (90 WP-04 regression preserved + 12 WP-05), orchestrator-reproduced not relayed; D-LOCK-PREC test green; adversarial pass (3 vectors) clean; N-A matrix complete with surviving WP-02 authority tests confirmed. Karpathy-clean (additive-only, no WP-02/03/04 reopen except the one ratified compiler-forced alias). WP-EVM-05 is parity-faithful and COMPLETE.

## Closeout (do these in order, then STOP — WP-05 to WP-06 rotation)

1. GSD post-exec verify + mark phase 05 complete in ROADMAP / STATE / REQUIREMENTS.
2. PUSH: git push origin feat/arc-protocol-v1 (all WP-05 commits). PR #204 comment: WP-EVM-05 completion summary — the 4 seams, P1 inference + P3 OPTIMIZED-DIVERGENCE as implemented, the ratified 2157b75 deviation, 102/102 + regression, the N-A matrix rows, and the carried WP-06 spec-defect/divergence log. File-scoped only; tree must stay clean; do NOT touch CLAUDE.md or .claude/skills.
3. UPDATE THE HANDOFF for a fresh WP-06 crew: extend docs/superpowers/handoffs/2026-05-18-arc-evm-port-handoff.md (commit file-scoped, push) with a WP-EVM-05 OUTCOMES (LOCKED) section: (a) WP-05 commit lineage; (b) P1/P3 final implemented forms as locked law (P1 inference predicate payableRefund < ev.refund with the ratified equivalence; the 2157b75 alias inline is locked-accepted; P3 OPTIMIZED-DIVERGENCE corner); (c) the 4 filled seams with final file:line; (d) the COMPLETE WP-06 spec-defect / parity-matrix worklist consolidated: section (d) items 1-4 PLUS locked ruling #8 (D1-scope refinement) PLUS the P3 unauthorized+paused corner PLUS the D2 createdAt divergence PLUS every N-A-ON-EVM row from 05-NA-MATRIX.md and the WP-02/03/04 N-A items — WP-06 must produce the per-variant parity matrix (IDENTICAL / OPTIMIZED-DIVERGENCE / N-A-on-EVM) and FORMALLY CORRECT the design spec for these; (e) WP-06 scope = the @pact-network/protocol-evm-v1-client package (viem, pnpm workspace member, mirrors protocol-v1-client modules), consolidated forge fuzz + gas-snapshot suite, the live IERC20(USDC).decimals()==6 assertion, and the parity-matrix doc + spec corrections; (f) WP-06 is authored-at-turn (NOT necessarily GSD — lighter than the settler WPs; captain decides plan method at WP-06 GATE A), same captain gate cadence (plan-review GATE A + final GATE B), same methodology/conventions/contamination/file-report rules. Keep ALL WP-02/03/04/05 locked rulings intact.
4. Append done-state to 05-REPORT-gateB.md; cockpit notice. Then STOP — do NOT start WP-06. Captain reviews the updated handoff, closes this crew, spawns a fresh WP-06 crew seeded with it.
