---
phase: 05-wp-evm-05-pactsettler-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md
autonomous: false
requirements: [SET-09, SET-10, SET-11, SET-12]
must_haves:
  truths:
    - "Captain has ruled P1 (ExposureCapClamped inference mechanism) and P3 (protocol-paused vs onlyRole modifier-order divergence) before any Solidity is written"
    - "The canonical 05-GATE-A-DECISIONS.md records the captain's verbatim P1 and P3 rulings plus the adversarial-pass decision"
    - "Downstream plans 05-02..05-06 can cite the rulings verbatim"
  artifacts:
    - path: ".planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md"
      provides: "Canonical P1 + P3 + adversarial-pass decision record (mirrors 04-GATE-A-DECISIONS.md)"
      contains: "## P1"
  key_links:
    - from: "05-02..05-06 plan task actions"
      to: "05-GATE-A-DECISIONS.md"
      via: "verbatim citation of the ruled P1/P3 mechanism"
      pattern: "05-GATE-A-DECISIONS"
---

<objective>
Bake the captain's GATE-A rulings on the two OPEN parity decisions (P1, P3) into
the canonical decision record `05-GATE-A-DECISIONS.md`, mirroring the
`04-GATE-A-DECISIONS.md` format. This plan is the GATE-A gate for the entire
phase: NO Solidity in any plan (05-02..05-06) may be written until the captain
has ruled P1 and P3 and this record is finalized.

Purpose: P1 and P3 are OPEN parity decisions (per 05-CONTEXT.md and
05-RESEARCH.md). The planner RECORDS them as captain decision points; it does
NOT resolve them. The lead resolutions are framed in 05-RESEARCH.md "P1 Framing"
and "P3 Framing" sections — these are presented to the captain, not implemented.

Output: `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md`
with the captain's verbatim rulings, so downstream plans cite settled law.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md
@.planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: GATE-A captain ruling on P1 and P3 + adversarial-pass decision</name>
  <read_first>
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md (decisions block: P1, P3, D-LOCK-CLAMP-ORDER, D-LOCK-PROTO-PAUSE)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("P1 Framing", "P3 Framing", "Adversarial Pass Recommendation (GATE A Report)")
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md (format to mirror)
  </read_first>
  <decision>Ratify (a) the P1 ExposureCapClamped inference mechanism + no-rollback + status precedence, (b) the P3 OPTIMIZED-DIVERGENCE for the unauthorized+paused corner, and (c) whether the WP-05 settlement adversarial pass is enabled.</decision>
  <context>
    The orchestrator writes 05-REPORT-gateA.md (verbatim P1 Framing + P3 Framing
    + Adversarial Pass Recommendation from 05-RESEARCH.md) and WAITS for the
    captain. Per 05-CONTEXT.md these are settled-law-pending decisions, NOT
    planner-resolvable. The seam constraints are LOCKED: P1 must NOT change the
    seam-pinned recordCallAndCapAccrual signature; P3's alternative requires a
    forbidden WP-04 rewrite.
  </context>
  <options>
    <option id="p1-infer">
      <name>P1: infer ExposureCapClamped in _settleSuccess via payableRefund &lt; intendedRefundAfterCap; no rollback; set before pool-balance check so PoolDepleted overwrites</name>
      <pros>Seam-pinned signature unchanged; provably equivalent to Solana intended &gt; cap_remaining; matches D-LOCK-CLAMP-ORDER exactly</pros>
      <cons>Status inferred rather than returned (requires the equivalence proof to hold — proven in 05-RESEARCH.md)</cons>
    </option>
    <option id="p3-optdiv">
      <name>P3: accept OPTIMIZED-DIVERGENCE — port only authorized-settler+paused → ProtocolPaused; document the unauthorized+paused corner in WP-06 matrix</name>
      <pros>No WP-04 rewrite; production path (authorized settler) is bit-identical on both chains</pros>
      <cons>Unauthorized+paused returns AccessControl error on EVM vs ProtocolPaused on Solana (caller rejected either way)</cons>
    </option>
    <option id="adv-pass">
      <name>Adversarial pass: enable a brief manual review of the 4 WP-05 seams (cap-saturation cross-agent, empty-batch bypass, PoolDepleted reentrancy) — not a fuzz suite (WP-06)</name>
      <pros>Cheap, narrow, recommended by WP-04 OUTCOMES §e + 05-RESEARCH.md</pros>
      <cons>None material; Nyquist stays off per config</cons>
    </option>
  </options>
  <action>
    The orchestrator presents 05-REPORT-gateA.md to the captain and WAITS. On
    captain ruling, create
    `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md`
    mirroring the structure of `04-GATE-A-DECISIONS.md`:
    - Header: "WP-EVM-05 — GATE A Decision Record", status RULED by captain
      &lt;date&gt;, source authority settle_batch.rs, D-LOCK-1 precedence note.
    - `## P1 — ExposureCapClamped status conveyance` — captain ruling verbatim
      (blockquote-free per feedback_no_blockquotes), then binding consequences:
      the inference predicate `payableRefund < intendedRefundAfterCap`, the
      no-rollback invariant (D-LOCK-CLAMP-ORDER), and the precedence
      (set ExposureCapClamped BEFORE the pool-balance check so PoolDepleted at
      PactSettler.sol:200-205 overwrites it). Pin recordCallAndCapAccrual
      signature UNCHANGED (PactRegistry.sol:244-249).
    - `## P3 — protocol-paused vs onlyRole(SETTLER_ROLE) modifier order` —
      captain ruling verbatim + binding consequence: port authorized-settler
      +paused → ProtocolPaused and resume-after-unpause only; record the
      unauthorized+paused corner as a WP-06 parity-matrix line.
    - `## Adversarial pass` — captain decision (enabled/deferred) + scope.
    Use plain paragraphs (no `>` blockquotes — feedback_no_blockquotes). No
    emojis. Conventional-commit the file (file-scoped; D-LOCK-6).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md &amp;&amp; grep -q "## P1" .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md &amp;&amp; grep -q "## P3" .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md &amp;&amp; grep -qi "RULED" .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md` exists.
    - `grep -c "^## " 05-GATE-A-DECISIONS.md` ≥ 3 (P1, P3, Adversarial pass sections present).
    - `grep -q "RULED" 05-GATE-A-DECISIONS.md` succeeds (captain ruling captured, not "OPEN"/"PENDING").
    - `grep -q "recordCallAndCapAccrual" 05-GATE-A-DECISIONS.md` succeeds (P1 seam-signature pin recorded).
    - `grep -L ">" 05-GATE-A-DECISIONS.md` returns the file (NO `>` blockquote lines — feedback_no_blockquotes).
    - No emoji bytes in file (`grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" 05-GATE-A-DECISIONS.md` returns nothing).
  </acceptance_criteria>
  <resume-signal>Captain types the P1/P3/adversarial rulings; then "approved — proceed to 05-02".</resume-signal>
  <done>05-GATE-A-DECISIONS.md committed with verbatim captain rulings on P1, P3, and the adversarial-pass decision. Downstream plans (05-02..05-06) are unblocked to cite it as settled law. NO Solidity has been written.</done>
</task>

</tasks>

<verification>
- 05-GATE-A-DECISIONS.md exists with P1, P3, and adversarial-pass sections.
- Captain ruling is captured verbatim (status RULED, not OPEN).
- recordCallAndCapAccrual signature pin is recorded for P1.
- No `>` blockquote lines; no emojis (D-LOCK-6 / feedback_no_blockquotes).
- No Solidity files touched by this plan (files_modified is the decision record only).
</verification>

<success_criteria>
Captain has ruled P1 and P3; 05-GATE-A-DECISIONS.md is the canonical settled-law
record that 05-02..05-06 cite verbatim. The phase's GATE-A gate is satisfied —
implementation plans may now execute in dependency order.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-01-SUMMARY.md`
</output>
