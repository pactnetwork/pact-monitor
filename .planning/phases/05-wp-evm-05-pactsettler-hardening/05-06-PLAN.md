---
phase: 05-wp-evm-05-pactsettler-hardening
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
  - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md
autonomous: false
requirements: [SET-09, SET-10, SET-11, SET-12]
notes: "Serial chain — all of 05-02..05-06 mutate PactSettler.sol/.t.sol; parallelization is enabled in config so depends_on MUST enforce sequential execution to prevent same-file concurrent-edit corruption. Seam independence is preserved (different anchors); only the file-write ordering is serialized."
must_haves:
  truths:
    - "Every PORT-disposition scenario in the 05-CONTEXT.md PORT/N-A table has a corresponding green Foundry test (settle-side only for 06/10; not duplicating WP-02 registry-side coverage)"
    - "Every N-A-ON-EVM scenario (09:47/78/144/172, 10:94/128) has a one-line WP-06-matrix rationale recorded in 05-NA-MATRIX.md plus a named surviving authority-invariant test already in WP-02"
    - "forge build && forge test is fully green: ≥90 prior WP-04 tests + all WP-05 tests, 0 failed"
    - "The orchestrator writes 05-REPORT-gateB.md and WAITS for the captain — NO git push, NO PR #204 comment"
  artifacts:
    - path: ".planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md"
      provides: "N-A-ON-EVM rationale lines + surviving-invariant pointers for the WP-06 parity matrix"
      contains: "N-A-ON-EVM"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "Completed WP-05 settle-side test port (any remaining 05/06/07 scenarios + the 07:25 cumulative if not already added)"
      contains: "function test_"
  key_links:
    - from: "05-NA-MATRIX.md"
      to: "WP-02 PactRegistry.t.sol authority-invariant tests"
      via: "each N-A line names the surviving onlyAuthority test that already covers the residual behavior"
      pattern: "test_(Pause|UpdateEndpointConfig|UpdateFeeRecipients|PauseProtocol).*RejectsNonAuthority|test_PauseEndpoint_RejectsNonAuthority"
---

<objective>
Complete the WP-05 test port: confirm every PORT-disposition scenario in the
05-CONTEXT.md PORT/N-A table has a green test (filling any gap left by
05-02..05-05 — e.g. dedupe 06/10 to settle-side only since WP-02
PactRegistry.t.sol already covers registry-side; add the 07:25 cumulative-cap
scenario if 05-04 left it as a single case). Record every N-A-ON-EVM scenario
(09:47/78/144/172, 10:94/128) with a one-line WP-06-matrix rationale plus the
already-existing surviving authority-invariant test in WP-02. Run the full
WP-02/03/04/05 regression. Then the orchestrator writes 05-REPORT-gateB.md and
WAITS for the captain (no push / no PR #204 comment).

Purpose: SET-09..12 closure verification — the phase goal requires the ported
pause/exposure/auth tests green AND the full regression green. This plan does
not add new production Solidity (05-02..05-05 own the seams); it closes the test
port and the N-A matrix and runs the GATE B regression.

Output: complete green WP-05 test port; 05-NA-MATRIX.md; full forge regression
green; GATE B report written; captain WAIT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md
@packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
@packages/program-evm/protocol-evm-v1/test/PactRegistry.t.sol

<interfaces>
<!-- 05-CONTEXT.md PORT vs N-A split (authoritative) + 05-RESEARCH.md WP-02 dedup -->

PORT scenarios already owned by prior WP-05 plans:
- 05:442 PoolDepleted -> 05-05; 05:485 ExposureCapClamped -> 05-04;
  05:529 ProtocolPaused / 05:595 resume -> 05-02; 06 EndpointPaused settle-side
  + dedup-precedence -> 05-03; 07:25 cumulative cap -> 05-04;
  07:81 1-hour reset -> 05-04; BatchTooLarge 51/50 -> 05-02.

WP-02 PactRegistry.t.sol ALREADY covers (do NOT duplicate — 05-RESEARCH.md
"WP-02/03 Coverage Dedup"):
- 06:12/34/55 -> test_PauseEndpoint_SetsPaused / _CanUnpause / _RejectsNonAuthority
- 10:28/72 -> test_PauseProtocol_SetAndClear / _RejectsNonAuthority

N-A-ON-EVM (PDA/owner spoof — §4#2/#6), surviving invariant ALREADY in WP-02:
- 09:47, 09:78 -> pauseEndpoint onlyAuthority (test_PauseEndpoint_RejectsNonAuthority)
- 09:144 -> updateEndpointConfig onlyAuthority (test_UpdateEndpointConfig_RejectsNonAuthority)
- 09:172 -> updateFeeRecipients onlyAuthority (test_UpdateFeeRecipients_RejectsNonAuthority)
- 10:94, 10:128 -> pauseProtocol onlyAuthority (test_PauseProtocol_RejectsNonAuthority)
- D-LOCK-EXPCAP-NA: ExposureCapExceeded — "no settle_batch trigger; the cap clamps to a status, it does not reject."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the WP-05 settle-side test port + write the N-A matrix</name>
  <read_first>
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md ("PORT vs N-A split for the deferred test ports" table — the authoritative disposition list)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("WP-02/03 Coverage Dedup", "Test Port Plan", "09-auth-pda.test.ts: All N-A-ON-EVM", "10-pause-protocol.test.ts")
    - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol (current WP-05 tests added by 05-02..05-05 — inventory what exists before adding)
    - packages/program-evm/protocol-evm-v1/test/PactRegistry.t.sol (confirm the named WP-02 surviving-invariant tests exist: test_PauseEndpoint_RejectsNonAuthority, test_PauseProtocol_RejectsNonAuthority, test_UpdateEndpointConfig_RejectsNonAuthority, test_UpdateFeeRecipients_RejectsNonAuthority)
  </read_first>
  <files>packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol, .planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md</files>
  <action>
    Step 1 — Inventory: list every `function test_` added to PactSettler.t.sol by
    05-02..05-05 (`grep -n "function test_" packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol`).
    Cross-check against the 05-CONTEXT.md PORT/N-A table. For every PORT-row not
    yet present, add the test using the verbatim Foundry port from
    05-RESEARCH.md "Test Port Plan". Specifically ensure 07:25 has BOTH the
    under-cap (Settled, full 600k) AND the over-cap-remaining (ExposureCapClamped,
    400k) sub-cases if 05-04 only added the single clamp case. Do NOT duplicate
    any WP-02 registry-side test (06:12/34/55, 10:28/72 are ALREADY in
    PactRegistry.t.sol — settle-side only here, per 05-RESEARCH.md dedup table).
    Convert any Solana `cr[]` byte-asserts to `vm.expectEmit` on
    `IPactSettler.CallSettled` status/refund (WP-04 E4 pattern).

    Step 2 — N-A matrix: create
    `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md` with a
    table: `| Source scenario | Disposition | Rationale (1 line for WP-06 matrix) | Surviving invariant test |`.
    One row per N-A-ON-EVM scenario (09:47, 09:78, 09:144, 09:172, 10:94,
    10:128) + the D-LOCK-EXPCAP-NA ExposureCapExceeded row. Rationale verbatim
    style: "PDA-address/owner spoofing is not possible on EVM — all privileged
    mutators are onlyAuthority (single address authority field); the
    authority-revert invariant is source-enforced and already tested in WP-02."
    Each row names the exact WP-02 test (e.g. `test_PauseEndpoint_RejectsNonAuthority`)
    and confirms it currently passes. No emojis; plain paragraphs (no `>`
    blockquotes — feedback_no_blockquotes). Conventional commit, file-scoped.
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | tail -3 && test -f /Users/q3labsadmin/Q3/Solder/pact-network/.planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md && grep -c "N-A-ON-EVM" /Users/q3labsadmin/Q3/Solder/pact-network/.planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - Every PORT row in the 05-CONTEXT.md PORT/N-A table maps to a present `function test_` in PactSettler.t.sol (auditable: list grep output against the table; zero PORT rows missing).
    - No WP-02 registry-side scenario (06:12/34/55, 10:28/72) is re-implemented in PactSettler.t.sol (`grep -c "function test_PauseEndpoint_SetsPaused\|function test_PauseProtocol_SetAndClear" test/PactSettler.t.sol` == 0).
    - `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md` exists; `grep -c "N-A-ON-EVM" 05-NA-MATRIX.md` ≥ 6 (09:47/78/144/172, 10:94/128) plus the ExposureCapExceeded D-LOCK-EXPCAP-NA row.
    - Each N-A row names a WP-02 test; `forge test --match-contract PactRegistry --match-test "RejectsNonAuthority"` shows those PASS (surviving invariants confirmed live).
    - `grep -L ">" 05-NA-MATRIX.md` returns the file (no `>` blockquote lines); no emoji bytes.
    - `forge test` full suite: 0 failed.
  </acceptance_criteria>
  <done>WP-05 settle-side test port is complete (every PORT row green, no WP-02 duplication); 05-NA-MATRIX.md records all N-A-ON-EVM rationales with named surviving WP-02 invariant tests confirmed passing.</done>
</task>

<task type="auto">
  <name>Task 2: Full WP-02/03/04/05 regression — forge build && forge test</name>
  <read_first>
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("Test Runner Facts", "Validation Architecture" — GATE B = forge build && forge test all green)
    - .planning/STATE.md (WP-04 baseline: 90/90 green; regression gate)
  </read_first>
  <files>packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol</files>
  <action>
    From `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`
    run `forge build` then `forge test`. The full WP-02/03/04/05 suite MUST be
    green: the ≥90 prior WP-04 tests unchanged + all WP-05 tests added by
    05-02..05-06, 0 failed. If ANY prior test regressed, STOP — that means a
    WP-05 seam reordered/rewrote WP-04 (forbidden, additive-only); report the
    regression rather than patching the test. Confirm the working tree is clean
    except `?? .planning/...` and `?? .claude/pr-reviews/` (D-LOCK-6
    contamination check: if `CLAUDE.md` shows ` M` or `.claude/skills/pact/`
    appears, STOP and report — contamination incident #5). Do NOT push, do NOT
    comment on PR #204.
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | grep -E "Suite result|failed|passed" | tail -5 && cd /Users/q3labsadmin/Q3/Solder/pact-network && git status --porcelain | grep -E "^ M CLAUDE.md|\.claude/skills/pact/" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - `forge build` exits 0 (clean compile).
    - `forge test` reports 0 failed; total passing ≥ 90 (WP-04 baseline) + the WP-05 additions (05-02: 4, 05-03: 3, 05-04: 3, 05-05: 2, plus any 05-06 fills) — i.e. ≥ 102 passing.
    - The original 90 WP-04 test names still pass (no regression; spot-check `forge test --match-test "test_DuplicateCallIdPrecedesRecipientCoverageMismatch"` PASSES).
    - `git status --porcelain | grep -E "^ M CLAUDE.md|.claude/skills/pact/"` returns NOTHING (no contamination — D-LOCK-6).
    - No `git push` executed; no PR #204 comment posted (verify no network push in this plan's actions).
  </acceptance_criteria>
  <done>forge build clean; full WP-02/03/04/05 suite green (≥102 passing, 0 failed); WP-04 baseline preserved; working tree uncontaminated; no push / no PR comment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GATE B — captain parity sign-off</name>
  <read_first>
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md (P1/P3 rulings the impl must honor)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-NA-MATRIX.md
    - .planning/STATE.md (gate cadence: GATE B = report + WAIT, no push / no PR comment until approved)
  </read_first>
  <files>.planning/phases/05-wp-evm-05-pactsettler-hardening/05-REPORT-gateB.md</files>
  <what-built>
    All four WP-05 seams filled bit-identically to settle_batch.rs as pure
    additive edits: ProtocolPaused fast-revert + BatchTooLarge pre-loop (05-02),
    per-event EndpointPaused at the D-LOCK-PREC slot (05-03), exposure-cap clamp
    + P1 ExposureCapClamped inference (05-04), PoolDepleted clamp with
    D-LOCK-CLAMP-ORDER precedence + no-rollback (05-05). Full ported test port
    + N-A matrix; full WP-02/03/04/05 forge regression green (≥102 passing).
  </what-built>
  <action>
    The orchestrator writes
    `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-REPORT-gateB.md`
    summarizing: the four seams + their settle_batch.rs anchors, the P1/P3
    rulings honored, the full PORT/N-A coverage (pointing at 05-NA-MATRIX.md),
    the `forge build && forge test` result (≥102 passing / 0 failed), the
    additive-only diff proof (no WP-04 deletions, IPactRegistry signature
    unchanged), and the adversarial-pass outcome (per 05-GATE-A-DECISIONS.md).
    The orchestrator ALSO sends a short `cockpit runtime send pact-network`
    notice (relay unreliable — feedback_crew_report_to_file). Then it WAITS for
    the captain. NO `git push`, NO PR #204 comment until the captain explicitly
    approves (D-LOCK-6 gate cadence).
  </action>
  <verify>
    <automated>test -f /Users/q3labsadmin/Q3/Solder/pact-network/.planning/phases/05-wp-evm-05-pactsettler-hardening/05-REPORT-gateB.md && cd /Users/q3labsadmin/Q3/Solder/pact-network && git log origin/feat/arc-protocol-v1..HEAD --oneline 2>/dev/null | head -1; echo "no-push-verified"</automated>
  </verify>
  <how-to-verify>
    1. `cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 && forge build && forge test` — expect 0 failed, ≥102 passing.
    2. Spot-check the four seams are additive: `git diff --stat <phase-base>..HEAD -- packages/program-evm/protocol-evm-v1/src/` shows additions in PactSettler.sol + PactRegistry.sol only, no WP-04 logic deletions.
    3. Confirm recordCallAndCapAccrual signature unchanged (P1 pin): `git diff <phase-base>..HEAD -- packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol` is empty.
    4. Confirm test_DuplicateCallIdPrecedesRecipientCoverageMismatch still passes (LOCKED precedence preserved).
    5. Review 05-NA-MATRIX.md against the 05-CONTEXT.md PORT/N-A table.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-REPORT-gateB.md` exists and references the four seam anchors + the forge result.
    - No commits exist on HEAD ahead of `origin/feat/arc-protocol-v1` that were pushed by this plan (no `git push` executed — verify `git log origin/feat/arc-protocol-v1..HEAD` content was NOT pushed).
    - No PR #204 comment posted by this plan.
    - Captain has explicitly approved before any downstream push/PR-comment step (resume-signal captured).
  </acceptance_criteria>
  <resume-signal>Captain types "GATE B approved" (or lists parity defects to fix before push).</resume-signal>
  <done>Captain has reviewed parity + regression and approved GATE B. Only after explicit approval may a later step push / comment on PR #204 (out of this plan's scope).</done>
</task>

</tasks>

<verification>
- Every 05-CONTEXT.md PORT row has a green test; no WP-02 registry-side duplication.
- 05-NA-MATRIX.md records all N-A-ON-EVM rationales + named passing WP-02 surviving-invariant tests.
- forge build clean; full WP-02/03/04/05 suite ≥102 passing, 0 failed; WP-04 baseline preserved.
- Working tree uncontaminated (no CLAUDE.md M, no .claude/skills/pact/).
- GATE B report written; captain WAIT; NO push / NO PR #204 comment.
</verification>

<success_criteria>
SET-09..12 are fully verified: the ported pause/exposure/auth tests are green,
the N-A matrix is recorded for WP-06, and the full WP-02/03/04/05 forge
regression is green with the WP-04 90/90 baseline preserved. The captain has the
GATE B report and the phase WAITS for sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-06-SUMMARY.md`
</output>
