---
phase: 05-wp-evm-05-pactsettler-hardening
plan: 03
type: tdd
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
autonomous: true
requirements: [SET-11]
notes: "Serial chain — all of 05-02..05-06 mutate PactSettler.sol/.t.sol; parallelization is enabled in config so depends_on MUST enforce sequential execution to prevent same-file concurrent-edit corruption. Seam independence is preserved (different anchors); only the file-write ordering is serialized."
must_haves:
  truths:
    - "An event whose endpoint is paused reverts EndpointPaused"
    - "After the endpoint is unpaused the same endpoint settles successfully"
    - "EndpointPaused fires AFTER the DuplicateCallId dedup READ and BEFORE RecipientCoverageMismatch (LOCKED D-LOCK-PREC slot)"
    - "A replayed callId on a paused endpoint reverts DuplicateCallId (dedup READ precedes EndpointPaused)"
    - "The LOCKED WP-04 precedence test test_DuplicateCallIdPrecedesRecipientCoverageMismatch still passes (no WP-04 reorder)"
    - "The WP-04 90/90 forge regression stays green"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "Per-event EndpointPaused check inserted at the D-LOCK-PREC slot"
      contains: "if (ep.paused) revert EndpointPaused();"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "EndpointPaused per-event + dedup-precedence + resume tests"
      contains: "test_EndpointPaused_RevertsPerEvent"
  key_links:
    - from: "PactSettler.settleBatch per-event loop"
      to: "ep.paused (from registry.getEndpoint)"
      via: "EndpointPaused check after getEndpoint, after dedup READ, before RecipientCoverageMismatch"
      pattern: "if \\(ep\\.paused\\) revert EndpointPaused\\(\\)"
---

<objective>
Insert the per-event `EndpointPaused` revert into `PactSettler.settleBatch` at
the LOCKED D-LOCK-PREC slot: AFTER the `DuplicateCallId` dedup READ
(PactSettler.sol:84) and the `getEndpoint` call, and BEFORE the
`RecipientCoverageMismatch` check (PactSettler.sol:90-91). Ports
`settle_batch.rs:209` (`if ep.paused != 0 -> EndpointPaused`). This is the ONLY
new per-event precedence slot and it is purely additive — no WP-04 reordering.

Purpose: SET-11 (endpoint-paused per event). The LOCKED precedence
(test_DuplicateCallIdPrecedesRecipientCoverageMismatch) MUST still hold; a
dedup-READ-before-EndpointPaused regression test pins the new ordering.

Output: per-event EndpointPaused check at the D-LOCK-PREC slot; 3 new tests
(per-event revert, resume, dedup-READ-precedence); WP-04 90/90 preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs

<interfaces>
<!-- Verified from source 2026-05-18. -->

PactSettler.sol:84-91 (WP-04 shipped — the exact D-LOCK-PREC slot):
```solidity
if (_settledCallIds[ev.callId]) revert DuplicateCallId();   // :84 dedup READ

// Step 4: endpoint snapshot — settle_batch.rs:200-221.
// Endpoint-paused check (settle_batch.rs:209-211) is WP-05; skip.
IPactRegistry.EndpointConfig memory ep =
    IPactRegistry(address(registry)).getEndpoint(ev.endpointSlug);   // :88-89
if (ep.feeRecipientCount != ev.feeRecipientCountHint)              // :90-91
    revert RecipientCoverageMismatch();
```

settle_batch.rs:194 dedup READ -> :209 `if ep.paused != 0 -> EndpointPaused` ->
:213 `ep_count != fee_count_hint -> RecipientCoverageMismatch`. (Verified.)

IPactRegistry.EndpointConfig.paused is `bool paused` (IPactRegistry.sol:25).
PactErrors: `error EndpointPaused();` (confirmed PactErrors.sol).
Registry endpoint pause toggle: `reg.pauseEndpoint(bytes16 slug, bool)` via `vm.prank(authority)`.
Existing LOCKED test: test_DuplicateCallIdPrecedesRecipientCoverageMismatch (WP-04, must stay green).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing EndpointPaused per-event / resume / dedup-precedence tests</name>
  <read_first>
    - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol (harness + the existing test_DuplicateCallIdPrecedesRecipientCoverageMismatch to mirror its dedup-precedence pattern)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs (:194 dedup READ, :209 EndpointPaused, :213 RecipientCoverageMismatch)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("06-pause.test.ts: Settle-side enforcement" — test_EndpointPaused_RevertsPerEvent, test_EndpointPaused_CanResumeAfterUnpause, test_EndpointPaused_DedupReadPrecedesEndpointPaused)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md (D-LOCK-PREC)
  </read_first>
  <behavior>
    - test_EndpointPaused_RevertsPerEvent: reg.pauseEndpoint(SLUG, true) -> settleBatch reverts EndpointPaused.selector.
    - test_EndpointPaused_CanResumeAfterUnpause: pause then unpause endpoint -> settleBatch succeeds (no revert).
    - test_EndpointPaused_DedupReadPrecedesEndpointPaused: consume callId while live; pause endpoint; replay same callId -> reverts DuplicateCallId (NOT EndpointPaused) — proves dedup READ precedes EndpointPaused (D-LOCK-PREC).
  </behavior>
  <action>
    Append three test functions to `PactSettler.t.sol`, using the verbatim
    Foundry ports in 05-RESEARCH.md "06-pause.test.ts: Settle-side enforcement":
    - `test_EndpointPaused_RevertsPerEvent`: `_register(SLUG); _fundPool(SLUG, 5_000_000)`;
      provision agent; `vm.prank(authority); reg.pauseEndpoint(SLUG, true);`
      1-event batch `_makeEvent(0x60, agent, SLUG, 1_000, 0, 50, false, 1, 1)`;
      `vm.prank(settlerSigner); vm.expectRevert(EndpointPaused.selector); settler.settleBatch(events);`
    - `test_EndpointPaused_CanResumeAfterUnpause`: pause then
      `vm.prank(authority); reg.pauseEndpoint(SLUG, false);` then a
      `_makeEvent(0x61, ...)` batch; `vm.prank(settlerSigner); settler.settleBatch(events);`
      (must NOT revert).
    - `test_EndpointPaused_DedupReadPrecedesEndpointPaused`: settle `_makeEvent(0x62, ...)`
      while endpoint is live (consumes callId); `vm.prank(authority); reg.pauseEndpoint(SLUG, true);`
      replay the SAME event array; `vm.prank(settlerSigner); vm.expectRevert(DuplicateCallId.selector); settler.settleBatch(ev1);`
      (DuplicateCallId, NOT EndpointPaused — D-LOCK-PREC).

    Do NOT modify PactSettler.sol. Run the three tests; confirm RED:
    test_EndpointPaused_RevertsPerEvent fails because no EndpointPaused check
    exists (batch proceeds, no/incorrect revert). test_EndpointPaused_DedupReadPrecedesEndpointPaused
    may pass pre-impl (dedup READ already at :84) — it is the precedence
    regression guard; document that its RED is conditional on impl ordering.
    Commit test additions only (file-scoped, conventional, D-LOCK-6).
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 && forge test --match-test "test_EndpointPaused_RevertsPerEvent|test_EndpointPaused_CanResumeAfterUnpause" -vvv 2>&1 | grep -E "FAIL|\[FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - `forge test --match-test "test_EndpointPaused_RevertsPerEvent" -vvv` shows FAIL (RED) — no EndpointPaused check exists yet (call did not revert with EndpointPaused).
    - The FAIL is a behavioral assertion failure, NOT a compile error (tests compile and run).
    - `grep -c "function test_EndpointPaused" packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol` == 3.
    - `git diff --name-only` lists only `test/PactSettler.t.sol` (PactSettler.sol unchanged this task).
  </acceptance_criteria>
  <done>Three EndpointPaused tests added and committed; test_EndpointPaused_RevertsPerEvent confirmed RED for the expected reason (no per-event pause check).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — insert EndpointPaused at the D-LOCK-PREC slot</name>
  <read_first>
    - packages/program-evm/protocol-evm-v1/src/PactSettler.sol (:84 dedup READ, :88-89 getEndpoint, :90-91 RecipientCoverageMismatch — the exact insertion gap between :89 and :90)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs (:209 — `if ep.paused != 0 { return Err(PactError::EndpointPaused.into()); }` sits between the dedup READ at :194 and RecipientCoverageMismatch at :213)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md (D-LOCK-PREC: dedup READ -> EndpointPaused -> RecipientCoverageMismatch; WP-05's EndpointPaused is the ONLY new per-event slot, additive)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("Seam 3b" + Pitfall 4 — EndpointPaused MUST NOT precede the dedup READ)
  </read_first>
  <action>
    In `packages/program-evm/protocol-evm-v1/src/PactSettler.sol`, in the
    `settleBatch` per-event loop, insert ONE statement immediately AFTER the
    `getEndpoint` assignment (current PactSettler.sol:88-89, the `ep` local) and
    immediately BEFORE the `if (ep.feeRecipientCount != ev.feeRecipientCountHint)`
    line (current PactSettler.sol:90-91). Replace the WP-04 placeholder comment
    `// Endpoint-paused check (settle_batch.rs:209-211) is WP-05; skip.` with the
    implementation (port of settle_batch.rs:209):

    ```solidity
    IPactRegistry.EndpointConfig memory ep =
        IPactRegistry(address(registry)).getEndpoint(ev.endpointSlug);
    // settle_batch.rs:209 — EndpointPaused (SET-11). D-LOCK-PREC slot:
    // AFTER the DuplicateCallId dedup READ (:84) and getEndpoint, BEFORE
    // RecipientCoverageMismatch. Pure additive insert — no WP-04 reorder.
    if (ep.paused) revert EndpointPaused();
    if (ep.feeRecipientCount != ev.feeRecipientCountHint)
        revert RecipientCoverageMismatch();
    ```

    `ep.paused` is `bool` (IPactRegistry.EndpointConfig); Solana's
    `ep.paused != 0` maps to the bool truthiness. `EndpointPaused()` is in
    PactErrors.sol (already imported). Do NOT move the `_settledCallIds` dedup
    READ (:84), do NOT touch the dedup SET (:98), the guards (:71-74), or
    `_settleSuccess`. The insert sits strictly between getEndpoint and
    RecipientCoverageMismatch — the LOCKED precedence
    (test_DuplicateCallIdPrecedesRecipientCoverageMismatch) is preserved because
    dedup READ at :84 is unchanged and still precedes this new check.
    Run full suite; confirm GREEN + 90/90 regression + the LOCKED dedup-precedence
    test still passes. Commit src change file-scoped (conventional, D-LOCK-6).
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | tail -3 && forge test --match-test "test_DuplicateCallIdPrecedesRecipientCoverageMismatch|test_EndpointPaused" 2>&1 | grep -E "PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - `grep -n "if (ep.paused) revert EndpointPaused();" src/PactSettler.sol` returns exactly one line.
    - Line order in settleBatch: the `if (_settledCallIds[ev.callId]) revert DuplicateCallId();` line < the `getEndpoint` line < the `if (ep.paused) revert EndpointPaused();` line < the `revert RecipientCoverageMismatch();` line (verify by line numbers; D-LOCK-PREC).
    - `grep -q "Endpoint-paused check (settle_batch.rs:209-211) is WP-05; skip." src/PactSettler.sol` returns NOTHING (WP-04 placeholder comment replaced).
    - `forge build` succeeds.
    - `forge test --match-test "test_EndpointPaused_RevertsPerEvent|test_EndpointPaused_CanResumeAfterUnpause|test_EndpointPaused_DedupReadPrecedesEndpointPaused"` — all 3 PASS (GREEN).
    - `forge test --match-test "test_DuplicateCallIdPrecedesRecipientCoverageMismatch"` PASSES (LOCKED WP-04 precedence preserved).
    - `forge test` full suite: 0 failed; total ≥ 93 (90 WP-04 + 3 new); prior 90 still pass.
    - `git diff src/PactSettler.sol | grep '^-' | grep -v '^---'` shows only the removed WP-04 placeholder comment line(s) — no WP-04 logic line deleted/reordered.
  </acceptance_criteria>
  <done>EndpointPaused check inserted strictly between getEndpoint and RecipientCoverageMismatch, after the dedup READ. 3 new tests GREEN; LOCKED dedup-precedence test still GREEN; full forge suite green with ≥90 prior tests passing.</done>
</task>

</tasks>

<verification>
- EndpointPaused check is between getEndpoint and RecipientCoverageMismatch, after the dedup READ (line-order grep).
- Solana parity: settle_batch.rs:209 ported at the D-LOCK-PREC slot (dedup READ :194 -> EndpointPaused :209 -> RecipientCoverageMismatch :213).
- test_DuplicateCallIdPrecedesRecipientCoverageMismatch still GREEN (no WP-04 reorder).
- 3 new tests GREEN; full forge suite green; ≥90 prior WP-04 tests still pass.
- Only the WP-04 placeholder comment is removed; no WP-04 logic deleted.
</verification>

<success_criteria>
SET-11 (endpoint-paused per event) is ported bit-identically to
settle_batch.rs:209 at the LOCKED D-LOCK-PREC slot as a pure additive insert.
The LOCKED WP-04 guard precedence is preserved and pinned by a regression test.
WP-04 90/90 preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-03-SUMMARY.md`
</output>
