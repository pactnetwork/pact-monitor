---
phase: 05-wp-evm-05-pactsettler-hardening
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
autonomous: true
requirements: [SET-11, SET-12]
must_haves:
  truths:
    - "An authorized settler calling settleBatch while the protocol is paused reverts ProtocolPaused before any per-event work or token transfer; no balances move"
    - "After registry.pauseProtocol(false) the same authorized settler can settle a fresh callId successfully"
    - "A batch of 51 events reverts BatchTooLarge; a batch of exactly 50 events is accepted"
    - "ProtocolPaused fast-revert executes BEFORE the BatchTooLarge check, which executes BEFORE the per-event loop"
    - "The WP-04 90/90 forge regression stays green (no WP-04 reorder/rewrite)"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "ProtocolPaused fast-revert + BatchTooLarge pre-loop checks at the settleBatch body head"
      contains: "registry.protocolPaused()"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "ProtocolPaused / resume / BatchTooLarge ported tests"
      contains: "test_ProtocolPaused_RejectsSettleBatch"
  key_links:
    - from: "PactSettler.settleBatch body head"
      to: "registry.protocolPaused()"
      via: "first statement in the function body, before BatchTooLarge and the loop"
      pattern: "if \\(registry\\.protocolPaused\\(\\)\\) revert ProtocolPaused\\(\\)"
    - from: "PactSettler.settleBatch body head"
      to: "ArcConfig.MAX_BATCH_SIZE"
      via: "events.length > MAX_BATCH_SIZE check, after ProtocolPaused, before the loop"
      pattern: "events\\.length > ArcConfig\\.MAX_BATCH_SIZE"
---

<objective>
Add the two PRE-loop kill-switch/edge guards to the head of
`PactSettler.settleBatch`: the protocol-paused fast-revert (SET-11, P3-dependent)
and the BatchTooLarge edge (SET-12). Ports `settle_batch.rs:99-115` (protocol
pause) and `settle_batch.rs:132-135` (batch size). Pure additive insertion at
the body head — the WP-04 loop and all guard paths are unchanged.

Purpose: SET-11 (protocol-paused fast-revert) + SET-12 (BatchTooLarge edge). The
protocol-paused path is GATE-A-blocked on P3 (05-01 must rule the
OPTIMIZED-DIVERGENCE first); the parity-relevant path ported here is
authorized-settler+paused → ProtocolPaused and resume-after-unpause.

Output: ProtocolPaused + BatchTooLarge checks at the settleBatch body head;
3 new ported tests; full WP-04 90/90 regression preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md
@.planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs

<interfaces>
<!-- Verified from source 2026-05-18 — executor uses these directly. -->

PactSettler.settleBatch body head (PactSettler.sol:60-65), WP-04 shipped:
```solidity
function settleBatch(SettlementEvent[] calldata events)
    external
    override
    onlyRole(SETTLER_ROLE)
{
    for (uint256 i = 0; i < events.length; i++) {
```

IPactRegistry getter (confirmed present, IPactRegistry.sol:69 / PactRegistry.sol:36):
```solidity
bool public override protocolPaused;   // toggled by registry.pauseProtocol(bool)
```

ArcConfig (confirmed ArcConfig.sol:25): `uint16 constant MAX_BATCH_SIZE = 50;`

PactErrors (confirmed PactErrors.sol): `error ProtocolPaused();` `error BatchTooLarge();`

Harness (PactSettler.t.sol): `reg` (PactRegistry), `settler` (PactSettler),
`authority`, `settlerSigner`, `usdc`, `pool`, `SLUG`; helpers
`_register(slug)`, `_fundPool(slug, amount)`, `_provisionAgent(agent, mint, allowance)`,
`_makeEvent(seed, agent, slug, premium, refund, latencyMs, breach, feeCountHint, tsOffset)`.
Registry pause toggle: `reg.pauseProtocol(bool)` called via `vm.prank(authority)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing ProtocolPaused / resume / BatchTooLarge tests</name>
  <read_first>
    - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol (harness: setUp, _register, _fundPool, _provisionAgent, _makeEvent, settlerSigner, authority, reg)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs (:99-115 protocol-paused fast-revert; :132-135 BatchTooLarge)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("Test Port Plan" 05:529, 05:595; "BatchTooLarge: No Solana test → source-enforced invariant test")
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md (D-LOCK-PROTO-PAUSE, D-LOCK-BATCH)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md (P3 ruling — port authorized-settler+paused path)
  </read_first>
  <behavior>
    - test_ProtocolPaused_RejectsSettleBatch: paused → authorized settler call reverts ProtocolPaused.selector; agent + pool USDC balances unchanged.
    - test_ProtocolPaused_ResumesAfterUnpause: paused → revert; reg.pauseProtocol(false); fresh callId settles, expectEmit CallSettled(Settled).
    - test_BatchTooLarge_51EventsRevert: 51 events → revert BatchTooLarge.selector.
    - test_BatchTooLarge_50EventsAccepted: exactly 50 events → no revert.
  </behavior>
  <action>
    Append four test functions to `PactSettler.t.sol`, porting
    `settle_batch.rs:99-115` (protocol pause fast-revert, ported per 05:529/05:595
    in 05-RESEARCH.md "Test Port Plan") and `settle_batch.rs:132-135`
    (BatchTooLarge, source-enforced invariant per D-LOCK-5).

    Use the verbatim Foundry ports in 05-RESEARCH.md "Test Port Plan":
    - `test_ProtocolPaused_RejectsSettleBatch` (05-RESEARCH.md lines for 05:529):
      `_register(SLUG); _fundPool(SLUG, 5_000_000)`; provision agent;
      `vm.prank(authority); reg.pauseProtocol(true);` snapshot agent + pool USDC;
      build a 1-event batch via `_makeEvent(0x5A, agent, SLUG, 1_000, 0, 50, false, 1, 1)`;
      `vm.prank(settlerSigner); vm.expectRevert(ProtocolPaused.selector); settler.settleBatch(events);`
      then `assertEq` agent and `address(pool)` USDC balances == snapshots.
    - `test_ProtocolPaused_ResumesAfterUnpause` (05:595): pause → first attempt
      reverts ProtocolPaused; `vm.prank(authority); reg.pauseProtocol(false);`
      second attempt with a fresh callId (`_makeEvent(0x5C, ...)`) — `vm.expectEmit(true,true,true,true)` then
      `emit IPactSettler.CallSettled(ev2[0].callId, SLUG, agent, 1_000, 0, 0, IPactSettler.SettlementStatus.Settled, false, 50, ev2[0].timestamp);`
      `vm.prank(settlerSigner); settler.settleBatch(ev2);` (must NOT revert).
    - `test_BatchTooLarge_51EventsRevert`: 51-element events array filled by a
      loop of `_makeEvent(i, agent, SLUG, 1_000, 0, 50, false, 1, 1)`;
      `vm.prank(settlerSigner); vm.expectRevert(BatchTooLarge.selector); settler.settleBatch(events);`
    - `test_BatchTooLarge_50EventsAccepted`: 50-element array (fund pool
      10_000_000, provision agent 100_000_000/100_000_000); must NOT revert.

    Do NOT modify PactSettler.sol in this task. Run the four tests; confirm RED
    (test_ProtocolPaused_* and test_BatchTooLarge_51EventsRevert fail because
    the checks do not exist yet — the batch enters the loop; expected failure
    reason: no revert / wrong revert; test_BatchTooLarge_50EventsAccepted may
    pass trivially pre-impl and is the regression guard for the 50-OK boundary).
    Commit the test additions only (file-scoped, conventional commit, D-LOCK-6).
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 &amp;&amp; forge test --match-test "test_ProtocolPaused_RejectsSettleBatch|test_ProtocolPaused_ResumesAfterUnpause|test_BatchTooLarge_51EventsRevert" -vvv 2>&amp;1 | grep -E "FAIL|\\[FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - `forge test --match-test "test_ProtocolPaused_RejectsSettleBatch|test_ProtocolPaused_ResumesAfterUnpause|test_BatchTooLarge_51EventsRevert" -vvv` shows these three as FAIL (RED) — they do not pass before the impl exists.
    - The FAIL reason is "call did not revert as expected" / wrong selector (NOT a compile error) — the tests compile and execute.
    - `grep -c "function test_ProtocolPaused\|function test_BatchTooLarge" packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol` == 4.
    - PactSettler.sol is unchanged in this task (`git diff --name-only` lists only test/PactSettler.t.sol).
  </acceptance_criteria>
  <done>Four tests added and committed; ProtocolPaused + BatchTooLarge-51 tests confirmed RED for the expected reason (no pre-loop guard exists); 50-OK guard compiles and runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — insert ProtocolPaused + BatchTooLarge pre-loop guards at the settleBatch body head</name>
  <read_first>
    - packages/program-evm/protocol-evm-v1/src/PactSettler.sol (settleBatch body head :60-65 — the exact insertion point, before `for (uint256 i = 0; ...)`)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs (:99-115 protocol-paused fast-revert FIRST; :132-135 BatchTooLarge AFTER pause, BEFORE loop; note Solana order: pause check :105-115 THEN batch size :132-135)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-CONTEXT.md (D-LOCK-PROTO-PAUSE: first statement in body; D-LOCK-BATCH: strictly greater, 50 OK / 51 rejects, after pause gate, pre-loop)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-RESEARCH.md ("Seam Mechanics" Seam 3a + Seam 4; Pitfall 3 — ProtocolPaused MUST precede BatchTooLarge)
    - .planning/phases/05-wp-evm-05-pactsettler-hardening/05-GATE-A-DECISIONS.md (P3 ruling)
  </read_first>
  <action>
    In `packages/program-evm/protocol-evm-v1/src/PactSettler.sol`, in
    `settleBatch`, insert two statements as the FIRST two statements of the
    function body, BEFORE `for (uint256 i = 0; i < events.length; i++)` (current
    PactSettler.sol:65), in this exact order (ports settle_batch.rs:105-115
    then :132-135 — Solana order is pause-then-batch-size, D-LOCK-PROTO-PAUSE +
    Pitfall 3):

    ```solidity
    // settle_batch.rs:99-115 — protocol-paused fast-revert (SET-11).
    // PRE-loop, FIRST body statement (D-LOCK-PROTO-PAUSE). For the
    // operationally-real authorized settler this is bit-identical to Solana
    // ProtocolPaused; the unauthorized+paused corner is the P3
    // OPTIMIZED-DIVERGENCE (05-GATE-A-DECISIONS.md P3).
    if (registry.protocolPaused()) revert ProtocolPaused();
    // settle_batch.rs:132-135 — BatchTooLarge edge (SET-12). Strictly
    // greater: 50 OK, 51 rejects. AFTER the pause gate, BEFORE the loop
    // (D-LOCK-BATCH).
    if (events.length > ArcConfig.MAX_BATCH_SIZE) revert BatchTooLarge();
    ```

    `registry` is the `IPactRegistry public immutable registry` already declared
    (PactSettler.sol:33); `protocolPaused()` is the confirmed getter.
    `ArcConfig.MAX_BATCH_SIZE` (uint16=50) is promoted to uint256 for the
    `events.length` comparison — standard, correct. `ProtocolPaused()` and
    `BatchTooLarge()` are confirmed in PactErrors.sol (already `import "./errors/PactErrors.sol";`).
    Do NOT touch the loop body, the per-event guards, `_settleSuccess`, or any
    WP-04 line. This is pure prepend at the body head — no WP-04 reorder.
    Run the full suite; confirm GREEN + 90/90 regression. Commit src change
    file-scoped (conventional commit, D-LOCK-6).
  </action>
  <verify>
    <automated>cd /Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1 &amp;&amp; forge build &amp;&amp; forge test 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Working dir: `/Users/q3labsadmin/Q3/Solder/pact-network/packages/program-evm/protocol-evm-v1`.
    - `grep -n "if (registry.protocolPaused()) revert ProtocolPaused();" src/PactSettler.sol` returns exactly one line, located BEFORE the `for (uint256 i = 0; i < events.length; i++)` line in settleBatch.
    - `grep -n "if (events.length > ArcConfig.MAX_BATCH_SIZE) revert BatchTooLarge();" src/PactSettler.sol` returns exactly one line, AFTER the protocolPaused line and BEFORE the `for` loop (verify line numbers: protocolPaused line < BatchTooLarge line < `for` loop line).
    - `forge build` succeeds (no compile error).
    - `forge test --match-test "test_ProtocolPaused_RejectsSettleBatch|test_ProtocolPaused_ResumesAfterUnpause|test_BatchTooLarge_51EventsRevert|test_BatchTooLarge_50EventsAccepted"` — all 4 PASS (GREEN).
    - `forge test` full suite: 0 failed; total ≥ 94 (90 WP-04 + 4 new); the prior 90 still pass (regression preserved).
    - `git diff --stat` for src shows only `src/PactSettler.sol` with additions only inside settleBatch head (no deletions in the WP-04 loop / _settleSuccess — `git diff src/PactSettler.sol | grep '^-' | grep -v '^---'` returns nothing).
  </acceptance_criteria>
  <done>ProtocolPaused fast-revert is the first settleBatch body statement; BatchTooLarge follows before the loop. All 4 new tests GREEN; full forge suite green with ≥90 prior tests still passing. No WP-04 line deleted or reordered.</done>
</task>

</tasks>

<verification>
- ProtocolPaused check is the first statement in settleBatch; BatchTooLarge is second; loop third (grep line-order check).
- Solana parity: settle_batch.rs:99-115 (pause) ported before :132-135 (batch) — matches Solana order.
- 4 ported tests GREEN; full `forge test` green; ≥90 prior WP-04 tests still pass (no regression).
- No WP-04 line deleted/reordered (additive-only diff).
</verification>

<success_criteria>
SET-11 (protocol-paused fast-revert) and SET-12 (BatchTooLarge) are ported
bit-identically to settle_batch.rs at the settleBatch body head, as a pure
additive prepend. The parity-relevant P3 path (authorized settler + paused →
ProtocolPaused; resume after unpause) is tested green. WP-04 90/90 preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-wp-evm-05-pactsettler-hardening/05-02-SUMMARY.md`
</output>
