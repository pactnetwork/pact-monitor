# WP-EVM-06 — GATE A Report (plan + method recommendation — captain ruling needed)

**Date:** 2026-05-19
**Crew:** WP-EVM-06 (fresh resume; continues `feat/arc-protocol-v1` @ `fd18f4d`)
**Status:** GATE A — plan authored. AWAITING captain verdict on (1) plan
method (recommended: authored-at-turn, NOT GSD), (2) four scoping decisions
D-A..D-D (recommended answers given), then plan approval. NO client/suite
code written. NO push. NO PR #204 comment. Working tree clean (only
`?? .claude/pr-reviews/`; `CLAUDE.md` unmodified; no `.claude/skills/pact/` —
no contamination).

This is the FINAL parity WP. WP-07 (deploy/verify) is a separate cycle,
explicitly out of scope.

## STEP 0 — settled law absorbed (NOT reopened)

Read end to end: the 588-line handoff (`2026-05-18-arc-evm-port-handoff.md`),
design spec `2026-05-15-arc-parity-port-design.md` §3/§4/§5/§6/§7/§8/§9,
impl-plan WP-06 section, `05-NA-MATRIX.md` (verbatim), the
`protocol-v1-client` module map (`index.ts`, `constants.ts`, `errors.ts`,
`pda.ts`, package/tsconfig/vitest toolchain), and the LOCKED EVM public
surface (`IPactRegistry`/`IPactPool`/`IPactSettler`, `PactErrors.sol` 30
variants, `PactEvents.sol`, `ArcConfig.sol`).

In force and NOT reopened: §(b) rulings 1-7 + ruling #8 (D1-scope
refinement); §(c) methodology (Solana source authority, STOP-AND-ASK on
parity ambiguity, strict TDD RED-before-GREEN, file-scoped conventional
commits); §(e) conventions (no emojis, pnpm/forge never npm, no push/PR
until captain GATE B, contamination guardrail, GitNexus deferral); WP-04
OUTCOMES E1-E4 / guard-precedence / seams; WP-05 OUTCOMES P1/P3 final forms,
the LOCKED-ACCEPTED `2157b75` alias inline (NOT reverted), the 4 filled
seams. WP-06 produces NO contract behavior change — the contracts are LOCKED
through WP-05.

## Baseline (asserted, re-verified as T0 before any change)

Handoff states `forge test` 102/102 green at `b601982`; HEAD `fd18f4d` adds
only docs commits. Test inventory confirmed by source count: ArcConstants 1
+ Deployment 4 + FeeValidation 15 + PactPool 19 + PactSettler 32 +
PactRegistry 31 = 102. First execution step (T0) re-runs `forge build &&
forge test` to pin the GREEN baseline before WP-06 adds anything; the 102
must remain green at GATE B (regression-preserved).

## DECISION 0 — plan method (RECOMMENDED, captain ratifies)

**Recommendation: authored-at-turn, sequential in-session, strict TDD per
module. NOT GSD.**

Rationale:

- Handoff §(f) explicitly: "WP-06 is authored-at-turn (lighter than the
  settler WPs — NOT necessarily GSD; the captain decides the plan method at
  WP-06 GATE A)."
- No bit-identical economic logic to port. WP-04/05 used GSD because
  per-event settlement parity is combinatorial and benefits from fresh
  200K context per atomic task + deviation protocol. WP-06 adds ZERO
  contract behavior: the client mechanically mirrors an EXISTING, stable
  module map against a LOCKED ABI; the parity matrix SYNTHESIZES the
  already-CLOSED §(d) items 1-8 + `05-NA-MATRIX.md` read verbatim (handoff:
  "do not re-derive"); the spec corrections are 8 enumerated CLOSED edits.
- The work is inherently sequential and small-surface (one new workspace
  package mirroring 7 modules + additive forge tests + 2 docs). GSD's
  parallel-wave / fresh-context machinery is overhead here and risks
  `.planning/` scaffold churn for a mirror+docs+additive-test WP.
- Strict TDD (§c) is still enforced per module (RED → confirm → GREEN).

**GSD alternative (captain may override):** if chosen, `.planning/config.json`
is already set (research/plan_check/verifier on; use_worktrees/code_review/
nyquist/security off; commit_docs:false). The T1..T11 breakdown below maps
1:1 to GSD plans 06-01..06-NN if the captain prefers the orchestrated path.

## The WP-06 plan (T1..T11)

Sequencing: T0 baseline → T1 scaffold → T2..T6 client (sequential; T2/T3
before T4/T5; T6 closes index+integration) → T7/T8 forge (independent of
client, any time after T0) → T9 matrix (synthesis) → T10 spec corrections
(after T9, references it) → T11 GATE B. One file-scoped conventional commit
per logical change.

### T0 — Pin GREEN baseline (verify-only, no change)
`cd packages/program-evm/protocol-evm-v1 && forge build && forge test`.
**Verify:** 102/102 green, build clean. Records the regression bar.

### T1 — Scaffold `packages/protocol-evm-v1-client/`
NEW pnpm workspace member `@pact-network/protocol-evm-v1-client` (under the
`packages/*` glob — confirmed in `pnpm-workspace.yaml`). Mirror the
`protocol-v1-client` toolchain EXACTLY: `package.json` (scripts
build/typecheck/test/coverage; dep `viem`; devDeps typescript/vitest/
@vitest/coverage-v8/@types/node), `tsconfig.json` + `tsconfig.build.json`
(CJS → `dist/`) + `tsconfig.cjs.json`, `vitest.config.ts` (include
`__tests__/**/*.test.ts`, coverage `src/**`), `.gitignore`. Per **D-A**:
committed curated ABI under `src/abi/` generated by a repo script from the
forge build (forge `out/` is gitignored — the workspace member must build
without the forge toolchain present).
**Verify:** `pnpm install` recognizes the package; `pnpm --filter
@pact-network/protocol-evm-v1-client build` green (empty `index.ts`); ABI
JSON files present + valid; `pnpm -r build` still green. No behavior → no
TDD test yet.

### T2 — `constants.ts` + `addresses.ts`
`pda.ts → addresses.ts`, `constants.ts → constants.ts` (spec §5). constants:
ARC_TESTNET_CHAIN_ID 5042002, ARC_TESTNET_USDC, EXPECTED_USDC_DECIMALS 6,
MAX_BATCH_SIZE 50, MIN_PREMIUM 100n, MAX_FEE_RECIPIENTS 8,
ABSOLUTE_FEE_BPS_CAP 10000, DEFAULT_MAX_TOTAL_FEE_BPS 3000, + ABI re-export.
addresses.ts per **D-B**: typed per-chain registry; Arc Testnet chain id +
USDC from ArcConfig; protocol contract addresses null/env-overridable
placeholders ("filled at WP-07"); NO PDA derivation (EVM has none, §4 #2).
**TDD:** RED test asserts every constant == the `ArcConfig.sol` /
`constants.rs` value (parity) and addresses registry shape → confirm RED →
GREEN.

### T3 — `errors.ts`
`errors.ts → errors.ts` (custom-error selector → name). All 30 PactErrors →
`keccak256("Name()")[:4]` selector map; `decodePactError(data)`,
`formatPactError`, `tryExtractPactError` (viem `BaseError` /
`ContractFunctionRevertedError` walk; mirrors `tryExtractProtocolError`).
**TDD:** RED test asserts the 30 selectors derived from the committed ABI ==
`keccak4(signature)`; decode round-trips; unknown selector → undefined →
confirm RED → GREEN.

### T4 — `encode.ts`
`instructions.ts → encode.ts` (viem `encodeFunctionData` builders). Mutating
surface: registerEndpoint, updateEndpointConfig, updateFeeRecipients,
pauseEndpoint, pauseProtocol, topUp, settleBatch, plus the SETTLER hooks
recordCallAndCapAccrual / recordRefundPaid (consumed by settler/indexer).
`slugToBytes16` helper (16-byte right-padded, bytes16). **TDD:** RED test
round-trips each builder via viem `decodeFunctionData` against the committed
ABI (selector + arg parity) → confirm RED → GREEN.

### T5 — `state.ts`
`state.ts → state.ts` (view-call + event decoders). View decoders:
getEndpoint→EndpointConfig, balanceOf→PoolState, isRegistered /
protocolPaused / authority / treasuryVault / maxTotalFeeBps. Event decoders:
CallSettled, EndpointRegistered, EndpointConfigUpdated, EndpointPaused,
ProtocolPaused, FeeRecipientsUpdated, PoolToppedUp. Mirror SettlementStatus
+ FeeRecipientKind enums. **TDD:** RED test decodes ABI-encoded view returns
+ log fixtures; asserts struct-field parity vs `IPactRegistry`/`IPactPool`/
`IPactSettler` → confirm RED → GREEN.

### T6 — `helpers.ts` + `index.ts`
Per **D-D**: implement the parity-relevant subset `protocol-v1-client`
exposes that EVM consumers need — defaultFeeRecipients, validateFeeRecipients
(`fee.rs` parity). SPL-custody helpers (Approve/Revoke, account-based
getAgentInsurableState) are §4-divergent → documented N-A-on-EVM in the
matrix (ERC-20 `approve` is a one-liner; no PDA helper). `index.ts`
re-export surface paralleling `protocol-v1-client/src/index.ts`. **TDD:**
RED test asserts helper outputs match documented fee-recipient rules; index
exports the full surface → confirm RED → GREEN. **Verify:** `pnpm --filter
@pact-network/protocol-evm-v1-client build && test` green; `pnpm -r build`
green (workspace integration intact).

### T7 — Consolidated forge fuzz + gas-snapshot suite (additive; NO contract change)
`test/Fuzz.t.sol`: property tests — fee-split integer rounding
(`premium*bps/10_000` u64 floor-div: pool residual == premium − Σsplits;
Σsplits ≤ premium; no overflow) and batch handling (size 0..50 OK; 51 reverts
BatchTooLarge; dedup invariant under event reordering). Oracle = the spec §3
documented formula (the proven WP-02/04 parity oracle; no live Solana
cross-check is possible in Foundry — the formula IS the oracle). Bounded
runs. `forge snapshot` → committed `.gas-snapshot` baseline. **TDD:** RED per
fuzz invariant → confirm RED → GREEN. **Verify:** 102 regression PRESERVED +
fuzz green; snapshot stable.

### T8 — Live `IERC20(USDC).decimals() == 6` assertion
`test/UsdcDecimals.t.sol`: assert
`IERC20Metadata(usdc).decimals() == ArcConfig.EXPECTED_USDC_DECIMALS` vs
MockUSDC (deferred from WP-EVM-01; resolves the `TODO(WP-EVM-06)` marker in
`ArcConfig.sol` — surgical: only that comment line). **TDD:** RED (decimals
≠ 6 fails loudly) → confirm RED → GREEN.

### T9 — Parity-matrix doc (synthesis — NO re-derivation)
Per **D-C**, new doc `docs/superpowers/specs/2026-05-18-arc-parity-matrix.md`.
Per-variant table: ALL 30 PactErrors + each spec §3 behavior tagged
IDENTICAL / OPTIMIZED-DIVERGENCE (§4 ref) / N-A-ON-EVM (rationale), each row
citing file:line authority. Synthesized strictly from: handoff §(b) rulings
1-8, §(d) consolidated items 1-8, `05-NA-MATRIX.md` (verbatim), WP-04
OUTCOMES E1-E4, `PactErrors.sol`. **Verify (checklist):** every 1 of 30
PactErrors has a row; every §3 behavior has a row; every §(d) item 1-8
represented; the `05-NA-MATRIX` rows (09:47/78, 09:144, 09:172, 10:94/128,
D-LOCK-EXPCAP-NA) + the P3 corner all present.

### T10 — Formal design-spec corrections (§(d) 1-8, CLOSED list)
Edit `docs/superpowers/specs/2026-05-15-arc-parity-port-design.md` in place:
(1) add `FeeBpsSumOver10k` to the §3 enumerated list (→ 30 variants); (2)
fix §4 #7 wording (InvalidAffiliateAta = OPTIMIZED-DIVERGENCE; only
FeeRecipientInvalidUsdcMint true N-A); (3) updateEndpointConfig
per-field-flags → full-typed-set note; (4) D2 created_at lazy-at-topUp note;
(5) append ruling #8 D1-scope refinement; (6) P3 OPTIMIZED-DIVERGENCE corner
note; (7) N-A-on-EVM rows reference; (8) WP-02/03/04 N-A items folded — all
cross-linking the T9 matrix. Add a "WP-EVM-06 Corrections" appendix.
**Verify:** each §(d) 1-8 has a corresponding edit; §3 lists 30; §4 #7
corrected.

### T11 — GATE B
`forge build` clean; `forge test` = 102 preserved + fuzz/gas/decimals green;
`pnpm --filter @pact-network/protocol-evm-v1-client build && test` green;
`pnpm -r build` green; matrix complete; spec corrected. Write
`06-REPORT-gateB.md` + short cockpit notice. WAIT. NO push / NO PR #204
comment until captain approval.

## Decisions for captain ratification (recommended answers; not blocking)

- **D-A — ABI sourcing.** forge `out/` is gitignored. RECOMMEND: commit a
  curated ABI JSON set under `protocol-evm-v1-client/src/abi/` generated by a
  repo script from the forge build, so the workspace member is self-contained
  (mirrors how `protocol-v1-client` hand-writes its decoders rather than
  depending on a build artifact). Reject: depending on forge `out/` at
  install (workspace must build without the forge toolchain).
- **D-B — addresses.ts contents.** WP-07 deploy deferred → no deployed
  protocol addresses exist. RECOMMEND: typed per-chain registry with Arc
  Testnet chain id + USDC from `ArcConfig`; protocol contract addresses
  `null`/env-overridable placeholders documented "filled at WP-07".
  Parity-neutral (no PDAs on EVM, §4 #2).
- **D-C — matrix location + spec-correction mechanism.** RECOMMEND matrix at
  `docs/superpowers/specs/2026-05-18-arc-parity-matrix.md`; design spec
  corrected in place + a "WP-EVM-06 Corrections" appendix cross-linking the
  matrix. Both are repo docs (committed per conventions; `commit_docs:false`
  applies only to `.planning/` GSD docs).
- **D-D — helpers.ts scope.** RECOMMEND mirror only the parity-relevant
  subset EVM consumers (settler ChainAdapter, indexer per-chain poller) need:
  defaultFeeRecipients + validateFeeRecipients (`fee.rs` parity).
  SPL-custody helpers are §4-divergent → recorded N-A-on-EVM in the matrix
  (ERC-20 `approve` needs no PDA helper).

## STOP-AND-ASK triggers for execution (per §c — halt + report, do NOT guess)

1. Any fuzz-discovered fee-split rounding divergence from the spec §3
   `premium*bps/10_000` floor-div oracle — that is a parity bug; halt and
   report; do NOT silently "fix" (a contract change reopens LOCKED WP-05,
   forbidden).
2. Any parity-matrix row where `PactErrors.sol` / contract code disagrees
   with the §(d) CLOSED list or `05-NA-MATRIX.md` — halt and report; do NOT
   re-derive or reopen.
3. Any client encode/decode mismatch vs the LOCKED ABI implying a contract
   change — halt (WP-02..05 reopen forbidden).

## Success criteria (spec §9, scoped to WP-06)

1. `@pact-network/protocol-evm-v1-client` builds + tests green; mirrors the
   `protocol-v1-client` module map (addresses/encode/state/errors/constants/
   helpers/index).
2. `forge build` green; `forge test` = 102 preserved + new fuzz/gas/decimals
   green; `.gas-snapshot` committed.
3. Live `IERC20(USDC).decimals()==6` assertion present and passing.
4. Parity-matrix doc complete: every PactError variant + every §3 behavior
   tagged IDENTICAL / OPTIMIZED-DIVERGENCE / N-A-on-EVM with rationale.
5. Design spec formally corrected for ALL §(d) items 1-8; corrects PR #201
   §7.1 (client IS required).
6. No mainnet/testnet deploy (WP-07, out of scope). No WP-02/03/04/05
   reopen.

## Status

GATE A request. NO client/suite code written. NO push. NO PR #204 comment.
Working tree clean (only `?? .claude/pr-reviews/`). AWAITING captain verdict
(delivered as a file) on DECISION 0 (method) + D-A..D-D, then plan approval,
BEFORE any client/suite code.
