# WP-EVM-06 — Captain GATE A Verdict: APPROVED

Plan + method + 4 scoping decisions reviewed. This is an exemplary Gate A (correct STEP-0 absorption, sound method rationale, clean task sequencing, the right STOP-AND-ASK triggers). Record this verdict verbatim; proceed per it.

## DECISION 0 (plan method): APPROVED — authored-at-turn, NOT GSD

Rationale is correct: WP-06 adds ZERO contract behavior (mirror + docs + additive tests against a LOCKED ABI; the parity matrix SYNTHESIZES already-closed items with no re-derivation; spec corrections are 8 enumerated closed edits). GSD existed for the combinatorial per-event settlement parity of WP-04/05 — none of that here. Authored-at-turn, sequential in-session, strict TDD RED-before-GREEN per module. Handoff §(f) explicitly left this to the captain; ratified.

## Scoping decisions — all APPROVED as recommended

- D-A (ABI sourcing): APPROVED — committed curated ABI under protocol-evm-v1-client/src/abi/ generated by a repo script from the forge build; the workspace member must build without the forge toolchain (forge out/ is gitignored). CONDITION (ABI drift guard): the committed ABI must be verified IN SYNC with the actual forge-compiled contracts at the locked HEAD — not merely self-consistent. The T3/T4/T5 TDD tests check the ABI against itself; ADD a drift check (a script/test, run at T1 and re-run at T7/T11) that regenerates the ABI from a fresh forge build and diffs it against the committed src/abi/ — fail loudly on any difference. A stale committed ABI silently breaking client parity vs the locked contracts is the one real parity risk of the curated-ABI approach; this guard closes it.
- D-B (addresses.ts): APPROVED — typed per-chain registry; Arc Testnet chain id + USDC from ArcConfig; protocol contract addresses null/env-overridable placeholders documented "filled at WP-07". Parity-neutral (no PDAs, §4 #2).
- D-C (matrix location + spec-correction mechanism): APPROVED — matrix at docs/superpowers/specs/2026-05-18-arc-parity-matrix.md; design spec corrected in place + a "WP-EVM-06 Corrections" appendix cross-linking the matrix. CONDITION: the appendix must enumerate every §(d) 1-8 edit explicitly (what changed + why) so a reviewer can verify the spec now matches implemented reality; git history preserves the before-state. Both docs committed per conventions (commit_docs:false applies only to .planning/ GSD docs).
- D-D (helpers.ts scope): APPROVED — mirror only the parity-relevant subset (defaultFeeRecipients + validateFeeRecipients, fee.rs parity); SPL-custody/PDA helpers are §4-divergent -> recorded N-A-on-EVM in the matrix (ERC-20 approve needs no PDA helper).

## 11-task plan (T0..T11): APPROVED

Sequencing is correct (T0 baseline-pin 102 green -> T1 scaffold -> T2..T6 client -> T7/T8 forge -> T9 matrix synthesis -> T10 spec corrections -> T11 GATE B), one file-scoped conventional commit per logical change, strict TDD per module, additive-only, no WP-02/03/04/05 reopen. CONDITION on T9 (parity matrix) — Gate B acceptance gate: the matrix completeness checklist is MANDATORY and I will verify it at Gate B — every 1 of 30 PactError variants has a row; every spec §3 behavior has a row; every §(d) item 1-8 represented; every 05-NA-MATRIX row (09:47/78, 09:144, 09:172, 10:94/128, D-LOCK-EXPCAP-NA) present; the P3 unauthorized+paused corner present; each row tagged IDENTICAL / OPTIMIZED-DIVERGENCE (with §4 ref) / N-A-on-EVM (with rationale) and citing file:line authority. T10: each §(d) 1-8 must have a corresponding traceable spec edit; §3 must list 30 variants; §4 #7 corrected; PR #201 §7.1 corrected (client IS required).

## STOP-AND-ASK triggers: ENDORSED (all 3 correct)

All three are exactly right. Emphasis on TRIGGER 1 (highest stakes): if the fuzz suite finds ANY fee-split rounding divergence from the spec §3 premium*bps/10_000 floor-div oracle, that is a genuine parity defect in shipped LOCKED contract code — HALT immediately, do NOT touch contract code, escalate to the captain via the report file + a short cockpit notice with the exact failing input + the oracle-expected vs actual values. Only the captain authorizes any contract-reopening fix (it would be a new captain-gated WP-05 reopen). Same posture for triggers 2 and 3 — halt, report, do not re-derive/reopen/guess.

## Proceed

Execute T0 -> T11 authored-at-turn, strict TDD per module, file-scoped conventional commits, Solana source authority, the 3 STOP-AND-ASK triggers in force, no WP-02/03/04/05 reopen, contamination guardrail + GitNexus deferral per handoff §(e). GATE B: write 06-REPORT-gateB.md + a short send-safe cockpit notice, WAIT, NO push / NO PR #204 comment until the captain approves (verdict delivered as a file). At Gate B I verify: 102 regression preserved + fuzz/gas/decimals green; client builds + tests green + pnpm -r build green; the ABI drift guard; the mandatory parity-matrix completeness checklist; and all 8 spec corrections traceable.
