---
phase: 04-pactsettler-happy-path
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
  - packages/program-evm/protocol-evm-v1/src/PactRegistry.sol
  - packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
  - packages/program-evm/protocol-evm-v1/test/Deployment.t.sol
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
autonomous: true
requirements: [SET-01, SET-07]
must_haves:
  truths:
    - "PactSettler inherits OZ AccessControl with SETTLER_ROLE; constructor matches the GATE-A E2 ruling (3-arg, DEFAULT_ADMIN_ROLE -> registry.authority()); Deployment.t.sol compiles and passes against the new ctor"
    - "Per the GATE-A E1 ruling (OPTION (a)), PactRegistry gains TWO SETTLER_ROLE-gated hooks mirroring settle_batch.rs's TWO distinct ep.* mutation points in EXACT source order: (1) recordCallAndCapAccrual (settle_batch.rs:385-414, BEFORE fee fan-out) does totalCalls/totalPremiums/totalBreaches + the WP-04 period reset + currentPeriodRefunds+=payableRefund and RETURNS payableRefund (== intendedRefund in WP-04; WP-05 makes it the clamped amount), with a clean WP-05 cap-clamp seam between the reset and the accrual; (2) recordRefundPaid (settle_batch.rs:493-499, AFTER the refund transfer) does totalRefunds+=actualRefund. All checked -> ArithmeticOverflow"
    - "Pure-addition regression gate: the full pre-existing WP-02 PactRegistry.t.sol + all 70 pre-existing forge tests stay green; no existing signature/behavior altered"
    - "test/PactSettler.t.sol exists with the shared setUp + helper scaffold mirroring helpers.ts; the deployed PactSettler holds SETTLER_ROLE on BOTH PactPool AND PactRegistry (E1xE2 two-layer grant)"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "AccessControl + SETTLER_ROLE; 3-arg ctor per E2; settleBatch still reverts pending plans 03/04"
      contains: "SETTLER_ROLE"
    - path: "packages/program-evm/protocol-evm-v1/src/PactRegistry.sol"
      provides: "OZ AccessControl + the TWO SETTLER_ROLE-gated stat hooks (recordCallAndCapAccrual + recordRefundPaid) per GATE-A E1, additive over committed WP-02"
      contains: "function recordCallAndCapAccrual"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "Shared setUp + _register/_fundPool/_provisionAgent helpers + two-layer role wiring (pool AND registry)"
      contains: "function setUp"
  key_links:
    - from: "test/PactSettler.t.sol setUp"
      to: "pool.grantRole(SETTLER_ROLE, settler) AND reg.grantRole(SETTLER_ROLE, settler)"
      via: "E1xE2 two-layer grant — deployed PactSettler granted SETTLER_ROLE on BOTH pool and registry"
      pattern: "grantRole"
    - from: "PactSettler / 04-04 settleBatch"
      to: "PactRegistry.recordCallAndCapAccrual (ep point 1, pre-fan-out) + PactRegistry.recordRefundPaid (ep point 2, post-transfer) (per GATE-A ruling on E1)"
      via: "TWO SETTLER_ROLE-gated stat hooks mirroring settle_batch.rs's two ep.* mutation points; recordCallAndCapAccrual returns payableRefund (the WP-05 clamp seam)"
      pattern: "recordCallAndCapAccrual|recordRefundPaid"
---

<objective>
Land the WP-04 foundation: refactor PactSettler to OZ AccessControl
(SETTLER_ROLE) per the GATE-A ruling on E2, implement the TWO SETTLER_ROLE-gated
endpoint-stats hooks on PactRegistry per the GATE-A ruling on E1 (OPTION (a),
INCLUDING the WP-04 period reset per the D-SPLIT refinement) — split to mirror
settle_batch.rs's TWO distinct `ep.*` mutation points in EXACT source order
(`recordCallAndCapAccrual` for :385-414 BEFORE fee fan-out;
`recordRefundPaid` for :493-499 AFTER the refund transfer) — fix
Deployment.t.sol for the new constructor, and stand up the
test/PactSettler.t.sol harness (setUp + helpers mirroring helpers.ts, with the
E1xE2 two-layer role grant). settleBatch logic is NOT implemented here
(plans 03/04).

Purpose: SET-01 (settler-role gate) and SET-07 (stats write path) need the
AccessControl + stats infrastructure in place before the per-event loop is
ported. The hook is SPLIT into two functions so 04-04's settleBatch composition
can place each at the EXACT settle_batch.rs mutation position
(`recordCallAndCapAccrual` BEFORE the fee fan-out, `recordRefundPaid` AFTER the
refund transfer), preserving source mutation ORDER and leaving the WP-05
cap-clamp as a return-value seam inside `recordCallAndCapAccrual` (zero WP-04
rewrite). Every code change is strict TDD: RED (failing forge test, confirm the
exact failure), then GREEN (implement, confirm pass).

Output: AccessControl-enabled PactSettler, the TWO E1 stat hooks on
PactRegistry (additive over committed WP-02), green Deployment.t.sol, and the
PactSettler.t.sol harness — with the full WP-02 PactRegistry.t.sol and all 70
pre-existing forge tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pactsettler-happy-path/04-CONTEXT.md
@.planning/phases/04-pactsettler-happy-path/04-RESEARCH.md
@.planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
@.planning/phases/04-pactsettler-happy-path/04-01-SUMMARY.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program-evm/protocol-evm-v1/src/PactPool.sol
@packages/program-evm/protocol-evm-v1/src/PactRegistry.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
@packages/program-evm/protocol-evm-v1/test/PactPool.t.sol
@packages/program-evm/protocol-evm-v1/test/Deployment.t.sol

<interfaces>
PactPool ctor + role pattern PactSettler AND the E1 PactRegistry hooks must
mirror (PactPool.sol:19, 22, 29-36, 79-84):
```solidity
contract PactPool is IPactPool, AccessControl { ... }
bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE");
constructor(address usdc_, address registry_) {
    _usdc = IERC20(usdc_); registry = IPactRegistry(registry_);
    _grantRole(DEFAULT_ADMIN_ROLE, registry.authority());
}
function _ckAdd(uint64 a, uint64 b) private pure returns (uint64 c) {
    unchecked { c = a + b; } if (c < a) revert ArithmeticOverflow();
}
```

Role-grant + vm.prank discipline (PactPool.t.sol:32-43, 169-172):
```solidity
function setUp() { ...; settlerRole = pool.SETTLER_ROLE(); } // cache BEFORE prank
function _grantSettler() { vm.prank(authority); pool.grantRole(settlerRole, settler); }
```

PactRegistry today (committed WP-02) — `is IPactRegistry, PactEvents`, has
`address public override authority` set once in the ctor + an `onlyAuthority`
modifier; `EndpointConfig` already carries `currentPeriodStart` (seeded to
`block.timestamp` at registerEndpoint, PactRegistry.sol:134) and
`currentPeriodRefunds`. The E1 hooks ADDITIVELY add OZ AccessControl as a NEW
base — they alter no existing function.

PactRegistry constructor signature (from Deployment.t.sol / PactPool.t.sol):
new PactRegistry(authority, address(usdc), treasuryVault, maxTotalFeeBps,
FeeRecipient[8] defaults, uint8 defaultCount)

Helpers.ts defaults to mirror (confirmed: PactPool.t.sol _register uses the
same numbers):
reg.registerEndpoint(slug, 500 /*flatPremium*/, 0 /*percentBps*/,
  5000 /*slaMs*/, 1000 /*imputedCost*/, 5_000_000 /*exposureCap*/,
  false /*recipientsPresent*/, 0 /*count*/, none)

GATE-A E1 stat hooks (the SPLIT recorded for 04-04 to compose at the exact
settle_batch.rs positions — TWO functions, NOT one):
```solidity
// PactRegistry, both onlyRole(SETTLER_ROLE):
// ep mutation point 1 — settle_batch.rs:385-414, called BEFORE fee fan-out:
function recordCallAndCapAccrual(
    bytes16 slug, uint64 premium, bool breach, uint64 intendedRefund
) external returns (uint64 payableRefund);
// ep mutation point 2 — settle_batch.rs:493-499, called AFTER the refund transfer:
function recordRefundPaid(bytes16 slug, uint64 actualRefund) external;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — PactSettler AccessControl + SETTLER_ROLE; fix Deployment.t.sol (per GATE-A ruling on E2)</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      §E2 (RULED: CONFIRM lead — OZ AccessControl SETTLER_ROLE on PactSettler,
      ctor 4-arg -> 3-arg dropping `address settler_`, DEFAULT_ADMIN_ROLE ->
      registry.authority(), Deployment.t.sol -> 3-arg. INTERPLAY: with E1(a)
      the deployed PactSettler must hold SETTLER_ROLE on BOTH PactPool AND
      PactRegistry). This is settled law — implement EXACTLY as ruled; the
      scaffold TODO(WP-EVM-05) is a superseded WP-01 sketch (D-LOCK-1).
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q7 (E2
      required changes 1-6) and §"Architecture Patterns" (post-WP-04 PactSettler
      structure block, lines 854-884)
    - packages/program-evm/protocol-evm-v1/src/PactSettler.sol (current
      4-arg ctor + plain `address settler` to replace)
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 19-36
      (the AccessControl ctor pattern to mirror exactly)
    - packages/program-evm/protocol-evm-v1/test/Deployment.t.sol lines 42-51
      (test_DeployPactSettler — currently passes 4 args incl. `address(this)`
      settler placeholder; must update to the E2 3-arg ctor)
  </read_first>
  <action>
    Implement EXACTLY per the GATE-A ruling on E2 in 04-GATE-A-DECISIONS.md §E2
    (RULED: CONFIRM lead). This is settled law — there is no "if the captain
    overrode it" branch.

    RED step (do FIRST, run, confirm the exact failure before any src change):
    Add to a new `packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol`
    a forge-std test contract `PactSettlerTest is Test` with a minimal
    `test_Deploy_WiresSettlerRoleAndAdmin()` asserting:
      - `settler.hasRole(0x00, authority) == true` (DEFAULT_ADMIN_ROLE to
        registry.authority())
      - `settler.SETTLER_ROLE() == keccak256("SETTLER_ROLE")`
      - `settler.hasRole(settler.SETTLER_ROLE(), settlerSigner) == false`
        before any grant
    Run `forge test --match-contract PactSettlerTest` from
    `packages/program-evm/protocol-evm-v1/`. Confirm RED: it MUST fail to
    COMPILE because PactSettler has no `SETTLER_ROLE`/`hasRole` and the 3-arg
    ctor does not yet exist. Record the exact compiler error in the SUMMARY.

    GREEN step:
    1. PactSettler.sol: `import {AccessControl} from
       "@openzeppelin/contracts/access/AccessControl.sol";` and
       `import {IPactRegistry} from "./interfaces/IPactRegistry.sol";`
    2. `contract PactSettler is IPactSettler, AccessControl`
    3. `bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE");`
    4. Replace the 4-arg ctor with 3-arg
       `constructor(address usdc_, address registry_, address pool_)`:
       set immutables `usdc`, `registry`, `pool` (keep `pool` typed as
       `IPactPool` per the research structure block); remove the plain
       `address public settler` storage var and the TODO comment;
       `_grantRole(DEFAULT_ADMIN_ROLE, IPactRegistry(registry_).authority());`
       (mirrors PactPool.sol:35).
    5. Add `onlyRole(SETTLER_ROLE)` to `settleBatch` (keep the body reverting
       — leave a single explicit `revert("NOT_IMPLEMENTED");` so plans 03/04
       fill it; the role gate is now active for SET-01).
    6. Deployment.t.sol test_DeployPactSettler: change to the 3-arg ctor
       `new PactSettler(ArcConfig.ARC_TESTNET_USDC, address(reg), address(pool))`
       — it now needs a real registry so `registry.authority()` is callable
       in the ctor; construct a PactRegistry + PactPool the same way
       test_DeployPactPool does (lines 33-37) and pass them. Keep the
       bytecode + `s.usdc()` asserts.
    Run `forge test --match-contract PactSettlerTest` — confirm GREEN. Then
    run the FULL suite `forge test` — confirm all pre-existing tests
    (Deployment + the 70 from WP-01..03) stay green.

    No emojis. Conventional commits, file-scoped (NO `git add -A`): commit the
    src change and the Deployment.t.sol fix together as one logical change
    `refactor(protocol-evm-v1): PactSettler AccessControl SETTLER_ROLE per
    GATE-A E2 (WP-EVM-04)`; commit the new test file separately
    `test(protocol-evm-v1): PactSettler.t.sol deploy/role harness (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-contract PactSettlerTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
    - `grep -q 'bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE")' src/PactSettler.sol` succeeds
    - `grep -q "is IPactSettler, AccessControl" src/PactSettler.sol` succeeds
    - `grep -q "onlyRole(SETTLER_ROLE)" src/PactSettler.sol` succeeds
    - PactSettler constructor matches the GATE-A E2 ruling: 3-arg `(address usdc_, address registry_, address pool_)`, no `address settler` storage var: `grep -q "address public settler" src/PactSettler.sol` MUST fail
    - `forge test --match-test test_Deploy_WiresSettlerRoleAndAdmin` exits 0
    - `forge test 2>&1 | grep -E "Suite result:|FAIL"` shows zero `FAIL`; pre-existing test count (70) still green
    - RED was observed and recorded in SUMMARY before GREEN (the compile failure on missing SETTLER_ROLE/3-arg ctor)
  </acceptance_criteria>
  <done>PactSettler inherits AccessControl with SETTLER_ROLE per the GATE-A E2 ruling; settleBatch is onlyRole-gated (still reverts NOT_IMPLEMENTED); Deployment.t.sol green; full suite green; RED-before-GREEN recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — TWO SETTLER_ROLE-gated endpoint-stats hooks on PactRegistry mirroring settle_batch.rs's two ep.* mutation points (per GATE-A ruling on E1, OPTION (a))</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      §E1 (RULED: OPTION (a) with conditions (1)-(4) — this is SETTLED LAW,
      cite it verbatim; the captain ruled, this is NOT "if captain ruled".
      Options (b) and (c) are REJECTED — no fallback branch). §D-SPLIT
      (RULED reading A, REFINED — the PERIOD RESET is WP-04 in this hook,
      specifically in `recordCallAndCapAccrual`). The earlier plan text "do
      NOT add the period reset" is OVERRIDDEN by this ruling.
    - .planning/phases/04-pactsettler-happy-path/04-CONTEXT.md "GATE A
      REFINEMENT" block (lines 38-49) + corrected per-event step 7 (lines
      240-252) — the period reset + the DISTINCT intended/actual inputs.
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q5 (the
      EndpointConfig mutation table + the E1 candidate analysis)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs
      THE AUTHORITY — read EXACTLY. There are TWO distinct `ep.*` mutation
      points in a FIXED order; the hook is SPLIT to mirror BOTH at their
      exact source positions:
      * ep mutation point 1 — lines 381-416 (inside the `endpoint_acct`
        borrow, BEFORE fee fan-out at :418 and BEFORE the refund transfer at
        :471): :380 `intended_refund_after_cap = refund_lamports` (the seed
        in 04-04, not here); :385 `ep.total_calls += 1`; :386-389
        `ep.total_premiums += premium`; :390-395 `if breach != 0 {
        ep.total_breaches += 1 }`; :396-399 the PERIOD RESET `if now >
        ep.current_period_start + 3600 { ep.current_period_start = now;
        ep.current_period_refunds = 0 }` — **WP-04, implement it in
        `recordCallAndCapAccrual`**; :400-408 the cap-remaining
        `saturating_sub` + clamp DECISION (`status = ExposureCapClamped`,
        `intended_refund_after_cap = cap_remaining`) — **WP-05, do NOT
        implement; leave a clean additive seam — in WP-04 `payableRefund =
        intendedRefund` unchanged**; :409-414 `if intended_refund_after_cap >
        0 { ep.current_period_refunds += intended_refund_after_cap }`
        (checked) — **WP-04, implement it using `payableRefund`**.
      * ep mutation point 2 — lines 492-500 (inside the paid `else` branch,
        AFTER `transfer_pda_signed` pool->agent at :471-477): :493-499
        `ep.total_refunds += actual_refund_lamports` (checked) — **WP-04,
        implement it in `recordRefundPaid`**. Note Solana only reaches
        :493-499 inside the paid branch (pool funded, `actual_refund > 0`),
        so `recordRefundPaid` is skipped by the 04-04 caller when
        `actualRefund == 0`.
      Note `now = Clock::get()?.unix_timestamp` (:140) -> `uint64(block.timestamp)`.
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 4 (the
      `AccessControl` import), 19 (`is IPactPool, AccessControl`), 22
      (`SETTLER_ROLE` constant), 29-36 (the ctor `_grantRole(DEFAULT_ADMIN_ROLE,
      registry.authority())` pattern), 79-92 (`_ckAdd`/`_ckSub`),
      96-130 (the `onlyRole(SETTLER_ROLE)` + `isRegistered`-gated hook shape)
      — the EXACT pattern these E1 hooks mirror (condition (1)).
    - packages/program-evm/protocol-evm-v1/src/PactRegistry.sol (the committed
      WP-02 contract — currently `is IPactRegistry, PactEvents`, has
      `address public override authority` + `onlyAuthority`; the EVM
      `currentPeriodStart` is seeded to `block.timestamp` at register time,
      line 134. Read its full structure: this task ADDITIVELY extends it —
      add OZ AccessControl as a NEW base, add SETTLER_ROLE + `_ckAdd` + the
      two hooks; alter NO existing function signature or behavior). Run
      `gitnexus_impact({target:"PactRegistry",direction:"upstream"})` first
      and report the blast radius (PURE-ADDITION regression gate, condition (3)).
    - packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
      (declare BOTH new hooks here additively; `EndpointConfig` already has
      `currentPeriodStart`/`currentPeriodRefunds` fields — no struct change)
  </read_first>
  <action>
    Implement EXACTLY per the GATE-A ruling on E1 (OPTION (a) RULED, conditions
    (1)-(4)) in 04-GATE-A-DECISIONS.md §E1, INCLUDING the D-SPLIT period-reset
    refinement. This is settled law — there is no "if captain ruled" branch and
    no (b)/(c) fallback (both REJECTED in the ruling). settle_batch.rs is the
    parity authority (D-LOCK-1); if applying this reveals a genuine
    settle_batch.rs ambiguity you cannot resolve from source, STOP-AND-ASK —
    do NOT guess.

    PARITY-SEAM CONSTRAINT (this is the central rule of this task):
    settle_batch.rs has TWO distinct `ep.*` mutation points in a FIXED order
    (point 1 = :385-414 BEFORE fee fan-out; point 2 = :493-499 AFTER the
    refund transfer). The E1 hook MUST be SPLIT into TWO functions so 04-04
    can place each at the EXACT source position. Do NOT consolidate them into
    one post-transfer call — that would break source mutation ORDER and make
    WP-05's cap clamp impossible to layer additively (the clamp reads
    `cap_remaining = exposure_cap.saturating_sub(current_period_refunds)`
    AFTER the period reset and BEFORE `current_period_refunds += intended`,
    and the clamped amount must drive the refund transfer — all of which is
    mutation-point-1, BEFORE the transfer; per GATE-A D-SPLIT the captain
    explicitly forbade a shape WP-05 must rewrite).

    SCOPE — TWO HOOKS:

    HOOK 1 — `recordCallAndCapAccrual` (settle_batch.rs:385-414, ep mutation
    point 1, called by 04-04 BEFORE the fee fan-out). EXACT signature:
      `function recordCallAndCapAccrual(bytes16 slug, uint64 premium,
       bool breach, uint64 intendedRefund) external onlyRole(SETTLER_ROLE)
       returns (uint64 payableRefund)`
    Body, in EXACT settle_batch.rs source order:
      - `if (!_registered[slug]) revert EndpointNotFound();`
      - `:385` `ep.totalCalls = _ckAdd(ep.totalCalls, 1);`
      - `:386-389` `ep.totalPremiums = _ckAdd(ep.totalPremiums, premium);`
      - `:390-395` `if (breach) ep.totalBreaches = _ckAdd(ep.totalBreaches, 1);`
      - `:396-399` PERIOD RESET (WP-04, D-SPLIT refinement):
        `if (uint64(block.timestamp) > ep.currentPeriodStart + 3600) {
        ep.currentPeriodStart = uint64(block.timestamp);
        ep.currentPeriodRefunds = 0; }`
      - `payableRefund = intendedRefund;`  // WP-04: unchanged, NO clamp
      - // ---- settle_batch.rs:400-408  WP-05 cap-clamp seam (do NOT
        // implement; do NOT collapse). WP-05 inserts HERE, between the period
        // reset and the accrual: `uint64 capRemaining =
        // ep.exposureCapPerHour - ep.currentPeriodRefunds (saturating); if
        // (payableRefund > capRemaining) { payableRefund = capRemaining;
        // status = ExposureCapClamped; }` and the clamped `payableRefund`
        // propagates back to 04-04 via the return, driving the refund
        // transfer. This is the parity seam — additive, zero WP-04 rewrite.
      - `:409-414` `if (payableRefund > 0) { ep.currentPeriodRefunds =
        _ckAdd(ep.currentPeriodRefunds, payableRefund); }`  // WP-04, uses the
        post-(would-be-)clamp amount; in WP-04 == intendedRefund
      - `return payableRefund;`  // == intendedRefund in WP-04; WP-05 returns
        the clamped amount with NO signature/call-site change
    Mutation ORDER inside HOOK 1 MUST match settle_batch.rs:385-414 exactly:
    calls -> premiums -> breaches -> period-reset -> (WP-05 cap-clamp seam) ->
    currentPeriodRefunds(payableRefund).

    HOOK 2 — `recordRefundPaid` (settle_batch.rs:493-499, ep mutation point 2,
    called by 04-04 AFTER the refund transfer). EXACT signature:
      `function recordRefundPaid(bytes16 slug, uint64 actualRefund)
       external onlyRole(SETTLER_ROLE)`
    Body:
      - `if (!_registered[slug]) revert EndpointNotFound();`
      - `:493-499` `ep.totalRefunds = _ckAdd(ep.totalRefunds, actualRefund);`
    (No `actualRefund > 0` guard needed inside the hook — 04-04 only calls it
    when `actualRefund > 0`, mirroring that Solana only reaches :493-499 in
    the paid branch; but a checked `_ckAdd` of 0 is harmless if called. Do
    NOT add `actualRefund > 0` early-return logic that would diverge from the
    simple `ep.total_refunds += actual_refund` source.)

    ONLY these three CLAMP DECISIONS stay WP-05 (do NOT implement; leave clean
    additive seams, do NOT shape the hooks so WP-05 must rewrite them):
      (i) the cap-remaining `saturating_sub` + `if intended > cap_remaining {
          clamp; status = ExposureCapClamped }` branch (:400-408) — WP-05
          inserts it INSIDE `recordCallAndCapAccrual` between the period reset
          and the `currentPeriodRefunds` accrual, adjusting the returned
          `payableRefund`;
      (ii) the `if pool_balance < intended { status = PoolDepleted;
           actual_refund = 0 }` branch (:462-469) — composed in 04-04, not here;
      (iii) protocol/endpoint pause kill-switches.

    DISTINCT INTENDED vs ACTUAL inputs (parity nuance — 04-GATE-A-DECISIONS.md
    §D-SPLIT "Parity nuance"): `currentPeriodRefunds` accumulates the post-cap
    INTENDED amount (:410, via `payableRefund` returned from HOOK 1);
    `totalRefunds` accumulates the ACTUAL paid amount (:493-499, via HOOK 2).
    In the WP-04 happy path these are equal (no clamp, pool funded) and the
    04-04 caller passes `intendedRefund == ev.refund` to HOOK 1 (which returns
    `payableRefund == ev.refund`) and `actualRefund == ev.refund` to HOOK 2,
    but the SPLIT into two functions at the two source positions is what makes
    WP-05's clamp (where intended != actual) layer in additively with NO
    rewrite.

    On the DelegateFailed path the hooks are NOT called — premium-in failed
    and `continue` runs before stats per settle_batch.rs:332-355.

    RED step (do FIRST, run, confirm exact failure before any src change):
    Add to PactSettler.t.sol (retargeted to the two-function shape — adapt the
    same scenarios; the period reset + four conditions + SET-07 coverage are
    UNCHANGED, only the call shape changes):
    - `test_EndpointStats_RecordCallAccumulates()`: via the deployed settler
      holding the registry's SETTLER_ROLE (Task 3 two-layer grant) or
      `vm.prank(settlerSigner)`, call `recordCallAndCapAccrual` on a
      registered slug: record one call (premium=1000, breach=false,
      intendedRefund=0) and assert the call returns `payableRefund == 0` and
      `reg.getEndpoint(slug).totalCalls == 1`, `.totalPremiums == 1000`,
      `.totalBreaches == 0`, `.totalRefunds == 0`; then a breaching call
      (premium=2000, breach=true, intendedRefund=500), assert the return is
      `payableRefund == 500` (unchanged, WP-04 no clamp), `.totalCalls == 2`,
      `.totalPremiums == 3000`, `.totalBreaches == 1`,
      `.currentPeriodRefunds == 500`, `.totalRefunds == 0` (HOOK 2 not yet
      called); then call `recordRefundPaid(slug, 500)` and assert
      `.totalRefunds == 500` (HOOK 2 accumulates the ACTUAL paid amount).
    - `test_EndpointStats_RecordCallRejectsNonSettler()`: a non-settler caller
      invoking `recordCallAndCapAccrual` reverts
      `IAccessControl.AccessControlUnauthorizedAccount` (condition (1) gate).
    - `test_EndpointStats_RecordRefundPaidRejectsNonSettler()`: a non-settler
      caller invoking `recordRefundPaid` reverts
      `IAccessControl.AccessControlUnauthorizedAccount` (HOOK 2 is also
      SETTLER_ROLE-gated).
    - `test_EndpointStats_PeriodReset()`: register slug (this seeds
      `currentPeriodStart = block.timestamp`); call
      `recordCallAndCapAccrual(slug, 1000, true, 300)` and assert
      `currentPeriodRefunds == 300`; then `vm.warp(block.timestamp + 3601)`
      (strictly > storedStart + 3600) and
      `recordCallAndCapAccrual(slug, 1000, true, 400)`; assert
      `currentPeriodStart` has advanced to the warped `block.timestamp` AND
      `currentPeriodRefunds == 400` (reset to 0 by the period reset inside
      HOOK 1, THEN += the new 400 — NOT 700).
    - `test_EndpointStats_WithinWindowAccumulates()`: register slug; two
      `recordCallAndCapAccrual` calls within the same hour (no warp, or
      `vm.warp(start + 1000)` between them — still <= storedStart + 3600);
      intendedRefund 300 then 400; assert `currentPeriodRefunds == 700`
      (accumulates, NO reset) and `currentPeriodStart` UNCHANGED from the
      register-time value.
    - `test_EndpointStats_RecordRefundPaidAccumulates()`: register slug; call
      `recordRefundPaid(slug, 200)` then `recordRefundPaid(slug, 300)`; assert
      `reg.getEndpoint(slug).totalRefunds == 500` (HOOK 2 accumulates the
      ACTUAL paid amount independently of HOOK 1).
    Run `forge test --match-test "test_EndpointStats"` — confirm RED: it MUST
    fail to compile (no `recordCallAndCapAccrual`/`recordRefundPaid`, no
    `SETTLER_ROLE`/`hasRole` on PactRegistry). Record the exact compiler error
    in the SUMMARY.

    GREEN step (E1 OPTION (a), per the ruling):
    1. PactRegistry.sol — ADDITIVE extension only (condition (3): alter no
       existing signature/behavior):
       - `import {AccessControl} from
         "@openzeppelin/contracts/access/AccessControl.sol";`
       - `contract PactRegistry is IPactRegistry, PactEvents, AccessControl`
         (AccessControl added as a NEW base — keep IPactRegistry + PactEvents).
       - `bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE");`
         (SAME literal as PactPool.sol:22).
       - In the constructor, AFTER `authority = authority_;`, add
         `_grantRole(DEFAULT_ADMIN_ROLE, authority_);` (mirrors
         PactPool.sol:35 `_grantRole(DEFAULT_ADMIN_ROLE, registry.authority())`
         — here `authority_` IS that same authority; condition (1)). Touch NO
         other ctor line; `onlyAuthority` and every existing function stay
         byte-identical.
       - Add `_ckAdd` mirroring PactPool.sol:79-84 verbatim (revert
         `ArithmeticOverflow`).
       - Add HOOK 1 (condition (2): bit-identical to settle_batch.rs:385-414,
         SAME ORDER):
         ```
         function recordCallAndCapAccrual(
             bytes16 slug, uint64 premium, bool breach,
             uint64 intendedRefund
         ) external onlyRole(SETTLER_ROLE) returns (uint64 payableRefund) {
             if (!_registered[slug]) revert EndpointNotFound();
             EndpointConfig storage ep = _endpoints[slug];
             // settle_batch.rs:385
             ep.totalCalls = _ckAdd(ep.totalCalls, 1);
             // settle_batch.rs:386-389
             ep.totalPremiums = _ckAdd(ep.totalPremiums, premium);
             // settle_batch.rs:390-395
             if (breach) ep.totalBreaches = _ckAdd(ep.totalBreaches, 1);
             // settle_batch.rs:396-399  PERIOD RESET (WP-04, D-SPLIT refinement)
             if (uint64(block.timestamp) > ep.currentPeriodStart + 3600) {
                 ep.currentPeriodStart = uint64(block.timestamp);
                 ep.currentPeriodRefunds = 0;
             }
             payableRefund = intendedRefund; // WP-04: NO clamp
             // settle_batch.rs:400-408  (WP-05 cap-clamp DECISION) — clean
             // additive seam: WP-05 inserts HERE, between the period reset and
             // the currentPeriodRefunds accrual, `capRemaining =
             // exposureCapPerHour.saturating_sub(currentPeriodRefunds); if
             // (payableRefund > capRemaining) { payableRefund = capRemaining;
             // status = ExposureCapClamped }` and the clamped payableRefund is
             // returned to 04-04 driving the refund transfer. Do NOT
             // implement; do NOT collapse the seam.
             // settle_batch.rs:409-414  (WP-04) — uses the post-cap amount
             if (payableRefund > 0) {
                 ep.currentPeriodRefunds =
                     _ckAdd(ep.currentPeriodRefunds, payableRefund);
             }
             return payableRefund;
         }
         ```
       - Add HOOK 2 (condition (2): bit-identical to settle_batch.rs:493-499):
         ```
         function recordRefundPaid(bytes16 slug, uint64 actualRefund)
             external onlyRole(SETTLER_ROLE)
         {
             if (!_registered[slug]) revert EndpointNotFound();
             EndpointConfig storage ep = _endpoints[slug];
             // settle_batch.rs:493-499  (WP-04) — uses ACTUAL paid amount
             ep.totalRefunds = _ckAdd(ep.totalRefunds, actualRefund);
         }
         ```
       HOOK 1 mutation ORDER MUST match settle_batch.rs:385-414 exactly; HOOK
       2 mirrors :493-499. The SPLIT is mandatory — do NOT merge them.
    2. IPactRegistry.sol — additively declare BOTH:
       `function recordCallAndCapAccrual(bytes16 slug, uint64 premium,
        bool breach, uint64 intendedRefund) external returns (uint64
        payableRefund);` and
       `function recordRefundPaid(bytes16 slug, uint64 actualRefund)
        external;` (no struct change — `currentPeriodStart`/
        `currentPeriodRefunds` already exist).
    Run `forge test --match-test "test_EndpointStats"` — confirm GREEN. Then
    the FULL suite `forge test` — the COMPLETE existing WP-02
    `PactRegistry.t.sol` and ALL 70 pre-existing forge tests MUST stay green
    (condition (3) PURE-ADDITION regression gate). If ANY pre-existing test
    fails, the change was not additive — fix it to be additive (do NOT alter
    test expectations).

    Run `gitnexus_detect_changes()` and confirm only PactRegistry.sol,
    IPactRegistry.sol, PactSettler.t.sol changed. No emojis. Conventional
    file-scoped commit (NO `git add -A`): `feat(protocol-evm-v1):
    split E1 endpoint-stats hooks (recordCallAndCapAccrual + recordRefundPaid)
    mirroring settle_batch.rs's two ep.* mutation points per GATE-A E1
    (WP-EVM-04)`.

    NOTE FOR THE RECORD: this ruling REFINES WP-03 D1's scope (it does NOT
    contradict it) — D1 barred PactPool reaching into the registry; it does
    NOT bar WP-04 adding its own SETTLER_ROLE-gated stat-writers where design
    spec §6 places EndpointConfig state. Cite 04-GATE-A-DECISIONS.md §E1
    "D1 scope refinement" in the SUMMARY.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | grep -E "Suite result:|FAIL" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PactRegistry is `is IPactRegistry, PactEvents, AccessControl`: `grep -q "is IPactRegistry, PactEvents, AccessControl" src/PactRegistry.sol` succeeds; `grep -q 'bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE")' src/PactRegistry.sol` succeeds; the ctor grants `DEFAULT_ADMIN_ROLE` to `authority_`: `grep -q "_grantRole(DEFAULT_ADMIN_ROLE, authority_)" src/PactRegistry.sol` succeeds (condition (1), PactPool pattern)
    - The hook is SPLIT into TWO functions: `grep -q "function recordCallAndCapAccrual(bytes16 slug, uint64 premium, bool breach, uint64 intendedRefund) external onlyRole(SETTLER_ROLE) returns (uint64 payableRefund)" src/PactRegistry.sol` succeeds AND `grep -q "function recordRefundPaid(bytes16 slug, uint64 actualRefund) external onlyRole(SETTLER_ROLE)" src/PactRegistry.sol` succeeds; there is NO single combined `recordSettlement` 5-arg function (`grep -q "function recordSettlement" src/PactRegistry.sol` MUST fail)
    - `recordCallAndCapAccrual` RETURNS the WP-05 clamp seam: `grep -q "payableRefund = intendedRefund" src/PactRegistry.sol` succeeds and `grep -q "return payableRefund" src/PactRegistry.sol` succeeds; the accrual uses `payableRefund` (`grep -q "_ckAdd(ep.currentPeriodRefunds, payableRefund)" src/PactRegistry.sol` succeeds)
    - The PERIOD RESET IS PRESENT in PactRegistry.sol inside `recordCallAndCapAccrual` (grep-POSITIVE — the old "forbid currentPeriodStart in PactRegistry" criterion is REMOVED per GATE-A D-SPLIT): `grep -q "ep.currentPeriodStart = uint64(block.timestamp)" src/PactRegistry.sol` succeeds AND the reset guard `grep -q "uint64(block.timestamp) > ep.currentPeriodStart + 3600" src/PactRegistry.sol` succeeds
    - `forge test --match-test test_EndpointStats_RecordCallAccumulates` exits 0: `recordCallAndCapAccrual` returns `payableRefund==intendedRefund` (0 then 500), after 2 calls `totalCalls==2`, `totalPremiums==3000`, `totalBreaches==1`, `currentPeriodRefunds==500`; after `recordRefundPaid(slug,500)` `totalRefunds==500`
    - `forge test --match-test test_EndpointStats_PeriodReset` exits 0: after `vm.warp(+3601)` and a 400 refund via `recordCallAndCapAccrual`, `currentPeriodStart` advanced to the warped timestamp AND `currentPeriodRefunds == 400` (reset then re-accumulated — NOT 700)
    - `forge test --match-test test_EndpointStats_WithinWindowAccumulates` exits 0: two in-window `recordCallAndCapAccrual` refunds (300 then 400) yield `currentPeriodRefunds == 700` and `currentPeriodStart` unchanged from register time
    - `forge test --match-test test_EndpointStats_RecordRefundPaidAccumulates` exits 0: `recordRefundPaid` 200 then 300 yields `totalRefunds == 500`
    - `forge test --match-test test_EndpointStats_RecordCallRejectsNonSettler` and `--match-test test_EndpointStats_RecordRefundPaidRejectsNonSettler` exit 0 (BOTH hooks SETTLER_ROLE-gated)
    - WP-05 clamp DECISION NOT present: `grep -q "ExposureCapClamped\|saturating\|cap_remaining\|capRemaining" src/PactRegistry.sol` MUST fail; the `recordCallAndCapAccrual` body carries the WP-05 cap-clamp SEAM comment between the period reset and the `currentPeriodRefunds` accrual (clean additive insertion point; the only `WP-05` token in PactRegistry.sol is that seam comment)
    - Mutation order matches settle_batch.rs: in `recordCallAndCapAccrual` the order is totalCalls -> totalPremiums -> totalBreaches -> currentPeriodStart-reset -> currentPeriodRefunds(payableRefund); `recordRefundPaid` does only totalRefunds(actualRefund) (verify by `grep -n` line order)
    - Accumulators use `_ckAdd` reverting `ArithmeticOverflow` (named error, not Panic 0x11); `_ckAdd` mirrors PactPool.sol:79-84
    - PURE ADDITION regression gate: the FULL pre-existing WP-02 `PactRegistry.t.sol` plus ALL 70 pre-existing forge tests stay green — `forge test 2>&1 | grep -c FAIL` == 0; no existing function signature/behavior altered (only the AccessControl base + SETTLER_ROLE + _ckAdd + the two hooks added; the ctor change is the single additive `_grantRole` line)
    - `gitnexus_detect_changes()` shows only PactRegistry.sol, IPactRegistry.sol, PactSettler.t.sol changed
    - RED recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>TWO SETTLER_ROLE-gated hooks on PactRegistry per the GATE-A E1 ruling (OPTION (a)) mirroring settle_batch.rs's two ep.* mutation points: `recordCallAndCapAccrual` (:385-414, returns payableRefund — the WP-05 cap-clamp seam — and does totalCalls/totalPremiums/totalBreaches + the WP-04 period reset + currentPeriodRefunds+=payableRefund in source order) and `recordRefundPaid` (:493-499, totalRefunds+=actualRefund); OZ AccessControl mirroring PactPool, bit-identical checked math, clean WP-05 cap-clamp seam inside HOOK 1, pure-addition regression gate held (full WP-02 PactRegistry.t.sol + all 70 pre-existing tests green), RED-before-GREEN recorded.</done>
</task>

<task type="auto">
  <name>Task 3: PactSettler.t.sol shared harness — setUp + helpers mirroring helpers.ts (E1xE2 two-layer grant)</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      §E2 "INTERPLAY" (with E1(a) the deployed PactSettler must hold
      SETTLER_ROLE on BOTH PactPool AND PactRegistry — the setUp does the
      two-layer grant on pool AND registry)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q8 (the
      common setUp block lines 646-675 + the helpers.ts -> Foundry helper
      mapping table lines 677-688) and §Q10 (toolchain: forge-std, MockUSDC,
      role-grant + vm.prank discipline)
    - packages/program-evm/protocol-evm-v1/test/PactPool.t.sol lines 16-49,
      169-172 (established setUp + _register + _grantSettler pattern, MockUSDC
      usage, settlerRole caching BEFORE vm.prank)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/tests/helpers.ts
      lines 701-744 (registerSimpleEndpoint defaults: flatPremium 500,
      percentBps 0, slaMs 5000, imputedCost 1000, exposureCap 5_000_000) and
      the provisionAgent/fundPoolDirect builders in 05-settle-batch.test.ts
      lines 37-43, 51-54
    - packages/program-evm/protocol-evm-v1/test/util/MockUSDC.sol (mint /
      approve / balanceOf surface)
  </read_first>
  <action>
    Extend test/PactSettler.t.sol (created in Task 1) with the SHARED harness
    that all 9 ported tests in plan 04 reuse. Mirror helpers.ts builders as
    Solidity helpers (Claude's discretion on layout per 04-CONTEXT.md, provided
    every Solana scenario is reproducible):

    - setUp(): `usdc = new MockUSDC();` PactRegistry with default template
      `FeeRecipient[8] d; d[0]={kind:0,destination:treasuryVault,bps:1000};`
      `reg = new PactRegistry(authority, address(usdc), treasuryVault, 3000,
      d, 1);` `pool = new PactPool(address(usdc), address(reg));`
      `settler = new PactSettler(address(usdc), address(reg), address(pool));`
      Cache `poolSettlerRole = pool.SETTLER_ROLE();`,
      `regSettlerRole = reg.SETTLER_ROLE();` and
      `settlerRole = settler.SETTLER_ROLE();` BEFORE any vm.prank (pitfall 3,
      RESEARCH §Common Pitfalls). E1xE2 TWO-LAYER GRANT — the deployed
      PactSettler must hold SETTLER_ROLE on BOTH pool AND registry (per the
      GATE-A E2 INTERPLAY ruling):
        `vm.prank(authority); pool.grantRole(poolSettlerRole, address(settler));`
        `vm.prank(authority); reg.grantRole(regSettlerRole, address(settler));`
        `vm.prank(authority); settler.grantRole(settlerRole, settlerSigner);`
      Addresses via `makeAddr`: authority, treasuryVault, settlerSigner.
    - `_register(bytes16 slug)`: `vm.prank(authority); reg.registerEndpoint(
      slug, 500, 0, 5000, 1000, 5_000_000, false, 0, none);` (helpers.ts
      defaults; identical to PactPool.t.sol _register).
    - `_registerWithRecipients(bytes16 slug, FeeRecipient[8] r, uint8 n)`:
      registerEndpoint with `feeRecipientsPresent=true`, count `n`, recipients
      `r` (for the 85/10/5 and mixed-3-endpoint tests).
    - `_fundPool(bytes16 slug, uint64 amount)`: `usdc.mint(authority, amount);
      vm.prank(authority); usdc.approve(address(pool), amount);
      vm.prank(authority); pool.topUp(slug, amount);` (mirrors fundPoolDirect).
    - `_provisionAgent(address agent, uint64 mintAmount, uint64 allowance)`:
      `usdc.mint(agent, mintAmount); vm.prank(agent);
      usdc.approve(address(settler), allowance);` (mirrors provisionAgent —
      the agent approves the SETTLER contract as ERC-20 spender per RESEARCH
      §Q3, NOT the pool).
    - A helper to build `IPactSettler.SettlementEvent` structs from the
      05-settle-batch.test.ts fields (callId fill(n) as `bytes16`, agent,
      slug, premium, refund, latencyMs, breach, feeRecipientCountHint,
      timestamp = `uint64(block.timestamp) - k`).
    This task writes harness/helpers ONLY — NO settleBatch behavior tests yet
    (those are plan 04). Confirm it compiles and the Task 1/2 tests still pass.

    No emojis. Conventional file-scoped commit:
    `test(protocol-evm-v1): PactSettler.t.sol shared harness mirroring
    helpers.ts (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-contract PactSettlerTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
    - `grep -q "function setUp" test/PactSettler.t.sol` and `grep -q "function _register" test/PactSettler.t.sol` and `grep -q "function _fundPool" test/PactSettler.t.sol` and `grep -q "function _provisionAgent" test/PactSettler.t.sol` all succeed
    - setUp does the E1xE2 TWO-LAYER grant on BOTH pool AND registry: `grep -q "pool.grantRole" test/PactSettler.t.sol` AND `grep -q "reg.grantRole" test/PactSettler.t.sol` AND `grep -q "settler.grantRole" test/PactSettler.t.sol` all succeed (the deployed settler holds SETTLER_ROLE on pool AND registry per the GATE-A E2 INTERPLAY ruling)
    - `_provisionAgent` approves `address(settler)` (not the pool): `grep -q "approve(address(settler)" test/PactSettler.t.sol` succeeds
    - `_register` uses the helpers.ts defaults exactly: `grep -q "registerEndpoint(slug, 500, 0, 5000, 1000, 5_000_000" test/PactSettler.t.sol` succeeds
    - SETTLER_ROLE values cached before any vm.prank (no `pool.SETTLER_ROLE()` / `reg.SETTLER_ROLE()` / `settler.SETTLER_ROLE()` call inside a prank block)
    - `forge test --match-contract PactSettlerTest 2>&1 | grep -c FAIL` == 0 (Task 1/2 tests still green; no new behavior tests added)
  </acceptance_criteria>
  <done>Shared PactSettler.t.sol harness in place mirroring helpers.ts (setUp E1xE2 two-layer role grant on pool AND registry, _register, _registerWithRecipients, _fundPool, _provisionAgent, event builder); compiles; Task 1/2 tests green.</done>
</task>

</tasks>

<verification>
- `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
- `forge test 2>&1 | grep -c FAIL` == 0 (70 pre-existing + new harness/E1/E2 tests green)
- PactSettler is `IPactSettler, AccessControl` with `SETTLER_ROLE` and an `onlyRole`-gated settleBatch (per GATE-A E2)
- PactRegistry has the TWO SETTLER_ROLE-gated hooks per GATE-A E1 mirroring settle_batch.rs's two ep.* mutation points: `recordCallAndCapAccrual` (:385-414, returns payableRefund, includes the WP-04 period reset + a clean WP-05 cap-clamp seam) and `recordRefundPaid` (:493-499)
- The deployed PactSettler holds SETTLER_ROLE on BOTH PactPool AND PactRegistry (E1xE2 two-layer grant)
- Deployment.t.sol compiles + passes against the new 3-arg ctor
- Pure-addition regression gate held: full WP-02 PactRegistry.t.sol + all 70 pre-existing tests green
- Every code task observed RED before GREEN (recorded in SUMMARY)
</verification>

<success_criteria>
- SET-01 infrastructure ready: settleBatch is SETTLER_ROLE-gated (per GATE-A E2)
- SET-07 infrastructure ready: the TWO E1 hooks per GATE-A E1, bit-identical settle_batch.rs:385-414 (`recordCallAndCapAccrual`) and :493-499 (`recordRefundPaid`) mutations in source order INCLUDING the WP-04 period reset, `payableRefund` return seam, named-error checked math
- PactSettler.t.sol harness reproduces every helpers.ts builder the 9 ported tests need, with the E1xE2 two-layer role grant
- Zero regressions: full WP-02 PactRegistry.t.sol + all 70 pre-existing forge tests still green (pure addition)
- WP-05 clamp DECISIONS (exposure-cap clamp inside recordCallAndCapAccrual, PoolDepleted branch in 04-04, pause) explicitly NOT implemented; clean additive seams left (the cap clamp is the return-value seam in recordCallAndCapAccrual — zero WP-04 rewrite)
</success_criteria>

<output>
After completion, create `.planning/phases/04-pactsettler-happy-path/04-02-SUMMARY.md`
recording: the E1/E2 rulings implemented per 04-GATE-A-DECISIONS.md, the RED
failure observed for each TDD task before GREEN, the exact SPLIT hook
signatures (`recordCallAndCapAccrual(bytes16,uint64,bool,uint64) returns
(uint64 payableRefund)` and `recordRefundPaid(bytes16,uint64)`), confirmation
the WP-04 period reset is in `recordCallAndCapAccrual` with a clean WP-05
cap-clamp seam (the `payableRefund` return), the D1 scope refinement note (per
§E1), and confirmation the pure-addition regression gate held (full WP-02
PactRegistry.t.sol + all 70 pre-existing tests).
</output>
