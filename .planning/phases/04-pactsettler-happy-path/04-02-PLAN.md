---
phase: 04-pactsettler-happy-path
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
  - packages/program-evm/protocol-evm-v1/src/PactRegistry.sol
  - packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
  - packages/program-evm/protocol-evm-v1/test/Deployment.t.sol
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
autonomous: true
requirements: [SET-01, SET-07]
must_haves:
  truths:
    - "PactSettler inherits OZ AccessControl with SETTLER_ROLE; constructor matches the GATE-A E2 ruling; Deployment.t.sol compiles and passes against the new ctor"
    - "Per the GATE-A E1 ruling, a SETTLER_ROLE-gated endpoint-stats write path exists and is reachable from PactSettler"
    - "test/PactSettler.t.sol exists with the shared setUp + helper scaffold mirroring helpers.ts; all 70 pre-existing forge tests stay green"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "AccessControl + SETTLER_ROLE; ctor per E2; settleBatch still reverts pending plans 03/04"
      contains: "SETTLER_ROLE"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "Shared setUp + _register/_fundPool/_provisionAgent helpers + role wiring"
      contains: "function setUp"
  key_links:
    - from: "test/PactSettler.t.sol setUp"
      to: "pool.grantRole(SETTLER_ROLE, address(settler))"
      via: "two-layer role grant (pool->settler contract, settler->settlerSigner)"
      pattern: "grantRole"
    - from: "PactSettler"
      to: "endpoint-stats write path (per E1 ruling)"
      via: "the E1-resolved mechanism"
      pattern: "per GATE-A ruling on E1"
---

<objective>
Land the WP-04 foundation: refactor PactSettler to OZ AccessControl
(SETTLER_ROLE) per the GATE-A E2 ruling, implement the endpoint-stats write
path per the GATE-A E1 ruling, fix Deployment.t.sol for the new constructor,
and stand up the test/PactSettler.t.sol harness (setUp + helpers mirroring
helpers.ts). settleBatch logic is NOT implemented here (plans 03/04).

Purpose: SET-01 (settler-role gate) and SET-07 (stats write path) need the
AccessControl + stats infrastructure in place before the per-event loop is
ported. Every code change is strict TDD: RED (failing forge test, confirm the
exact failure), then GREEN (implement, confirm pass).

Output: AccessControl-enabled PactSettler, E1-resolved endpoint-stats path,
green Deployment.t.sol, and the PactSettler.t.sol harness — with all 70
pre-existing forge tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pactsettler-happy-path/04-CONTEXT.md
@.planning/phases/04-pactsettler-happy-path/04-RESEARCH.md
@.planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
@.planning/phases/04-pactsettler-happy-path/04-01-SUMMARY.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program-evm/protocol-evm-v1/src/PactPool.sol
@packages/program-evm/protocol-evm-v1/src/PactRegistry.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
@packages/program-evm/protocol-evm-v1/test/PactPool.t.sol
@packages/program-evm/protocol-evm-v1/test/Deployment.t.sol

<interfaces>
PactPool ctor + role pattern PactSettler must mirror (PactPool.sol:29-36):
```solidity
constructor(address usdc_, address registry_) {
    _usdc = IERC20(usdc_); registry = IPactRegistry(registry_);
    _grantRole(DEFAULT_ADMIN_ROLE, registry.authority());
}
bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE");
```

Role-grant + vm.prank discipline (PactPool.t.sol:32-43, 169-172):
```solidity
function setUp() { ...; settlerRole = pool.SETTLER_ROLE(); } // cache BEFORE prank
function _grantSettler() { vm.prank(authority); pool.grantRole(settlerRole, settler); }
```

PactRegistry constructor signature (from Deployment.t.sol / PactPool.t.sol):
new PactRegistry(authority, address(usdc), treasuryVault, maxTotalFeeBps,
FeeRecipient[8] defaults, uint8 defaultCount)

Helpers.ts defaults to mirror (confirmed: PactPool.t.sol _register uses the
same numbers):
reg.registerEndpoint(slug, 500 /*flatPremium*/, 0 /*percentBps*/,
  5000 /*slaMs*/, 1000 /*imputedCost*/, 5_000_000 /*exposureCap*/,
  false /*recipientsPresent*/, 0 /*count*/, none)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — PactSettler AccessControl + SETTLER_ROLE; fix Deployment.t.sol (E2)</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      (the captain's E2 ruling — implement EXACTLY as ruled; the action below
      assumes the research lead resolution: AccessControl, 3-arg ctor,
      DEFAULT_ADMIN_ROLE to registry.authority(); if the captain overrode it,
      follow the recorded ruling instead and note the deviation)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q7 (E2
      required changes 1-6) and §"Architecture Patterns" (post-WP-04 PactSettler
      structure block, lines 854-884)
    - packages/program-evm/protocol-evm-v1/src/PactSettler.sol (current
      4-arg ctor + plain `address settler` to replace)
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 19-36
      (the AccessControl ctor pattern to mirror exactly)
    - packages/program-evm/protocol-evm-v1/test/Deployment.t.sol lines 42-51
      (test_DeployPactSettler — currently passes 4 args incl. `address(this)`
      settler placeholder; must update to the E2 ctor)
  </read_first>
  <action>
    Implement per the GATE-A E2 ruling in 04-GATE-A-DECISIONS.md. The steps
    below encode the research lead resolution; if the captain's recorded ruling
    differs, follow the ruling and record the deviation in the SUMMARY.

    RED step (do FIRST, run, confirm the exact failure before any src change):
    Add to a new `packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol`
    a forge-std test contract `PactSettlerTest is Test` with a minimal
    `test_Deploy_WiresSettlerRoleAndAdmin()` asserting:
      - `settler.hasRole(0x00, authority) == true` (DEFAULT_ADMIN_ROLE to
        registry.authority())
      - `settler.SETTLER_ROLE() == keccak256("SETTLER_ROLE")`
      - `settler.hasRole(settler.SETTLER_ROLE(), settlerSigner) == false`
        before any grant
    Run `forge test --match-contract PactSettlerTest` from
    `packages/program-evm/protocol-evm-v1/`. Confirm RED: it MUST fail to
    COMPILE because PactSettler has no `SETTLER_ROLE`/`hasRole` and the 3-arg
    ctor does not yet exist. Record the exact compiler error in the SUMMARY.

    GREEN step:
    1. PactSettler.sol: `import {AccessControl} from
       "@openzeppelin/contracts/access/AccessControl.sol";` and
       `import {IPactRegistry} from "./interfaces/IPactRegistry.sol";`
    2. `contract PactSettler is IPactSettler, AccessControl`
    3. `bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE");`
    4. Replace the 4-arg ctor with 3-arg
       `constructor(address usdc_, address registry_, address pool_)`:
       set immutables `usdc`, `registry`, `pool` (keep `pool` typed as
       `IPactPool` per the research structure block); remove the plain
       `address public settler` storage var and the TODO comment;
       `_grantRole(DEFAULT_ADMIN_ROLE, IPactRegistry(registry_).authority());`
       (mirrors PactPool.sol:35).
    5. Add `onlyRole(SETTLER_ROLE)` to `settleBatch` (keep the body reverting
       — leave a single explicit `revert("NOT_IMPLEMENTED");` so plans 03/04
       fill it; the role gate is now active for SET-01).
    6. Deployment.t.sol test_DeployPactSettler: change to the 3-arg ctor
       `new PactSettler(ArcConfig.ARC_TESTNET_USDC, address(reg), address(pool))`
       — it now needs a real registry so `registry.authority()` is callable
       in the ctor; construct a PactRegistry + PactPool the same way
       test_DeployPactPool does (lines 33-37) and pass them. Keep the
       bytecode + `s.usdc()` asserts.
    Run `forge test --match-contract PactSettlerTest` — confirm GREEN. Then
    run the FULL suite `forge test` — confirm all pre-existing tests
    (Deployment + the 70 from WP-01..03) stay green.

    No emojis. Conventional commits, file-scoped (NO `git add -A`): commit the
    src change and the Deployment.t.sol fix together as one logical change
    `refactor(protocol-evm-v1): PactSettler AccessControl SETTLER_ROLE per
    GATE-A E2 (WP-EVM-04)`; commit the new test file separately
    `test(protocol-evm-v1): PactSettler.t.sol deploy/role harness (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-contract PactSettlerTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
    - `grep -q 'bytes32 public constant SETTLER_ROLE = keccak256("SETTLER_ROLE")' src/PactSettler.sol` succeeds
    - `grep -q "is IPactSettler, AccessControl" src/PactSettler.sol` succeeds
    - `grep -q "onlyRole(SETTLER_ROLE)" src/PactSettler.sol` succeeds
    - PactSettler constructor matches the recorded GATE-A E2 ruling (research lead = 3-arg `(address usdc_, address registry_, address pool_)`, no `address settler` storage var: `grep -q "address public settler" src/PactSettler.sol` MUST fail)
    - `forge test --match-test test_Deploy_WiresSettlerRoleAndAdmin` exits 0
    - `forge test 2>&1 | grep -E "Suite result:|FAIL"` shows zero `FAIL`; pre-existing test count (70) still green
    - RED was observed and recorded in SUMMARY before GREEN (the compile failure on missing SETTLER_ROLE/3-arg ctor)
  </acceptance_criteria>
  <done>PactSettler inherits AccessControl with SETTLER_ROLE per E2; settleBatch is onlyRole-gated (still reverts NOT_IMPLEMENTED); Deployment.t.sol green; full suite green; RED-before-GREEN recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — endpoint-stats write path per GATE-A E1 ruling</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      (the captain's E1 ruling — this task implements EXACTLY the resolved
      mechanism; the action below assumes E1 option (a): extend PactRegistry
      with a SETTLER_ROLE-gated stat hook symmetric to WP-03 pool hooks. If
      the captain ruled (b) PactSettler-local storage, implement that instead
      and note the deviation. If (c), STOP and report — (c) fails SET-07.)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q5 (the
      EndpointConfig mutation table: total_calls@385, total_premiums@387-389,
      total_breaches@391-394, current_period_refunds@410-414 [WP-04 additive
      path only], total_refunds@496-499; and the E1 candidate analysis)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs
      lines 385-416 (the WP-04 additive stat block; note period RESET at
      396-399 and the clamp DECISION at 404-408 are WP-05 — see Task scope
      note) and lines 493-500 (ep.total_refunds += actual_refund)
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 22, 79-130
      (the SETTLER_ROLE + _ckAdd pattern + isRegistered gate to mirror)
    - packages/program-evm/protocol-evm-v1/src/PactRegistry.sol (the WP-02
      contract — read its storage layout + how EndpointConfig is stored before
      adding any hook; run `gitnexus_impact({target:"PactRegistry",direction:"upstream"})`
      first and report blast radius — modifying a committed WP-02 contract)
  </read_first>
  <action>
    Implement per the recorded GATE-A E1 ruling. The steps below encode E1
    option (a) (research lead). Follow the captain's recorded ruling if it
    differs; STOP and report if the ruling was (c).

    SCOPE NOTE (derived from settle_batch.rs, NOT a guess — RESEARCH §Q1 step
    7 / §Q5): WP-04 persists ONLY the unconditional additive mutations that
    execute on the happy path: `total_calls += 1`, `total_premiums += premium`,
    `total_breaches += 1` (if breach), `total_refunds += actualRefund` (on a
    paid refund), and `current_period_refunds += actualRefund` (the additive
    accounting at settle_batch.rs:410-414 which executes when no clamp fires —
    in all 9 happy-path tests the default exposureCap is 5_000_000 and refunds
    are <= 50_000 so the clamp branch is never taken). The period RESET
    (settle_batch.rs:396-399) and the cap-clamp DECISION
    (`status = ExposureCapClamped`, lines 404-408) are explicitly WP-05 — do
    NOT implement them. Use `_ckAdd` mirroring `checked_add ->
    ArithmeticOverflow` for every accumulator (D-LOCK-4 / D6).

    RED step: add to PactSettler.t.sol a test
    `test_EndpointStats_RecordCallAccumulates()` that, via the E1-resolved
    write path, records one call (premium=1000, breach=false, refund=0) on a
    registered slug and asserts `reg.getEndpoint(slug).totalCalls == 1`,
    `.totalPremiums == 1000`, `.totalBreaches == 0`, `.totalRefunds == 0`;
    then records a breaching call (premium=2000, breach=true,
    actualRefund=500) and asserts `.totalCalls == 2`, `.totalPremiums == 3000`,
    `.totalBreaches == 1`, `.totalRefunds == 500`,
    `.currentPeriodRefunds == 500`. Also assert the hook is SETTLER_ROLE-gated:
    a non-settler caller reverts
    `IAccessControl.AccessControlUnauthorizedAccount`. Run
    `forge test --match-test test_EndpointStats_RecordCallAccumulates` — confirm
    RED (no such write path / function compile error). Record the failure.

    GREEN step (E1 option (a)): in IPactRegistry.sol declare and in
    PactRegistry.sol implement a SETTLER_ROLE-gated hook, e.g.
    `function recordSettlement(bytes16 slug, uint64 premium, bool breach,
    uint64 actualRefund) external onlyRole(SETTLER_ROLE)` that, on a
    registered slug (`isRegistered` gate, mirroring the pool hooks):
      - `ep.totalCalls = _ckAdd(ep.totalCalls, 1);`
      - `ep.totalPremiums = _ckAdd(ep.totalPremiums, premium);`
      - `if (breach) ep.totalBreaches = _ckAdd(ep.totalBreaches, 1);`
      - `if (actualRefund > 0) { ep.totalRefunds = _ckAdd(ep.totalRefunds,
        actualRefund); ep.currentPeriodRefunds = _ckAdd(ep.currentPeriodRefunds,
        actualRefund); }`
    PactRegistry must inherit/expose OZ AccessControl with SETTLER_ROLE the
    same way PactPool does (DEFAULT_ADMIN_ROLE to authority() in ctor if not
    already present — check first; do the minimal change). Add `_ckAdd`
    mirroring PactPool._ckAdd. Mutation ORDER must match settle_batch.rs:385-499
    exactly (calls, premiums, breaches, then refunds/period). Do NOT add the
    period reset or clamp decision.
    Run the matched test — confirm GREEN. Run FULL `forge test` — confirm all
    WP-02 PactRegistry tests + all 70 pre-existing tests stay green
    (regression gate for a committed contract).

    Run `gitnexus_detect_changes()` and confirm only PactRegistry +
    IPactRegistry + PactSettler.t.sol changed. No emojis. Conventional
    file-scoped commit: `feat(protocol-evm-v1): SETTLER_ROLE-gated endpoint
    stats hook per GATE-A E1 (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | grep -E "Suite result:|FAIL" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The endpoint-stats write path exists per the recorded E1 ruling and is SETTLER_ROLE-gated (non-settler caller reverts `AccessControlUnauthorizedAccount`)
    - `forge test --match-test test_EndpointStats_RecordCallAccumulates` exits 0 with the exact assertions: after 2 calls `totalCalls==2`, `totalPremiums==3000`, `totalBreaches==1`, `totalRefunds==500`, `currentPeriodRefunds==500`
    - Mutation order in the hook matches settle_batch.rs:385-499 (calls -> premiums -> breaches -> refunds/period); period RESET (lines 396-399) and clamp DECISION (404-408) are NOT present: `grep -n "currentPeriodStart" src/PactRegistry.sol` shows no settler-side reset write, `grep -n "ExposureCapClamped" src/PactRegistry.sol` finds nothing
    - Accumulators use a checked-add that reverts `ArithmeticOverflow` (named error, not Panic 0x11)
    - `forge test 2>&1 | grep -c FAIL` == 0; WP-02 PactRegistry.t.sol + all 70 pre-existing tests still green
    - `gitnexus_detect_changes()` shows only PactRegistry.sol, IPactRegistry.sol, PactSettler.t.sol changed
    - RED recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>Endpoint-stats write path implemented per E1 ruling, SETTLER_ROLE-gated, additive WP-04 mutations only (no period reset / clamp), all regression tests green, RED-before-GREEN recorded.</done>
</task>

<task type="auto">
  <name>Task 3: PactSettler.t.sol shared harness — setUp + helpers mirroring helpers.ts</name>
  <read_first>
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q8 (the
      common setUp block lines 646-675 + the helpers.ts -> Foundry helper
      mapping table lines 677-688) and §Q10 (toolchain: forge-std, MockUSDC,
      role-grant + vm.prank discipline)
    - packages/program-evm/protocol-evm-v1/test/PactPool.t.sol lines 16-49,
      169-172 (established setUp + _register + _grantSettler pattern, MockUSDC
      usage, settlerRole caching BEFORE vm.prank)
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/tests/helpers.ts
      lines 701-744 (registerSimpleEndpoint defaults: flatPremium 500,
      percentBps 0, slaMs 5000, imputedCost 1000, exposureCap 5_000_000) and
      the provisionAgent/fundPoolDirect builders in 05-settle-batch.test.ts
      lines 37-43, 51-54
    - packages/program-evm/protocol-evm-v1/test/util/MockUSDC.sol (mint /
      approve / balanceOf surface)
  </read_first>
  <action>
    Extend test/PactSettler.t.sol (created in Task 1) with the SHARED harness
    that all 9 ported tests in plan 04 reuse. Mirror helpers.ts builders as
    Solidity helpers (Claude's discretion on layout per 04-CONTEXT.md, provided
    every Solana scenario is reproducible):

    - setUp(): `usdc = new MockUSDC();` PactRegistry with default template
      `FeeRecipient[8] d; d[0]={kind:0,destination:treasuryVault,bps:1000};`
      `reg = new PactRegistry(authority, address(usdc), treasuryVault, 3000,
      d, 1);` `pool = new PactPool(address(usdc), address(reg));`
      `settler = new PactSettler(address(usdc), address(reg), address(pool));`
      Cache `poolSettlerRole = pool.SETTLER_ROLE();` and
      `settlerRole = settler.SETTLER_ROLE();` BEFORE any vm.prank (pitfall 3,
      RESEARCH §Common Pitfalls). Two-layer grant:
      `vm.prank(authority); pool.grantRole(poolSettlerRole, address(settler));`
      `vm.prank(authority); settler.grantRole(settlerRole, settlerSigner);`
      Addresses via `makeAddr`: authority, treasuryVault, settlerSigner.
    - `_register(bytes16 slug)`: `vm.prank(authority); reg.registerEndpoint(
      slug, 500, 0, 5000, 1000, 5_000_000, false, 0, none);` (helpers.ts
      defaults; identical to PactPool.t.sol _register).
    - `_registerWithRecipients(bytes16 slug, FeeRecipient[8] r, uint8 n)`:
      registerEndpoint with `feeRecipientsPresent=true`, count `n`, recipients
      `r` (for the 85/10/5 and mixed-3-endpoint tests).
    - `_fundPool(bytes16 slug, uint64 amount)`: `usdc.mint(authority, amount);
      vm.prank(authority); usdc.approve(address(pool), amount);
      vm.prank(authority); pool.topUp(slug, amount);` (mirrors fundPoolDirect).
    - `_provisionAgent(address agent, uint64 mintAmount, uint64 allowance)`:
      `usdc.mint(agent, mintAmount); vm.prank(agent);
      usdc.approve(address(settler), allowance);` (mirrors provisionAgent —
      the agent approves the SETTLER contract as ERC-20 spender per RESEARCH
      §Q3, NOT the pool).
    - A helper to build `IPactSettler.SettlementEvent` structs from the
      05-settle-batch.test.ts fields (callId fill(n) as `bytes16`, agent,
      slug, premium, refund, latencyMs, breach, feeRecipientCountHint,
      timestamp = `uint64(block.timestamp) - k`).
    This task writes harness/helpers ONLY — NO settleBatch behavior tests yet
    (those are plan 04). Confirm it compiles and the Task 1/2 tests still pass.

    No emojis. Conventional file-scoped commit:
    `test(protocol-evm-v1): PactSettler.t.sol shared harness mirroring
    helpers.ts (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-contract PactSettlerTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
    - `grep -q "function setUp" test/PactSettler.t.sol` and `grep -q "function _register" test/PactSettler.t.sol` and `grep -q "function _fundPool" test/PactSettler.t.sol` and `grep -q "function _provisionAgent" test/PactSettler.t.sol` all succeed
    - setUp does the two-layer grant: `grep -q "pool.grantRole" test/PactSettler.t.sol` and `grep -q "settler.grantRole" test/PactSettler.t.sol` succeed
    - `_provisionAgent` approves `address(settler)` (not the pool): `grep -q "approve(address(settler)" test/PactSettler.t.sol` succeeds
    - `_register` uses the helpers.ts defaults exactly: `grep -q "registerEndpoint(slug, 500, 0, 5000, 1000, 5_000_000" test/PactSettler.t.sol` succeeds
    - SETTLER_ROLE cached before any vm.prank (no `pool.SETTLER_ROLE()` / `settler.SETTLER_ROLE()` call inside a prank block)
    - `forge test --match-contract PactSettlerTest 2>&1 | grep -c FAIL` == 0 (Task 1/2 tests still green; no new behavior tests added)
  </acceptance_criteria>
  <done>Shared PactSettler.t.sol harness in place mirroring helpers.ts (setUp two-layer role grant, _register, _registerWithRecipients, _fundPool, _provisionAgent, event builder); compiles; Task 1/2 tests green.</done>
</task>

</tasks>

<verification>
- `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
- `forge test 2>&1 | grep -c FAIL` == 0 (70 pre-existing + new harness/E1/E2 tests green)
- PactSettler is `IPactSettler, AccessControl` with `SETTLER_ROLE` and an `onlyRole`-gated settleBatch
- Endpoint-stats write path exists per the recorded GATE-A E1 ruling, SETTLER_ROLE-gated, WP-04 additive mutations only
- Deployment.t.sol compiles + passes against the new ctor
- Every code task observed RED before GREEN (recorded in SUMMARY)
</verification>

<success_criteria>
- SET-01 infrastructure ready: settleBatch is SETTLER_ROLE-gated
- SET-07 infrastructure ready: endpoint-stats write path per E1, additive WP-04 mutations, named-error checked math
- PactSettler.t.sol harness reproduces every helpers.ts builder the 9 ported tests need
- Zero regressions: all 70 pre-existing forge tests still green
- WP-05 scope (period reset, clamp decision) explicitly NOT implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-pactsettler-happy-path/04-02-SUMMARY.md`
recording: the E1/E2 ruling implemented (and any captain deviation from the
research lead), the RED failure observed for each TDD task before GREEN, the
endpoint-stats hook signature, and confirmation the 70-test regression gate
held.
</output>
