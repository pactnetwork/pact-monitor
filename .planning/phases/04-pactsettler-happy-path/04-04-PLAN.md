---
phase: 04-pactsettler-happy-path
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
autonomous: false
requirements: [SET-05, SET-06, SET-07, SET-08]
must_haves:
  truths:
    - "On premium-in success: pool credited gross premium; fee fan-out pays each recipient floor(premium*bps/10000), pool keeps the residual; balances bit-identical to the 05 default-10%, 85/10/5, and mixed-3-endpoint assertions (SET-05)"
    - "On breach with sufficient pool + within cap: refund flows pool->agent for the full intended amount, status Settled, actualRefund == intended (SET-06)"
    - "Pool + endpoint stats updated via the GATE-A E1 SPLIT hooks composed at the EXACT settle_batch.rs mutation positions: recordCallAndCapAccrual (ep point 1, :385-414, BEFORE the fee fan-out, returns payableRefund) then recordRefundPaid (ep point 2, :493-499, AFTER the refund transfer); WP-04 happy path payableRefund==ev.refund and actualRefund==ev.refund, leaving the WP-05 cap-clamp seam inside recordCallAndCapAccrual and the PoolDepleted seam in the empty settleBatch if (SET-07)"
    - "Exactly one IPactSettler.CallSettled event per call carrying premium/refund/actualRefund/status/breach/latencyMs/timestamp (SET-08, per GATE-A ruling on E3/E4)"
    - "All 9 ported happy-path tests green; all 70 pre-existing forge tests green; forge build clean"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "complete settleBatch happy-path per-event loop with the two ep.* hooks at the exact source positions"
      contains: "creditPremium"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "9 ported happy-path tests with exact 05 numeric assertions"
      contains: "test_SingleEventDefaultTreasuryFanOut"
  key_links:
    - from: "settleBatch success path"
      to: "pool.creditPremium / pool.payout / pool.debitForFees / pool.debitForRefund"
      via: "WP-03 SETTLER_ROLE hooks composed in settle_batch.rs order"
      pattern: "pool\\.(creditPremium|debitForFees|debitForRefund|payout)"
    - from: "settleBatch stats — ep mutation point 1 (BEFORE fee fan-out)"
      to: "registry.recordCallAndCapAccrual(slug, ev.premium, ev.breach, intendedRefundAfterCap)"
      via: "GATE-A E1 split hook 1 (settle_batch.rs:385-414); returns payableRefund (==ev.refund in WP-04; the WP-05 cap-clamp seam)"
      pattern: "recordCallAndCapAccrual"
    - from: "settleBatch stats — ep mutation point 2 (AFTER the refund transfer)"
      to: "registry.recordRefundPaid(slug, actualRefund)"
      via: "GATE-A E1 split hook 2 (settle_batch.rs:493-499); called only when actualRefund > 0 (mirrors Solana's paid branch)"
      pattern: "recordRefundPaid"
    - from: "settleBatch per call"
      to: "CallSettled event (per GATE-A ruling on E3/E4)"
      via: "exactly one emit per call, typed-enum IPactSettler.CallSettled, no second PactEvents emission"
      pattern: "emit .*CallSettled"
---

<objective>
Complete the settleBatch happy-path per-event loop: pool gross credit
(SET-07), endpoint stats via the GATE-A E1 SPLIT hooks composed at the EXACT
settle_batch.rs mutation positions — `recordCallAndCapAccrual` (ep point 1,
:385-414, BEFORE the fee fan-out) then `recordRefundPaid` (ep point 2,
:493-499, AFTER the refund transfer) (SET-07) — fee fan-out floor-div +
residual (SET-05), breach refund on sufficient liquidity within cap (SET-06),
and exactly one IPactSettler.CallSettled event per call (SET-08) — replacing
the plan 03 provisional emit. Port the remaining 5 happy-path tests of
05-settle-batch.test.ts with bit-identical numeric assertions, ensure all 9
are green, and STOP at GATE B (report to file + cockpit notice; no push, no
PR #204 comment).

Purpose: This is the economic core of the parity port. Every arithmetic
operation AND mutation ORDER must be bit-identical to settle_batch.rs:357-522
and the 05 Solana assertions. The TWO ep.* mutation points are composed at
their EXACT source positions (recordCallAndCapAccrual BEFORE the fee fan-out,
recordRefundPaid AFTER the refund transfer) so WP-05's cap clamp and
PoolDepleted branch layer in ADDITIVELY with zero WP-04 rewrite (GATE-A
D-SPLIT). Strict TDD.

Output: full settleBatch happy path with the two ep.* hooks at the exact
source positions; 9 ported tests + 70 pre-existing tests green; forge build
clean; GATE-B report written to
.planning/phases/04-pactsettler-happy-path/04-REPORT-gateB.md + cockpit notice;
execution STOPPED (no push/PR).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pactsettler-happy-path/04-CONTEXT.md
@.planning/phases/04-pactsettler-happy-path/04-RESEARCH.md
@.planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
@.planning/phases/04-pactsettler-happy-path/04-03-SUMMARY.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program-evm/protocol-evm-v1/src/PactPool.sol
@packages/program-evm/protocol-evm-v1/src/PactRegistry.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactPool.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactSettler.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
@packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol

<interfaces>
WP-03 pool hooks composed (IPactPool.sol — DO NOT reopen, D-LOCK-4):
```solidity
function creditPremium(bytes16 slug, uint64 amount) external; // bal+=a; totalPremiums+=a
function debitForFees(bytes16 slug, uint64 amount) external;   // bal-=a
function debitForRefund(bytes16 slug, uint64 amount) external; // bal-=a; totalRefunds+=a
function payout(address to, uint64 amount) external;           // USDC egress
function balanceOf(bytes16 slug) external view returns (PoolState memory);
struct PoolState { uint64 currentBalance; uint64 totalDeposits; uint64 totalPremiums; uint64 totalRefunds; uint64 createdAt; }
```

GATE-A E1 endpoint-stats hooks from plan 02 (per 04-GATE-A-DECISIONS.md §E1,
signatures recorded in 04-02-SUMMARY.md) — SPLIT into TWO functions so
settleBatch composes them at the EXACT settle_batch.rs mutation positions:
```solidity
// PactRegistry, both onlyRole(SETTLER_ROLE).
// ep mutation point 1 — settle_batch.rs:385-414, composed BEFORE the fee
// fan-out: totalCalls+=1; totalPremiums+=premium; if breach totalBreaches+=1;
// the PERIOD RESET (if now > currentPeriodStart+3600 -> reset);
// [WP-05 cap-clamp seam — returns payableRefund]; currentPeriodRefunds +=
// payableRefund. The period reset lives HERE (PactRegistry.sol),
// legitimately — it is WP-04 per GATE-A D-SPLIT. In WP-04 payableRefund ==
// intendedRefund (NO clamp); WP-05 returns the clamped amount with no
// signature/call-site change.
function recordCallAndCapAccrual(
    bytes16 slug, uint64 premium, bool breach, uint64 intendedRefund
) external returns (uint64 payableRefund);
// ep mutation point 2 — settle_batch.rs:493-499, composed AFTER the refund
// transfer: totalRefunds += actualRefund. settleBatch calls this ONLY when
// actualRefund > 0 (mirrors Solana only reaching :493-499 in the paid branch).
function recordRefundPaid(bytes16 slug, uint64 actualRefund) external;
```

FeeRecipient (IPactRegistry.sol): { uint8 kind; address destination; uint16 bps; }
EndpointConfig.feeRecipients is FeeRecipient[8]; iterate ep.feeRecipientCount.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — pool credit + the TWO ep.* hooks at the EXACT source positions + fee fan-out + breach refund + one CallSettled (SET-05/06/07/08)</name>
  <files>packages/program-evm/protocol-evm-v1/src/PactSettler.sol, packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol</files>
  <read_first>
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs
      THE AUTHORITY (D-LOCK-1) — the mutation ORDER is UNAMBIGUOUS and has
      TWO distinct ep.* mutation points; mirror it EXACTLY:
      lines 357-369 (pool gross credit: current_balance += premium;
      total_premiums += premium -> pool.creditPremium),
      lines 380-416 (ep mutation point 1, BEFORE fee fan-out: :380 seeds
      `intended_refund_after_cap = refund_lamports`; :385-395
      calls/premiums/breaches; :396-399 the PERIOD RESET — WP-04, lives in
      `recordCallAndCapAccrual` (PactRegistry, plan 02), NOT here; :400-408
      the cap clamp DECISION — WP-05, the return-value seam inside
      `recordCallAndCapAccrual`, do NOT add here; :409-414
      `current_period_refunds += intended_refund_after_cap` — WP-04 via
      `recordCallAndCapAccrual`),
      lines 418-453 (fee fan-out: fee = (premium as u128 * bps / 10_000) as
      u64; skip if 0; payout pool->recipient; total_fee_paid checked_add;
      then current_balance -= total_fee_paid if >0 -> pool.debitForFees),
      lines 455-502 (refund on breach: WP-04 = pool_balance >=
      intended_refund branch (the `else`, :470-501) only -> payout
      pool->agent; current_balance -= r; total_refunds += r ->
      pool.debitForRefund. The :462-469 `pool_balance < intended ->
      PoolDepleted` branch is WP-05 — leave the EMPTY if as the additive
      seam, do NOT set the status. ep.total_refunds += actual_refund at
      :492-500 is ep mutation point 2 — WP-04 via `recordRefundPaid`, AFTER
      the transfer, INSIDE the paid branch (only when actual_refund > 0)),
      lines 504-522 (CallRecord write -> EVM emit one
      IPactSettler.CallSettled, per GATE-A E4)
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md §E1
      (the SPLIT hooks `recordCallAndCapAccrual` + `recordRefundPaid`; the
      period reset is WP-04 IN `recordCallAndCapAccrual`), §D-SPLIT (the
      WP-05 clamp seams — the cap clamp is the return-value seam inside
      recordCallAndCapAccrual, the PoolDepleted is the empty settleBatch if;
      the DISTINCT intended/actual inputs — in WP-04 happy path
      payableRefund==actualRefund==ev.refund; "WP-04 settleBatch MUST
      preserve settle_batch.rs mutation ORDER exactly and leave clean
      insertion points so WP-05 layers the 3 clamps additively — do NOT
      build a happy-path-only shape WP-05 must rewrite"), §E3 (emit ONLY
      IPactSettler.CallSettled, no second PactEvents emission), §E4
      (event-as-truth). All RULED — no conditional branch.
    - .planning/phases/04-pactsettler-happy-path/04-CONTEXT.md corrected
      per-event step 7 (lines 240-252) — period reset in
      recordCallAndCapAccrual (WP-04); ONLY the cap clamp/PoolDepleted/pause
      are WP-05
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q1 steps
      6-10, §Q2 (fee math: uint64(uint256(ev.premium) * bps / 10_000), exact
      truncation; numeric checks 10_000@1000bps=1_000, 100_000@1000bps=10_000
      & @500bps=5_000, 300_000@1bps=30), §Q5 (E1 stat ordering table), §Q6
      (CallSettled field mapping), §"Common Pitfalls" 4/5/6 (use _ckAdd for
      totalFeePaid; guard `if (totalFeePaid > 0)` before debitForFees;
      iterate ep.feeRecipientCount not the hint)
    - .planning/phases/04-pactsettler-happy-path/04-02-SUMMARY.md (the exact
      SPLIT hook signatures: `recordCallAndCapAccrual(bytes16 slug, uint64
      premium, bool breach, uint64 intendedRefund) returns (uint64
      payableRefund)` and `recordRefundPaid(bytes16 slug, uint64
      actualRefund)`)
    - .planning/phases/04-pactsettler-happy-path/04-03-SUMMARY.md (the
      provisional CallSettled(Settled) emit seam this task REPLACES)
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 79-137
      (creditPremium/debitForFees/debitForRefund/payout/_ckAdd contracts)
  </read_first>
  <action>
    Implement per the GATE-A rulings (§E1, §D-SPLIT, §E3, §E4 — all RULED, no
    conditional branch). settle_batch.rs is the parity authority (D-LOCK-1):
    preserve its mutation ORDER exactly and leave clean ADDITIVE insertion
    points for the 3 WP-05 clamps. Do NOT build a happy-path-only shape WP-05
    must rewrite (explicit task constraint per 04-GATE-A-DECISIONS.md §D-SPLIT).
    STOP-AND-ASK on any genuine source ambiguity — do NOT guess.

    TASK CONSTRAINT — mutation order mirrors settle_batch.rs EXACTLY:
    cp credit -> ep point 1 (`recordCallAndCapAccrual`, pre-fan-out) -> fee
    fan-out -> refund transfer -> ep point 2 (`recordRefundPaid`,
    post-transfer). WP-05 inserts the cap clamp INSIDE
    `recordCallAndCapAccrual` (between the period reset and the accrual,
    adjusting the return) and the PoolDepleted branch in the empty settleBatch
    `if` — both ADDITIVE, ZERO WP-04 rewrite. Do NOT consolidate the two ep.*
    writes into one post-transfer call: that breaks source mutation ORDER and
    makes WP-05's cap clamp impossible to layer additively (the clamp reads
    `cap_remaining` AFTER the period reset and BEFORE
    `current_period_refunds += intended`, and the clamped amount must drive
    the refund transfer — all of which is mutation-point-1, BEFORE the
    transfer; the captain explicitly forbade a shape WP-05 must rewrite).

    RED step FIRST: add to PactSettler.t.sol a focused economic test
    `test_SingleEventDefaultTreasuryFanOut` (port of 05 "single event with
    default 10% Treasury fan-out", callId fill(1)): _register("helius")
    (default template Treasury 1000bps via setUp), _fundPool("helius",
    5_000_000), _provisionAgent(agent, 10_000_000, 10_000_000); event
    premium=10_000, refund=0, breach=false, feeRecipientCountHint=1,
    timestamp=now-1. `vm.expectEmit(true,true,true,true); emit
    IPactSettler.CallSettled(callId, SLUG_HELIUS, agent, 10_000, 0, 0,
    IPactSettler.SettlementStatus.Settled, false, 100, ts);
    vm.prank(settlerSigner); settler.settleBatch([ev]);` then assert:
      - `usdc.balanceOf(agent) == 10_000_000 - 10_000` (9_990_000)
      - `usdc.balanceOf(address(pool)) == 5_000_000 + 9_000`
      - `usdc.balanceOf(treasuryVault) == 1_000`
      - `pool.balanceOf(SLUG_HELIUS).currentBalance == 5_000_000 + 9_000`
      - `reg.getEndpoint(SLUG_HELIUS).totalCalls == 1` and `.totalPremiums == 10_000`
    Run `forge test --match-test test_SingleEventDefaultTreasuryFanOut` —
    confirm RED: fails because the success tail (credit/fees/refund/stats/real
    emit) is unimplemented / still the plan-03 provisional emit (treasury==0,
    pool==5_000_000, wrong CallSettled). Record the exact assertion failure.

    GREEN step: complete the settleBatch success path AFTER the plan-03
    premium-in success branch, in EXACT settle_batch.rs order. Use mutable
    locals shaped so WP-05 layers in additively (constraint: do NOT build a
    happy-path-only shape WP-05 must rewrite):
    1. Pool gross credit: `pool.creditPremium(ev.endpointSlug, ev.premium);`
       (settle_batch.rs:357-369).
    2. Seed the WP-05-ready refund local from `ev.refund`
       (settle_batch.rs:380): `uint64 intendedRefundAfterCap = ev.refund;`
       (the WP-05-ready local — the name is retained for parity-doc clarity;
       in WP-04 it is never reduced).
    3. ep mutation point 1 — BEFORE the fee fan-out (settle_batch.rs:385-414,
       exact source position): call the GATE-A E1 split hook 1 and capture
       its return:
       `uint64 payableRefund = IPactRegistry(registry).recordCallAndCapAccrual(
        ev.endpointSlug, ev.premium, ev.breach, intendedRefundAfterCap);`
       This hook (PactRegistry, plan 02) performs, in source order:
       totalCalls+=1; totalPremiums+=premium; if breach totalBreaches+=1; the
       WP-04 PERIOD RESET; [WP-05 cap-clamp seam — returns the clamped amount];
       currentPeriodRefunds += payableRefund. In the WP-04 happy path
       `payableRefund == ev.refund` (== intendedRefundAfterCap, no clamp).
       settleBatch does NOT do any ep.* mutation inline and does NOT contain
       `currentPeriodStart`. The clamp seam is INSIDE the hook (the return) —
       WP-05 changes only the hook body, not this call site.
    4. Fee fan-out (settle_batch.rs:418-453): `uint64 totalFeePaid = 0;`
       `for (uint8 j = 0; j < ep.feeRecipientCount; j++) { uint64 feeAmount =
       uint64(uint256(ev.premium) * ep.feeRecipients[j].bps / 10_000);
       if (feeAmount == 0) continue; pool.payout(ep.feeRecipients[j].destination,
       feeAmount); totalFeePaid = _ckAdd(totalFeePaid, feeAmount); }`
       then `if (totalFeePaid > 0) pool.debitForFees(ev.endpointSlug,
       totalFeePaid);`. Add a private `_ckAdd` mirroring PactPool._ckAdd
       (reverts ArithmeticOverflow). Iterate `ep.feeRecipientCount` (NOT the
       hint — pitfall 6).
    5. Refund on breach (settle_batch.rs:455-502, WP-04 = sufficient liquidity
       branch only — leave the WP-05 PoolDepleted seam as the EMPTY `if`):
       `uint64 actualRefund = 0;
        if (payableRefund > 0) {
            IPactPool.PoolState memory ps = pool.balanceOf(ev.endpointSlug);
            if (ps.currentBalance < payableRefund) {
                // settle_batch.rs:462-469 PoolDepleted DECISION — WP-05.
                // Clean additive seam: WP-05 sets status=PoolDepleted &
                // actualRefund=0 HERE. WP-04 leaves this `if` EMPTY and the
                // refund transfer in the `else` (the branch WP-05 turns into
                // the pool-sufficient else). Do NOT add a WP-05 status here.
            } else {
                pool.payout(ev.agent, payableRefund);
                pool.debitForRefund(ev.endpointSlug, payableRefund);
                actualRefund = payableRefund;
            }
        }`
       Do NOT implement the PoolDepleted status assignment or the exposure-cap
       clamp (WP-05). On the 9 happy-path tests the pool is always funded and
       within cap so the else branch always runs and actualRefund==ev.refund.
       (The refund branch keys off `payableRefund` — the value returned by
       recordCallAndCapAccrual — exactly as settle_batch.rs keys off
       `intended_refund_after_cap` after the ep block; in WP-04 they are
       equal, in WP-05 the clamp inside the hook reduces the returned value
       and the transfer follows it with zero settleBatch rewrite.)
    6. ep mutation point 2 — AFTER the refund transfer (settle_batch.rs:493-499,
       exact source position, INSIDE the paid branch only):
       `if (actualRefund > 0) IPactRegistry(registry).recordRefundPaid(
        ev.endpointSlug, actualRefund);`
       This is the SECOND, distinct ep.* mutation point: it does
       `ep.totalRefunds += actualRefund` (the ACTUAL paid amount). Skip the
       call when `actualRefund == 0`, mirroring that Solana only reaches
       :493-499 inside the paid `else` branch (pool funded). In the WP-04
       happy breach path actualRefund==payableRefund==ev.refund.
    7. Status = Settled; emit exactly ONE IPactSettler.CallSettled per call
       (replace the plan-03 provisional emit) per the GATE-A E3 ruling
       (typed-enum, NO second PactEvents emission): `emit CallSettled(
       ev.callId, ev.endpointSlug, ev.agent, ev.premium, ev.refund,
       actualRefund, SettlementStatus.Settled, ev.breach, ev.latencyMs,
       ev.timestamp);`. The DelegateFailed path (plan 03) still emits exactly
       one IPactSettler.CallSettled and `continue`s (and does NOT call
       recordCallAndCapAccrual or recordRefundPaid) — total one emit per call
       either way (SET-08).
    Run the matched test — confirm GREEN. Run FULL `forge test` — 70
    pre-existing + plan 02/03 tests still green.

    No emojis. Conventional file-scoped commits: src
    `feat(protocol-evm-v1): settleBatch credit/ep-point-1+2/fee-fanout/refund +
    one CallSettled per call (SET-05/06/07/08, WP-EVM-04)`; test file
    `test(protocol-evm-v1): port 05 default-10% fan-out economic assertions
    (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-test test_SingleEventDefaultTreasuryFanOut 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pool.creditPremium(ev.endpointSlug, ev.premium)" src/PactSettler.sol` succeeds
    - The TWO ep.* hooks are composed at the EXACT source positions (this is the parity-seam fix): `grep -q "uint64 payableRefund = IPactRegistry(registry).recordCallAndCapAccrual(ev.endpointSlug, ev.premium, ev.breach, intendedRefundAfterCap)" src/PactSettler.sol` succeeds AND `grep -q "recordRefundPaid(ev.endpointSlug, actualRefund)" src/PactSettler.sol` succeeds; there is NO single combined `recordSettlement(...)` call (`grep -q "recordSettlement" src/PactSettler.sol` MUST fail)
    - ORDER verified by `grep -n`: the `recordCallAndCapAccrual` call line number is BEFORE the fee-fan-out loop (`for (uint8 j = 0; j < ep.feeRecipientCount`), and the `recordRefundPaid` call line number is AFTER the `pool.payout(ev.agent, payableRefund)` / `pool.debitForRefund(ev.endpointSlug, payableRefund)` lines (mutation order: cp credit -> ep point 1 -> fee fan-out -> refund transfer -> ep point 2)
    - `recordRefundPaid` is guarded by `if (actualRefund > 0)` (mirrors Solana only hitting :493-499 in the paid branch): `grep -q "if (actualRefund > 0) IPactRegistry(registry).recordRefundPaid" src/PactSettler.sol` succeeds (or the equivalent guarded form)
    - The WP-05 refund seam keys off the hook return: `grep -q "uint64 intendedRefundAfterCap = ev.refund" src/PactSettler.sol` succeeds and the refund branch is `if (payableRefund > 0)` with an EMPTY `ps.currentBalance < payableRefund` `if` (the PoolDepleted seam — no status set) and the transfer in the `else` (clean additive insertion point — do NOT build a happy-path-only shape WP-05 must rewrite)
    - Fee math is `uint64(uint256(ev.premium) * ep.feeRecipients[j].bps / 10_000)` with a `if (feeAmount == 0) continue;` skip: `grep -q "uint256(ev.premium)" src/PactSettler.sol` and `grep -q "feeAmount == 0" src/PactSettler.sol` succeed
    - Fee loop iterates `ep.feeRecipientCount` (not `ev.feeRecipientCountHint`): `grep -q "j < ep.feeRecipientCount" src/PactSettler.sol` succeeds
    - `if (totalFeePaid > 0)` guards `pool.debitForFees`: `grep -q "totalFeePaid > 0" src/PactSettler.sol` succeeds
    - Refund branch calls `pool.payout(ev.agent, payableRefund)` then `pool.debitForRefund(ev.endpointSlug, payableRefund)`; the `ps.currentBalance < payableRefund` branch is an empty WP-05 seam (no status set)
    - WP-05 NOT leaked into PactSettler.sol (greps SCOPED to src/PactSettler.sol — the period reset/accrual legitimately live in PactRegistry.sol per GATE-A D-SPLIT and are NOT flagged here): `grep -q "SettlementStatus.PoolDepleted\|SettlementStatus.ExposureCapClamped" src/PactSettler.sol` MUST fail; `grep -q "currentPeriodStart" src/PactSettler.sol` MUST fail (the period reset is in recordCallAndCapAccrual, not inline in the settler)
    - Exactly one `emit ...CallSettled` per call: the success path and the DelegateFailed path each emit exactly one IPactSettler.CallSettled (no second PactEvents emission per GATE-A E3); no double emit on any path
    - `forge test --match-test test_SingleEventDefaultTreasuryFanOut` exits 0 with EXACTLY: agent==9_990_000, pool USDC==5_009_000, treasury==1_000, pool.balanceOf.currentBalance==5_009_000, getEndpoint.totalCalls==1, .totalPremiums==10_000, CallSettled(status=Settled, refund=0, actualRefund=0)
    - `forge test 2>&1 | grep -c FAIL` == 0; plan 02/03 tests + 70 pre-existing still green
    - RED observed and recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>settleBatch success path complete in settle_batch.rs order with the TWO ep.* hooks at the EXACT source positions (cp credit -> ep point 1 `recordCallAndCapAccrual` pre-fan-out, capturing payableRefund -> fee fan-out floor-div+residual -> breach refund transfer -> ep point 2 `recordRefundPaid` post-transfer, guarded by actualRefund>0 -> one IPactSettler.CallSettled); plan-03 provisional emit replaced; period reset stays in recordCallAndCapAccrual (NOT inline); clean WP-05 seams (the cap-clamp return-value seam inside recordCallAndCapAccrual + the empty PoolDepleted `if` + the payableRefund-keyed refund branch) left, not a shape WP-05 must rewrite; default-10% economic test green with exact numbers; regression gate held; RED-before-GREEN recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — port the remaining 5 happy-path tests (85/10/5, breach, mixed-3, Settled-status); full 9 green</name>
  <files>packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol, packages/program-evm/protocol-evm-v1/src/PactSettler.sol</files>
  <read_first>
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/tests/05-settle-batch.test.ts
      lines 98-152 (test 2: 85/10/5, callId fill(2), premium=100_000,
      treasury==10_000, aff==5_000, pool==5_000_000+85_000, agent==
      10_000_000-100_000), lines 154-198 (test 3: breach, callId fill(3),
      premium=1_000 refund=50_000 latency 6000, agent==10_000_000-1_000+50_000,
      pool==5_000_000+1_000-100-50_000==4_950_900, treasury==100), lines
      200-282 (test 4: mixed 3 endpoints, callIds fill(11/12/13), premiums
      100_000/200_000/300_000, agent==30_000_000-600_000, ep1
      currentBalance==1_000_000+90_000, ep2==1_000_000+180_000, ep3==
      1_000_000+299_970, treasury==20_030, aff2==10_000), lines 674-706
      (test 9: happy path Settled, callId fill(80), premium=1_000 refund=
      50_000 breach=true, CallSettled status=Settled refund=50_000
      actualRefund=50_000)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q8 per-test
      mappings (Tests 2,3,4,9 exact setups + Foundry numeric assertions),
      §Q9 (PORT vs N-A: byte-offset CallRecord reads -> CallSettled fields)
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md §E4
      (RULED: port cr[2]->CallSettled.status, readU64(cr,80)->CallSettled.refund,
      readU64(cr,88)->CallSettled.actualRefund via vm.expectEmit; one
      IPactSettler.CallSettled per call incl. the DelegateFailed path) and §E3
      (single emission, byte-ABI-identical to PactEvents.CallSettled)
    - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol (the shared
      harness + _registerWithRecipients for the multi-recipient endpoints)
  </read_first>
  <action>
    Implement per the GATE-A rulings on E3/E4 (RULED). RED step FIRST: add the
    remaining 4 ported tests (Task 1 already added test 1; tests 5-8 = the 4
    reject/DelegateFailed tests landed in plan 03; this task adds tests
    2,3,4,9). Numbers are COPIED VERBATIM from 05-settle-batch.test.ts — no
    recomputation:
    - `test_SingleEventExplicit85_10_5Split` (test 2, callId fill(2)):
      _registerWithRecipients("jupiter", [Treasury 1000bps to treasuryVault,
      AffiliateAta 500bps to affAddr], 2); _fundPool 5_000_000;
      _provisionAgent(agent, 10_000_000, 10_000_000); premium=100_000,
      refund=0, breach=false, hint=2. Assert: treasury==10_000, affAddr==5_000,
      pool USDC==5_000_000+85_000, agent==10_000_000-100_000.
    - `test_BreachRefundsAgentFromPool` (test 3, callId fill(3)):
      _register("helius") default; _fundPool 5_000_000; premium=1_000,
      refund=50_000, breach=true, latencyMs=6000, hint=1. Assert:
      agent==10_000_000-1_000+50_000, pool USDC==5_000_000+1_000-100-50_000
      (==4_950_900), treasury==100, CallSettled(status=Settled, refund=50_000,
      actualRefund=50_000). Also assert
      `reg.getEndpoint(SLUG_HELIUS).currentPeriodRefunds == 50_000` and
      `.totalRefunds == 50_000` (recordCallAndCapAccrual returned
      payableRefund==50_000 and accrued currentPeriodRefunds within the
      period BEFORE the fee fan-out; recordRefundPaid added totalRefunds==
      50_000 AFTER the transfer — confirms the two-mutation-point wiring at
      the correct source positions + the WP-04 period accounting).
    - `test_MixedBatch3Endpoints` (test 4, callIds fill(11/12/13)): register
      "ep1" default; "ep2" [Treasury 500bps, Aff 500bps to aff2Addr]; "ep3"
      [Treasury 1bps]; _fundPool each 1_000_000; _provisionAgent(agent,
      30_000_000, 30_000_000); ONE settleBatch with 3 events premiums
      100_000/200_000/300_000, all non-breach, timestamps now-3/now-2/now-1.
      Assert: agent==30_000_000-600_000, pool.balanceOf("ep1").currentBalance
      ==1_000_000+90_000, "ep2"==1_000_000+180_000, "ep3"==1_000_000+299_970,
      treasury==20_030, aff2Addr==10_000.
    - `test_HappyPathSettledStatusEvent` (test 9, callId fill(80)):
      _register("helius"); _fundPool 5_000_000; premium=1_000, refund=50_000,
      breach=true, latencyMs=6000, hint=1. vm.expectEmit the IPactSettler
      CallSettled with status==Settled, refund==50_000, actualRefund==50_000
      (per GATE-A E4: replaces cr[2]==0, readU64(cr,80)==50_000,
      readU64(cr,88)==50_000).
    Run `forge test --match-contract PactSettlerTest` — confirm RED for the 4
    new tests (record which assertion fails for each). They should fail only
    on the new assertions, not compile.

    GREEN step: these tests should pass against the Task 1 implementation with
    NO new src logic if the fee-math, residual, refund, the two ep.* hooks at
    the correct source positions and single-emit are bit-identical. If any
    fails, the defect is in PactSettler.sol parity — fix the SRC to match
    settle_batch.rs (do NOT bend the test numbers; the 05 assertions are the
    oracle). Re-run until all 4 green.
    Add an ABI-identity assertion (per GATE-A E3 condition): a test asserting
    `IPactSettler.CallSettled` and `PactEvents.CallSettled` have the SAME
    topic0 — e.g. assert
    `keccak256("CallSettled(bytes16,bytes16,address,uint64,uint64,uint64,uint8,bool,uint32,uint64)")`
    equals the selector of each declaration (the SettlementStatus enum encodes
    as uint8 in the signature), proving "alias" is literally true and there is
    a single emission per call.
    Then run the FULL `forge test`: confirm ALL 9 ported happy-path tests
    green (tests 1-4 + 9 here/Task1, tests 5-8 from plan 03) AND all 70
    pre-existing forge tests green (regression gate, success criterion 8).

    Mark N-A-ON-EVM (one line each, for the WP-06 parity matrix, per D-LOCK-5
    / RESEARCH §Q9): deriveCallRecord PDA, getAccountData CallRecord existence,
    cr[2]/readU64 byte reads, is_data_empty dedup, SPL delegate pre-flight,
    SettlementAuthority PDA signer, pool_vault ATA binding,
    NotEnoughAccountKeys/InvalidSeeds. Record these in the SUMMARY.

    No emojis. Conventional file-scoped commit:
    `test(protocol-evm-v1): port remaining 05 happy-path scenarios; 9/9 green
    (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | grep -E "Suite result:|FAIL" | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-test test_SingleEventExplicit85_10_5Split` exits 0: treasury==10_000, affAddr==5_000, pool USDC==5_085_000, agent==9_900_000
    - `forge test --match-test test_BreachRefundsAgentFromPool` exits 0: agent==10_049_000, pool USDC==4_950_900, treasury==100, CallSettled(Settled, refund=50_000, actualRefund=50_000), and `reg.getEndpoint(SLUG_HELIUS).currentPeriodRefunds==50_000` and `.totalRefunds==50_000`
    - `forge test --match-test test_MixedBatch3Endpoints` exits 0: agent==29_400_000, pool.balanceOf("ep1").currentBalance==1_090_000, "ep2"==1_180_000, "ep3"==1_299_970, treasury==20_030, aff2Addr==10_000
    - `forge test --match-test test_HappyPathSettledStatusEvent` exits 0: CallSettled status==Settled(0), refund==50_000, actualRefund==50_000
    - The E3 ABI-identity test passes: IPactSettler.CallSettled and PactEvents.CallSettled share the same topic0 (single emission per call; "alias" literally true)
    - All 9 ported happy-path tests present and green: `forge test --match-contract PactSettlerTest 2>&1 | grep -c "FAIL"` == 0 and the 9 named tests (DefaultTreasuryFanOut, Explicit85_10_5Split, BreachRefundsAgentFromPool, MixedBatch3Endpoints, RevokeMarksDelegateFailedAndContinues, MinPremiumEdgeRejected, DuplicateCallIdRejected, UnauthorizedSettlerRejected, HappyPathSettledStatusEvent) all pass
    - FULL `forge test 2>&1 | grep -c FAIL` == 0; the 70 pre-existing tests still green (regression gate)
    - Test numbers were copied verbatim from 05-settle-batch.test.ts (no recomputed/changed expected values)
    - N-A-ON-EVM list recorded in SUMMARY for the WP-06 parity matrix
    - RED observed and recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>All 9 ported happy-path tests green with bit-identical 05 numeric assertions + the E3 ABI-identity assertion; the breach test additionally confirms currentPeriodRefunds==50_000 (ep point 1, pre-fan-out) and totalRefunds==50_000 (ep point 2, post-transfer); any parity defect fixed in src (not by bending test numbers); 70-test regression gate held; N-A-ON-EVM list recorded; RED-before-GREEN recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GATE B — final parity gate; write report file + cockpit notice and STOP (no push, no PR #204 comment)</name>
  <files>packages/program-evm/protocol-evm-v1/src/PactSettler.sol</files>
  <what-built>
    Full WP-EVM-04 PactSettler happy path: AccessControl SETTLER_ROLE (E2),
    the TWO SETTLER_ROLE-gated endpoint-stats hooks on PactRegistry
    (`recordCallAndCapAccrual` + `recordRefundPaid`) mirroring settle_batch.rs's
    two ep.* mutation points INCLUDING the WP-04 period reset (E1 + D-SPLIT
    refinement), per-event guards + dedup-before-premium-in + premium-in
    try/catch -> DelegateFailed, pool credit + the two ep.* hooks composed at
    their EXACT source positions (recordCallAndCapAccrual BEFORE the fee
    fan-out, recordRefundPaid AFTER the refund transfer) + fee fan-out
    floor-div + residual + breach refund, one IPactSettler.CallSettled per
    call (E3/E4), and the 9 ported happy-path tests of 05-settle-batch.test.ts
    — all green with the 70 pre-existing forge tests still green. WP-05 scope
    (PoolDepleted / ExposureCapClamped clamp DECISIONS, protocol/endpoint
    pause, BatchTooLarge, ports of 06/07/09/10 + the 4 clamp/killswitch tests
    of 05) explicitly NOT implemented; clean additive seams left (the cap
    clamp is the return-value seam inside recordCallAndCapAccrual; the
    PoolDepleted is the empty settleBatch `if`).
  </what-built>
  <read_first>
    - .planning/STATE.md (GATE B cadence: "after impl + forge build && forge
      test green, report, WAIT, no push / no PR comment until approved")
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
      "Reporting protocol change" + "Proceed directive" (write the GATE-B
      report to .planning/phases/04-pactsettler-happy-path/04-REPORT-gateB.md
      AND send a short `cockpit runtime send pact-network` notice; the relay
      is unreliable — the captain pulls reports from that file)
    - .planning/phases/04-pactsettler-happy-path/04-CONTEXT.md D-LOCK-6 (NO
      push / NO PR #204 comment until captain approves; working tree clean;
      contamination check)
    - .planning/phases/04-pactsettler-happy-path/04-04-SUMMARY.md (this plan's
      summary so far) and 04-02/03 SUMMARYs (E1/E2 implemented, seams)
  </read_first>
  <action>
    This is a verification + reporting gate — write NO production code. Run
    the GATE-B checks in order and STOP:
    1. From `packages/program-evm/protocol-evm-v1/`: run `forge build` —
       MUST be clean (exit 0).
    2. Run `forge test` — MUST show zero FAIL; report the total test count
       and confirm the 9 WP-04 ported tests + the 70 pre-existing are green.
    3. Run `git status --porcelain` — MUST show only the expected modified
       files (PactSettler.sol, PactRegistry.sol, IPactRegistry.sol,
       Deployment.t.sol, PactSettler.t.sol, the `.planning/` docs) plus the
       expected untracked `?? .claude/pr-reviews/`. If `CLAUDE.md` shows ` M`
       or `.claude/skills/pact/` appears -> STOP and report contamination
       (D-LOCK-6 / RESEARCH pitfall 7); do NOT proceed.
    4. Confirm NO WP-05 code leaked into PactSettler.sol (greps SCOPED to
       src/PactSettler.sol only — the period reset/accrual legitimately live
       in PactRegistry.sol per the GATE-A D-SPLIT ruling and MUST NOT be
       flagged):
       `grep -rn "PoolDepleted\|ExposureCapClamped\|BatchTooLarge\|currentPeriodStart\|protocolPaused" src/PactSettler.sol`
       returns nothing. (Do NOT grep PactRegistry.sol for currentPeriodStart —
       the WP-04 period reset is there by design, inside
       recordCallAndCapAccrual.) Also confirm the two ep.* hooks are composed
       at the correct positions in PactSettler.sol:
       `recordCallAndCapAccrual` appears BEFORE the fee-fan-out loop and
       `recordRefundPaid` appears AFTER the pool.payout/debitForRefund (the
       parity-seam fix); no single combined `recordSettlement` call.
    5. Write the GATE-B report to
       `.planning/phases/04-pactsettler-happy-path/04-REPORT-gateB.md`:
       the 9 ported test names + pass status, the 70-test regression
       confirmation, the E1-E4 rulings as implemented per
       04-GATE-A-DECISIONS.md (E1 OPTION (a) — the TWO SPLIT hooks
       recordCallAndCapAccrual + recordRefundPaid composed at the EXACT
       settle_batch.rs mutation positions + the period reset in
       recordCallAndCapAccrual; E2 3-arg ctor + two-layer grant; E3 single
       IPactSettler.CallSettled; E4 dedup-before-premium-in), the WP-05 clean
       additive seams left (the cap-clamp return-value seam inside
       recordCallAndCapAccrual + the empty PoolDepleted `if` in settleBatch),
       the N-A-ON-EVM list for the WP-06 parity matrix, and the
       disabled-gates note (Nyquist/security threat model intentionally
       DISABLED per the captain config flag / D-LOCK-2 — WP-05 enables a
       settlement threat-model pass, WP-06 fuzz/gas covers statistical
       adequacy).
    6. Send a short `cockpit runtime send pact-network` notice pointing the
       captain at the report file (one line: GATE B reached, report at
       04-REPORT-gateB.md, forge build+test green, awaiting approval).
    7. STOP. Do NOT `git push`. Do NOT post a PR #204 comment. WAIT for the
       captain's GATE-B approval. (GATE-B approval + push/PR are out of
       plan-execution scope.)
  </action>
  <how-to-verify>
    1. From `packages/program-evm/protocol-evm-v1/`: run
       `forge build` — MUST be clean (exit 0).
    2. Run `forge test` — MUST show zero FAIL; report total test count and
       confirm the 9 WP-04 ported tests + 70 pre-existing are green.
    3. Run `git status --porcelain` — MUST show only the expected modified
       files plus the expected untracked `?? .claude/pr-reviews/`. If
       `CLAUDE.md` shows ` M` or `.claude/skills/pact/` appears -> STOP and
       report contamination (D-LOCK-6 / RESEARCH pitfall 7), do NOT proceed.
    4. Confirm NO WP-05 code leaked into src/PactSettler.sol (grep from step 4
       of the action, scoped to PactSettler.sol, returns nothing — the period
       reset in PactRegistry.sol is by design, not a leak) AND the two ep.*
       hooks are at the correct source positions (recordCallAndCapAccrual
       BEFORE the fee-fan-out loop, recordRefundPaid AFTER the
       payout/debitForRefund; no combined recordSettlement call).
    5. `04-REPORT-gateB.md` written + the `cockpit runtime send pact-network`
       notice sent; execution STOPPED with no push, no PR comment.
  </how-to-verify>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test 2>&1 | grep -cE "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
    - `forge test 2>&1 | grep -c FAIL` == 0
    - All 9 ported WP-04 happy-path tests pass; the 70 pre-existing tests pass
    - `git status --porcelain` shows only expected files + `?? .claude/pr-reviews/`; no `CLAUDE.md` ` M`, no `.claude/skills/pact/`
    - `grep -rn "PoolDepleted\|ExposureCapClamped\|BatchTooLarge\|currentPeriodStart" src/PactSettler.sol` returns nothing (WP-05 not leaked INTO THE SETTLER; the period reset in PactRegistry.sol recordCallAndCapAccrual is intentional per GATE-A D-SPLIT and is NOT grepped here)
    - The parity-seam fix is in place: in src/PactSettler.sol the `recordCallAndCapAccrual` call appears BEFORE the `j < ep.feeRecipientCount` fee-fan-out loop and the `recordRefundPaid` call appears AFTER the `pool.payout(ev.agent`/`pool.debitForRefund` lines; `grep -q "recordSettlement" src/PactSettler.sol` MUST fail (no single combined call)
    - `.planning/phases/04-pactsettler-happy-path/04-REPORT-gateB.md` exists with the 9 test names, 70-test regression confirmation, E1-E4 as implemented (incl. the two SPLIT hooks at the exact source positions), WP-05 seams, N-A-ON-EVM list, and the disabled-gates note
    - A `cockpit runtime send pact-network` notice was sent pointing at 04-REPORT-gateB.md
    - Execution STOPS with no `git push` and no PR #204 comment performed
  </acceptance_criteria>
  <resume-signal>Captain pulls 04-REPORT-gateB.md, reviews, and approves (or requests changes). Push and PR #204 comment happen only AFTER captain approval, outside this plan's execution.</resume-signal>
  <done>GATE-B report written to 04-REPORT-gateB.md + cockpit notice sent; forge build + forge test green; working tree clean (no contamination); WP-05 not leaked into PactSettler.sol (period reset legitimately in PactRegistry.sol recordCallAndCapAccrual); the two ep.* hooks composed at the exact source positions (recordCallAndCapAccrual pre-fan-out, recordRefundPaid post-transfer); execution stopped pending captain approval — no push, no PR comment.</done>
</task>

</tasks>

<verification>
- `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
- `forge test 2>&1 | grep -c FAIL` == 0 (9 ported WP-04 + 70 pre-existing green)
- settleBatch composes WP-03 pool hooks + the GATE-A E1 SPLIT hooks at the EXACT settle_batch.rs mutation positions: cp credit -> recordCallAndCapAccrual (ep point 1, BEFORE fee fan-out) -> fee fan-out -> refund transfer -> recordRefundPaid (ep point 2, AFTER the transfer); fee math is uint256-intermediate floor-div; one IPactSettler.CallSettled per call
- No WP-05 code in src/PactSettler.sol (PoolDepleted/ExposureCapClamped/BatchTooLarge/inline period reset/pause); WP-05 clamp seams left clean (cap-clamp = the payableRefund return seam inside recordCallAndCapAccrual; PoolDepleted = the empty settleBatch `if`); the WP-04 period reset lives in recordCallAndCapAccrual by design
- Every code task observed RED before GREEN
- GATE-B reached: report written to 04-REPORT-gateB.md + cockpit notice sent, execution stopped, no push/PR
</verification>

<success_criteria>
- SET-05: pool gross credit + floor-div fee fan-out + residual; 05 default-10%, 85/10/5, mixed-3-endpoint balances bit-identical
- SET-06: breach refund pool->agent full intended, status Settled, actualRefund==intended
- SET-07: pool + endpoint stats identical arithmetic AND mutation ORDER to settle_batch.rs via the GATE-A E1 SPLIT hooks composed at the EXACT source positions (recordCallAndCapAccrual ep point 1 :385-414 BEFORE fee fan-out, recordRefundPaid ep point 2 :493-499 AFTER the transfer); period reset is WP-04 in recordCallAndCapAccrual; the payableRefund return + the empty PoolDepleted `if` leave the WP-05 clamp seams additive (zero WP-04 rewrite)
- SET-08: exactly one IPactSettler.CallSettled per call with all fields (per GATE-A E3/E4); ABI-identity with PactEvents.CallSettled asserted
- All 9 ported happy-path tests green; 70 pre-existing green; build clean
- GATE B: report written to 04-REPORT-gateB.md + cockpit notice sent, stopped, no push, no PR #204 comment
</success_criteria>

<output>
After completion, create `.planning/phases/04-pactsettler-happy-path/04-04-SUMMARY.md`
recording: RED observed per TDD task before GREEN, the 9 ported test results,
the 70-test regression confirmation, E1-E4 as implemented per
04-GATE-A-DECISIONS.md (including the TWO SPLIT E1 hooks
recordCallAndCapAccrual + recordRefundPaid composed at the EXACT
settle_batch.rs mutation positions — recordCallAndCapAccrual BEFORE the fee
fan-out, recordRefundPaid AFTER the refund transfer — the period reset inside
recordCallAndCapAccrual + the distinct payableRefund/actualRefund wiring + the
clean WP-05 seams: the cap-clamp return-value seam inside
recordCallAndCapAccrual and the empty PoolDepleted `if` in settleBatch), the
N-A-ON-EVM list for the WP-06 parity matrix, the disabled-gates note, and
explicit confirmation that execution STOPPED at GATE B with the report written
to 04-REPORT-gateB.md, a cockpit notice sent, and no push and no PR #204
comment.
</output>
