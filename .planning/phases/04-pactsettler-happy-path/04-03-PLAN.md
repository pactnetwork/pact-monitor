---
phase: 04-pactsettler-happy-path
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - packages/program-evm/protocol-evm-v1/src/PactSettler.sol
  - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol
autonomous: true
requirements: [SET-01, SET-02, SET-03, SET-04]
must_haves:
  truths:
    - "settleBatch reverts for an unauthorized caller (SET-01)"
    - "Per event: premium < MIN_PREMIUM reverts PremiumTooSmall; timestamp > now reverts InvalidTimestamp; feeRecipientCountHint != stored count reverts RecipientCoverageMismatch (SET-03)"
    - "Repeated callId reverts DuplicateCallId via mapping(bytes16=>bool); the mapping is set BEFORE the premium-in attempt so DelegateFailed events also dedup (SET-04)"
    - "Premium-in via raw IERC20.transferFrom in try/catch; on failure the event is marked DelegateFailed, no funds move, the batch continues (SET-02)"
  artifacts:
    - path: "packages/program-evm/protocol-evm-v1/src/PactSettler.sol"
      provides: "settleBatch per-event guards + dedup + premium-in try/catch -> DelegateFailed"
      contains: "try IERC20"
    - path: "packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol"
      provides: "4 ported guard/dedup/auth/DelegateFailed tests"
      contains: "test_UnauthorizedSettlerRejected"
  key_links:
    - from: "settleBatch dedup check"
      to: "_settledCallIds mapping set"
      via: "set true immediately after dedup check, BEFORE premium-in try/catch"
      pattern: "_settledCallIds\\[.*\\] = true"
    - from: "settleBatch premium-in"
      to: "DelegateFailed status + emit + continue"
      via: "raw IERC20.transferFrom in try/catch (NOT SafeERC20)"
      pattern: "try IERC20\\(.*\\)\\.transferFrom\\("
---

<objective>
Port the per-event PRE-ECONOMIC half of settle_batch.rs into
PactSettler.settleBatch: the settler-role gate (SET-01), the three per-event
hard-revert guards (SET-03), the mapping(bytes16=>bool) dedup (SET-04), and
the premium-in try/catch -> DelegateFailed-and-continue path (SET-02). The
pool-credit / fee fan-out / refund / CallSettled-emit economic half is plan 04.

Purpose: These four behaviors are the parity-critical control flow of the
per-event loop (settle_batch.rs:95-355). Strict TDD: each behavior gets a
failing ported test (RED, confirm exact revert/state), then the minimal
implementation (GREEN).

Output: settleBatch with the guarded, deduped, DelegateFailed-recoverable
per-event prologue and 4 ported tests green; 70 pre-existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pactsettler-happy-path/04-CONTEXT.md
@.planning/phases/04-pactsettler-happy-path/04-RESEARCH.md
@.planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md
@.planning/phases/04-pactsettler-happy-path/04-02-SUMMARY.md
@packages/program-evm/protocol-evm-v1/src/PactSettler.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactSettler.sol
@packages/program-evm/protocol-evm-v1/src/interfaces/IPactRegistry.sol
@packages/program-evm/protocol-evm-v1/src/errors/PactErrors.sol
@packages/program-evm/protocol-evm-v1/src/ArcConfig.sol
@packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol

<interfaces>
SettlementEvent (IPactSettler.sol) — already in calldata, no decode needed:
```solidity
struct SettlementEvent {
  bytes16 callId; address agent; bytes16 endpointSlug;
  uint64 premium; uint64 refund; uint32 latencyMs;
  bool breach; uint8 feeRecipientCountHint; uint64 timestamp;
}
enum SettlementStatus { Settled, DelegateFailed, PoolDepleted, ExposureCapClamped }
```

Existing custom errors (PactErrors.sol — DO NOT redeclare):
InvalidTimestamp, PremiumTooSmall, FeeRecipientArrayTooLong,
RecipientCoverageMismatch, DuplicateCallId, EndpointNotFound, ArithmeticOverflow

ArcConfig constants: MIN_PREMIUM = 100; MAX_FEE_RECIPIENTS = 8.

IPactRegistry.getEndpoint(slug) returns EndpointConfig memory with
`feeRecipientCount` (uint8) — compared against ev.feeRecipientCountHint.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — SET-01 unauthorized settler + SET-03 per-event guards</name>
  <read_first>
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs
      lines 95-97 (settler signer check -> EVM onlyRole, SET-01),
      lines 132-138 (event_count decode — EVM: events.length, batch-size guard
      is WP-05 do NOT add it), lines 144-166 (per-event guards: timestamp>now
      InvalidTimestamp @158-160, premium<MIN_PREMIUM PremiumTooSmall @161-163,
      fee_count_hint>MAX_FEE_RECIPIENTS FeeRecipientArrayTooLong @164-166),
      lines 200-221 (endpoint snapshot; ep.fee_recipient_count != hint ->
      RecipientCoverageMismatch @213-215; endpoint-paused @209-211 is WP-05 do
      NOT add it)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q1 steps
      "Pre-loop" + "Step 2" + "Step 3", §Q8 Test 6 (min-premium edge, callId
      fill(30), premium=50) and Test 8 (unauthorized settler, callId fill(50))
    - packages/program-evm/protocol-evm-v1/test/PactPool.t.sol lines 174-182,
      264-272 (the AccessControlUnauthorizedAccount expectRevert pattern to
      mirror for SET-01)
    - packages/program-evm/protocol-evm-v1/test/PactSettler.t.sol (the shared
      harness from plan 02 Task 3: setUp, _register, _fundPool,
      _provisionAgent, event builder)
  </read_first>
  <action>
    RED step FIRST: add to PactSettler.t.sol:
    - `test_UnauthorizedSettlerRejected`: _register("helius"), _fundPool 5_000_000,
      _provisionAgent; build one event (callId bytes16(uint128(0x50...)) /
      `fill(50)` equivalent, premium=1_000, breach=false, timestamp=now-1,
      feeRecipientCountHint=1); call `settler.settleBatch(events)` with
      `vm.prank(makeAddr("fake"))`; `vm.expectRevert(
      abi.encodeWithSelector(IAccessControl.AccessControlUnauthorizedAccount.selector,
      makeAddr("fake"), settler.SETTLER_ROLE()))`.
    - `test_MinPremiumEdgeRejected`: same setup, premium=50 (< ArcConfig.MIN_PREMIUM
      == 100), callId fill(30); `vm.prank(settlerSigner);
      vm.expectRevert(PremiumTooSmall.selector); settler.settleBatch(events)`.
    - `test_InvalidTimestampRejected`: premium=1_000, timestamp =
      uint64(block.timestamp)+1 (> now); `vm.expectRevert(InvalidTimestamp.selector)`.
    - `test_RecipientCoverageMismatchRejected`: _register default (stored
      feeRecipientCount==1), event feeRecipientCountHint=2;
      `vm.expectRevert(RecipientCoverageMismatch.selector)`.
    Run `forge test --match-contract PactSettlerTest` — confirm RED: all four
    fail because settleBatch still reverts NOT_IMPLEMENTED (string revert, not
    the expected selectors / not AccessControl). Record the exact failure.

    GREEN step: implement the per-event prologue in settleBatch (keep
    onlyRole(SETTLER_ROLE) from plan 02 — that satisfies SET-01 with no extra
    code; the test asserts the OZ revert). Loop
    `for (uint256 i = 0; i < events.length; i++)` with
    `SettlementEvent calldata ev = events[i];`. Order EXACTLY per
    settle_batch.rs:158-215:
      1. `if (ev.timestamp > uint64(block.timestamp)) revert InvalidTimestamp();`
      2. `if (ev.premium < ArcConfig.MIN_PREMIUM) revert PremiumTooSmall();`
      3. `if (ev.feeRecipientCountHint > ArcConfig.MAX_FEE_RECIPIENTS) revert
         FeeRecipientArrayTooLong();`
      4. `IPactRegistry.EndpointConfig memory ep =
         IPactRegistry(registry).getEndpoint(ev.endpointSlug);` then
         `if (ep.feeRecipientCount != ev.feeRecipientCountHint) revert
         RecipientCoverageMismatch();`
    Do NOT add the endpoint-paused or batch-size guards (WP-05). Leave the
    rest of the loop body unimplemented for now (a trailing `revert(...)` is
    fine — plan 03 Task 2 + plan 04 fill it; the four guard tests assert the
    guards fire BEFORE any unimplemented tail). These guards are HARD reverts
    on the whole tx (not per-event continues) — settle_batch.rs:158-166 returns
    Err, aborting the batch.
    Run the four matched tests — confirm GREEN. Run FULL `forge test` — 70
    pre-existing tests still green.

    No emojis. Conventional file-scoped commits: src change
    `feat(protocol-evm-v1): settleBatch per-event guards + settler gate
    (SET-01/03, WP-EVM-04)`; test file
    `test(protocol-evm-v1): port 05 unauthorized/min-premium/guard rejects
    (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-test "test_UnauthorizedSettlerRejected|test_MinPremiumEdgeRejected|test_InvalidTimestampRejected|test_RecipientCoverageMismatchRejected" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-test test_UnauthorizedSettlerRejected` exits 0; the revert is `AccessControlUnauthorizedAccount(fake, SETTLER_ROLE)` (mirrors PactPool.t.sol test_Hooks_RejectNonSettler)
    - `forge test --match-test test_MinPremiumEdgeRejected` exits 0; revert selector is `PremiumTooSmall` for premium=50 < 100
    - `forge test --match-test test_InvalidTimestampRejected` exits 0; revert selector is `InvalidTimestamp` for timestamp > block.timestamp
    - `forge test --match-test test_RecipientCoverageMismatchRejected` exits 0; revert selector is `RecipientCoverageMismatch` for hint != stored feeRecipientCount
    - Guard order in src matches settle_batch.rs:158-215: `grep -n "InvalidTimestamp\|PremiumTooSmall\|FeeRecipientArrayTooLong\|RecipientCoverageMismatch" src/PactSettler.sol` shows timestamp before premium before fee-count-hint before coverage-mismatch
    - No endpoint-paused / batch-size guard added: `grep -q "EndpointPaused\|BatchTooLarge" src/PactSettler.sol` MUST fail (WP-05 scope)
    - `forge test 2>&1 | grep -c FAIL` == 0; 70 pre-existing tests still green
    - RED observed and recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>settleBatch enforces the settler-role gate (SET-01) and the three per-event hard-revert guards (SET-03) in settle_batch.rs order; 4 ported reject tests green; no WP-05 guards added; regression gate held; RED-before-GREEN recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — SET-04 dedup mapping + SET-02 premium-in try/catch -> DelegateFailed</name>
  <read_first>
    - packages/program/programs-pinocchio/pact-network-v1-pinocchio/src/instructions/settle_batch.rs
      lines 194-196 (dedup: non-empty CallRecord -> DuplicateCallId),
      lines 243-262 (CallRecord PDA ALLOCATED before the delegate pre-flight —
      this is why the EVM dedup mapping must be SET before premium-in),
      lines 295-355 (premium-in pre-flight + on `!premium_in_ok`: status =
      DelegateFailed, write record, `continue` — NO funds move)
    - .planning/phases/04-pactsettler-happy-path/04-RESEARCH.md §Q3 (premium-in
      mechanism: raw IERC20.transferFrom in try/catch, NOT SafeERC20; handle
      both `false` return and revert as DelegateFailed; funds flow agent ->
      address(pool)), §Q4 (dedup mapping set BEFORE premium-in so
      DelegateFailed dedups), §Q1 steps 3-5, §Q8 Test 5 (revoke between
      events: callIds fill(20) success then fill(21) DelegateFailed,
      agent balance unchanged) and Test 7 (duplicate call_id, callId fill(40)),
      §"Common Pitfalls" pitfalls 1 + 2
    - .planning/phases/04-pactsettler-happy-path/04-GATE-A-DECISIONS.md (E3:
      which CallSettled to emit, E4: event-as-truth — needed because the
      DelegateFailed path emits CallSettled then continues; emit per the
      recorded E3 ruling)
    - packages/program-evm/protocol-evm-v1/src/PactPool.sol lines 32-35,
      48-52 (pool is the USDC custodian / `transferFrom` receiver — premium-in
      target is `address(pool)`)
  </read_first>
  <action>
    RED step FIRST: add to PactSettler.t.sol:
    - `test_DuplicateCallIdRejected`: _register("helius"), _fundPool 5_000_000,
      _provisionAgent(agent, 10_000_000, 10_000_000); event callId fill(40),
      premium=1_000, breach=false. First `vm.prank(settlerSigner);
      settler.settleBatch([ev])` succeeds (no revert). Second identical call:
      `vm.prank(settlerSigner); vm.expectRevert(DuplicateCallId.selector);
      settler.settleBatch([ev])`. (Mirrors 05 "duplicate call_id rejected".)
    - `test_RevokeMarksDelegateFailedAndContinues`: _register, _fundPool,
      _provisionAgent. First batch callId fill(20) premium=1_000 succeeds;
      capture `balanceAfterFirst = usdc.balanceOf(agent)`. Revoke:
      `vm.prank(agent); usdc.approve(address(settler), 0)`. Second batch
      callId fill(21) premium=1_000: `vm.prank(settlerSigner);
      settler.settleBatch([ev2])` MUST NOT revert (the tx succeeds). Assert
      `usdc.balanceOf(agent) == balanceAfterFirst` (unchanged — no premium
      debited), expect a `CallSettled` event with
      `status == IPactSettler.SettlementStatus.DelegateFailed` (vm.expectEmit
      per E3/E4 ruling — emit the IPactSettler typed-enum event), and a third
      retry of callId fill(21) reverts `DuplicateCallId` (dedup set even on
      failure, RESEARCH §Q4).
    Run `forge test --match-test "test_DuplicateCallIdRejected|test_RevokeMarksDelegateFailedAndContinues"`
    — confirm RED (no dedup mapping / no premium-in path yet; the second
    settle does not revert DuplicateCallId, the revoke case currently reverts
    NOT_IMPLEMENTED). Record the exact failure.

    GREEN step in settleBatch, after the Task 1 guards + endpoint snapshot,
    in settle_batch.rs order:
    1. Add storage `mapping(bytes16 => bool) private _settledCallIds;`
    2. Dedup check: `if (_settledCallIds[ev.callId]) revert DuplicateCallId();`
       (hard revert — settle_batch.rs:194-196).
    3. Set sentinel BEFORE premium-in: `_settledCallIds[ev.callId] = true;`
       (settle_batch.rs:243-262 allocates CallRecord before the delegate
       check, so DelegateFailed also dedups — RESEARCH §Q4 / pitfall 2).
    4. Premium-in: raw IERC20 in try/catch (NOT SafeERC20 — pitfall 1):
       ```
       bool premiumInOk;
       try IERC20(usdc).transferFrom(ev.agent, address(pool), ev.premium)
           returns (bool ok) { premiumInOk = ok; }
       catch { premiumInOk = false; }
       ```
    5. On `!premiumInOk`: set `status = SettlementStatus.DelegateFailed`,
       emit the CallSettled event per the recorded E3 ruling (typed-enum
       IPactSettler.CallSettled) with refund=ev.refund, actualRefund=0,
       breach=ev.breach, latencyMs/timestamp from ev, then `continue;` — NO
       funds move, NO pool credit, NO fee fan-out for that event (matches
       settle_batch.rs:332-355). Leave the success-path tail
       (pool credit/fees/refund) for plan 04 — for now a `revert(...)` after
       the premium-in success branch is acceptable ONLY if no green test
       exercises the success tail; since test_DuplicateCallIdRejected's first
       settle DOES need success, you MUST also implement at minimum the
       success path emitting CallSettled(Settled) WITHOUT economics is NOT
       parity-faithful — instead: implement the premium-in transfer + emit on
       failure here, and gate the success tail so plan 04 completes it. To
       keep test_DuplicateCallIdRejected's first settle non-reverting, the
       dedup mapping is set before premium-in, so even if the success tail is
       unfinished the dedup test only needs the first call to NOT revert.
       Therefore implement the FULL premium-in (transfer succeeds, funds move
       agent->pool) and let plan 04 add credit/fees/refund/emit-on-success;
       emit a provisional CallSettled(Settled, actualRefund=0) at loop end so
       the first settle does not revert. Plan 04 replaces the provisional emit
       with the full economic emit. Document this seam explicitly in the
       SUMMARY so plan 04 knows to replace it.
    Run the two matched tests — confirm GREEN. Run FULL `forge test` — 70
    pre-existing tests still green; the Task 1 four guard tests still green.

    No emojis. Conventional file-scoped commits: src
    `feat(protocol-evm-v1): settleBatch dedup mapping + premium-in
    try/catch -> DelegateFailed (SET-02/04, WP-EVM-04)`; test file
    `test(protocol-evm-v1): port 05 duplicate-call-id + revoke-DelegateFailed
    (WP-EVM-04)`.
  </action>
  <verify>
    <automated>cd packages/program-evm/protocol-evm-v1 && forge build && forge test --match-test "test_DuplicateCallIdRejected|test_RevokeMarksDelegateFailedAndContinues" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "mapping(bytes16 => bool) private _settledCallIds" src/PactSettler.sol` succeeds
    - The dedup sentinel is set BEFORE the premium-in try/catch: in src, the line matching `_settledCallIds\[.*\] = true` appears on a lower line number than the line matching `try IERC20(.*).transferFrom(` (verify with `grep -n`)
    - Premium-in uses raw `IERC20(...).transferFrom` in try/catch, NOT SafeERC20: `grep -q "try IERC20" src/PactSettler.sol` succeeds and `grep -q "safeTransferFrom(ev.agent" src/PactSettler.sol` MUST fail
    - `forge test --match-test test_DuplicateCallIdRejected` exits 0: first settle no revert, second reverts `DuplicateCallId`
    - `forge test --match-test test_RevokeMarksDelegateFailedAndContinues` exits 0: second batch does NOT revert, `usdc.balanceOf(agent)` unchanged from after-first, a `CallSettled` event with `status == DelegateFailed` is emitted, and a retry of the same callId reverts `DuplicateCallId`
    - `forge test 2>&1 | grep -c FAIL` == 0; Task 1 guard tests + 70 pre-existing tests still green
    - SUMMARY documents the provisional-emit seam plan 04 must replace
    - RED observed and recorded in SUMMARY before GREEN
  </acceptance_criteria>
  <done>Dedup mapping (set before premium-in) and premium-in try/catch -> DelegateFailed-and-continue implemented per settle_batch.rs order; SET-02/SET-04 ported tests green; provisional success-emit seam documented for plan 04; regression gate held; RED-before-GREEN recorded.</done>
</task>

</tasks>

<verification>
- `cd packages/program-evm/protocol-evm-v1 && forge build` exits 0
- `forge test 2>&1 | grep -c FAIL` == 0
- The 4 ported tests (UnauthorizedSettler, MinPremiumEdge, DuplicateCallId, RevokeMarksDelegateFailedAndContinues) + the 2 inline guard tests green
- Dedup mapping set BEFORE premium-in; premium-in is raw IERC20 try/catch (not SafeERC20)
- No WP-05 guards (endpoint-paused, batch-size) added
- Every code task observed RED before GREEN (recorded in SUMMARY)
</verification>

<success_criteria>
- SET-01: unauthorized caller reverts (OZ AccessControl)
- SET-02: premium-in failure -> DelegateFailed, no funds move, batch continues
- SET-03: three per-event hard-revert guards in settle_batch.rs order
- SET-04: repeated callId -> DuplicateCallId; dedup set even on DelegateFailed
- Zero regressions; provisional success-emit seam documented for plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/04-pactsettler-happy-path/04-03-SUMMARY.md`
recording: RED failure observed per TDD task before GREEN, the exact
settleBatch prologue order vs settle_batch.rs line refs, the provisional
CallSettled(Settled) emit seam plan 04 must replace, and confirmation the
70-test regression gate held.
</output>
